# AI Accountant — Definitive Interaction Map (Mobile + Web)

> Scope: the single Flutter codebase at `D:/Desktop/Ai_Accountant/aiaccountant/lib` (Android mobile **and** Web) and every external system it touches. Backend code referenced from `D:/Desktop/TallyBridge/backend/src`. Grounded in source as of 2026-06-30.

---

## 1. System context

The app is a **thin capture-and-review client**. It talks to four external systems, two of them directly and two through the cloud.

| # | External system | App reaches it… | Transport / auth | What it's for |
|---|-----------------|-----------------|-------------------|----------------|
| 1 | **Firebase Auth** | **Directly** | Phone OTP; `getIdToken()` JWT | Login; the JWT becomes the `Authorization: Bearer` for backend calls |
| 2 | **Supabase (client project)** | **Directly** (anon/publishable key, RLS) | `supabase_flutter` REST + Realtime websocket | Read/write `push_queue`, `scan_jobs`; read `ledgers`, `stock_items`, `voucher_items`; 4 realtime channels |
| 3 | **Main backend** — Cloud Run `tallybridge-backend` | **Via backend** (`ApiClient` / `activateUrl`) | HTTPS + `x-api-key` (+ optional Bearer) | Push-to-Tally activation, stored invoice images, reorder-levels CSV |
| 4 | **Parsing service** — Cloud Run `tallybridge-parsing` | **Directly** (raw PDF POST, fire-and-forget) | HTTPS, `Content-Type: application/pdf` | OCR/VLM parse → it **inserts** a `push_queue` row and **deletes** the `scan_jobs` row |

```
                 ┌────────────────────────────── Firebase Auth (OTP, JWT)
                 │
   ┌─────────────┴─────────────┐
   │   AI Accountant (Flutter)  │──── raw PDF ───▶ Parsing service ──┐ inserts push_queue
   │   Android mobile + Web     │                                    │ deletes scan_jobs
   └──────┬──────────────┬──────┘                                    ▼
          │ direct REST   │ ApiClient (x-api-key + Bearer)      ┌───────────┐
          │ + Realtime    └──────────────────▶ Main backend ───▶│ Supabase  │
          ▼                                    (Cloud Run)       │  client   │
      Supabase client project ◀──────────────────────────────── │  project  │
      (push_queue, scan_jobs, ledgers, stock_items, voucher_items)└───────────┘
```

Tally itself is **never** contacted by the app. Push-to-Tally is asynchronous: the app flips a queue row to `push_now` via the backend, and the desktop TallyBridge engine (separate process) does the actual XML push and writes the result back to `push_queue`, which the app observes over Realtime.

Environment note: default build = **testing** (`yynuu` Supabase). Production = `ztugw`. The reorder-levels endpoint reads a **separate** client Supabase on the backend (`SUPABASE_URL_Client`), distinct from the project the app connects to.

---

## 2. Backend endpoints the app calls

All `ApiClient` calls send headers: `Content-Type: application/json` (dropped for raw/bytes GETs), `x-api-key: Config.mrpApiKey`, `x-request-id: <corr id>`, and `Authorization: Bearer <Firebase JWT>` when a user is signed in. A non-2xx is reported to Sentry with the request id; `401` throws `Unauthorized`.

### Reports surface (`report_screen.dart` via `ApiClient.getRaw`)

| Method | Path | Auth (backend) | Purpose | Error codes |
|--------|------|----------------|---------|-------------|
| GET | `/api/sync/reorder-levels/{reportKey}?company_name=K V ENTERPRISES&format=csv` | `requireClientApiKey` — `x-api-key` timing-safe vs **`API_KEY_CLIENT`**; reads **client** Supabase (`SUPABASE_URL_Client`) | Inventory reorder report as CSV | `400` invalid report key, `409` company lookup (duplicate/missing company), `500` |

> Contract gotcha: the app sends `Config.mrpApiKey` as `x-api-key`. For this endpoint to authorize, `mrpApiKey` must equal the backend's `API_KEY_CLIENT`. `company_name` is **hardcoded** to `K V ENTERPRISES`.

### Voucher detail / image surface

| Method | Path | Auth (backend) | Purpose | Error codes |
|--------|------|----------------|---------|-------------|
| GET | `/api/sync/push-queue/{rowId}/image/{page}` | `requireApiKey` (`x-api-key` **or** Firebase Bearer) | Streams stored scanned-invoice JPEG (page `0..N-1`, N from `source_payload.scan.pages`) | `404` storage not configured / image not found, `400` bad id or page, `500` read failed |
| POST | `/api/sync/push-queue/activate`  body `{ job_id }` | `requireApiKey` | Flips the queue row `pending → push_now` and clears `error_message`; queues the desktop push | `400` no `job_id`, `404` job not found, `409` wrong company / **not `pending`** (msg includes current status) / could-not-activate, `500` |

> Auth-contract nuance: the **activate** call in `voucher_detail_sheet.dart` bypasses `ApiClient._headers` and posts via `ApiClient.client` with only `Content-Type`, `x-api-key: Config.activateApiKey`, and `x-request-id` — **no Firebase Bearer**. Push-to-Tally is therefore authorized purely by the shipped API key. The activate URL is configured separately (`Config.activateUrl`) but currently points at the same Cloud Run service.

---

## 3. Direct Supabase operations & realtime channels

### Table / RPC operations (anon key, RLS)

| Table | Op | Where | Filter / shape | Purpose |
|-------|-----|-------|----------------|---------|
| `push_queue` | SELECT | `PushQueueService.refresh` | `status in (pending, push_now, failed)`, `order created_at desc`; cols `id,status,created_at,voucher_payload,source_payload,edit_state,pushed_at,error_message` | Load the live queue |
| `push_queue` | SELECT | `HistoryScreen` | `status = pushed`, `order pushed_at desc` | History list |
| `push_queue` | SELECT (single) | `HistoryScreen._ingestPushedRow` | `eq id` | Re-fetch full row when Realtime UPDATE omitted TOASTed `voucher_payload` |
| `push_queue` | UPDATE | `_persistEditsToSupabase` | `{voucher_payload}` `eq id` **`eq status pending`**, `.select('id')` to confirm | Save voucher edits (only while pending) |
| `push_queue` | UPDATE | `_activate` (pre-push) | `{voucher_payload}` `eq id` (**no status guard**) — overwrites narration to `Replara AI` | Persist narration before activate |
| `push_queue` | DELETE | `_confirmAndDiscard` | `eq id` | Discard a queued voucher |
| `scan_jobs` | INSERT | `ScanJobsService.startScan` | `{type}` `.select(id,type,created_at).single()` | Record an in-flight scan (badge +1); returns `job_id` |
| `scan_jobs` | SELECT | `ScanJobsService._refresh` | `gte created_at (now-300s)` | Catch-up / resync badge state |
| `ledgers` | SELECT | `CustomersCache` | `eq group_name 'Sundry Debtors'`; cols `name,group_name,state` | Customer picker (sale party) |
| `ledgers` | SELECT | `VendorsCache` | `eq group_name 'Sundry Creditors'` | Vendor picker (purchase party) |
| `stock_items` | SELECT | `StockItemsCache.fetch` | cols `name,group_name,rate,unit,part_code`, `order name`, `.range()` paginated **1000/page** (~13k rows) | Stock catalog (PURCHASE picker; unit/part-code lookups) |
| `voucher_items` | SELECT | SALE stock picker | party-aware history (per-party item rows; no `part_code`) | Stock picker for SALE lines |

> Parsing service writes (server-side, not the app): **INSERT** `push_queue` invoice row + **DELETE** `scan_jobs` row when an invoice lands.

### Realtime channels

| Channel | Table / events | Filter | Consumer & effect |
|---------|----------------|--------|-------------------|
| `push_queue_live` | `push_queue` INSERT / UPDATE / DELETE | active-status only on insert | Live queue list; row leaves only on `status=pushed`; UPDATE keeps prior `edit_state` if the echo omits the column |
| `scan_jobs_live` | `scan_jobs` INSERT / DELETE | — | "Processing…" badge +1 / −1 on **every** client; 300 s sweep + foreground `resync` guard stuck badges |
| `history_push_queue` | `push_queue` INSERT / UPDATE / DELETE | — | History list (`status=pushed`); re-fetches full row when payload omitted |
| `vd_status_<rowId>` | `push_queue` UPDATE | `eq id = rowId` | Per-voucher push status while the detail sheet is open; drives the pushing→result transition |

---

## 4. Critical end-to-end sequences

### (a) Login / OTP
1. `LoginScreen`: user enters 10-digit number; app prefixes **`+91`** and calls `FirebaseAuth.verifyPhoneNumber` (with `forceResendingToken` on resend).
2. `codeSent` → navigate to `OtpScreen` with `verificationId` (Android may auto-verify via `verificationCompleted`).
3. `OtpScreen`: 6 digits (on-screen keypad on narrow screens; physical keyboard + Ctrl/⌘-V paste on desktop) → `PhoneAuthProvider.credential` → `signInWithCredential`.
4. `main.dart` `StreamBuilder(authStateChanges)` flips Splash → `AccountantShell`. On web refresh the persisted session restores via the same stream.
5. `_wireSessionAndSentryUser` (uid-guarded): on sign-in, eagerly `fetch()` `StockItemsCache` + `CustomersCache` + `VendorsCache` and tag the Sentry user; on sign-out, `clear()` all three.
6. Subsequent backend calls attach `Authorization: Bearer <getIdToken()>`.

### (b) Scan → Parse → Queue  *(scan step is Android-only)*
1. `AppShell`: user picks Sale/Purchase (`CaptureTypeDialog`). On web/iOS a snackbar says scanning is Android-only and the flow stops.
2. ML Kit `DocumentScanner` (jpeg+pdf, up to 20 pages, gallery import) returns a PDF path; a local `CapturedShot` is added.
3. `_enqueueScan`: shows "being processed…" snackbar, then `ScanJobsService.startScan(type)` **inserts a `scan_jobs` row** → `scan_jobs_live` INSERT makes the badge climb on every client; returns the row id as **`job_id`**.
4. `sendScanToParser(pdfPath, url)` POSTs the raw PDF (fire-and-forget, 300 s timeout) to:
   - Sale: `…/?type=sale&push=queue&job_id=<id>`
   - Purchase: `…/docstrange?purchase=all&source=runpod&job_id=<id>`
5. Parsing service parses, **inserts a `push_queue` row** and **deletes the `scan_jobs` row** (`job_id`).
6. App effects: `push_queue_live` INSERT adds the new active row to the queue; `scan_jobs_live` DELETE drains the badge everywhere. If the parse never lands, the 300 s `_maxAge` sweep expires the badge (no queue row appears).

### (c) Edit → Push-to-Tally → result
1. Queue row → `VoucherDetailSheet`. **Edit** is allowed only while `status='pending'` and after the stock cache finishes loading.
2. Edits mutate editable copies; charges rescale client-side (`inventory_ledger` gets new taxable, tax ledgers scaled by captured `_taxRatios`, discount/total recomputed). **Save** writes `voucher_payload` back via `push_queue` UPDATE guarded `eq status pending` (empty result ⇒ "may already be queued"). A vendor change also renames the party ledger line in `ledger_entries`.
3. **Push To Tally** (`_confirmAndActivate`): if still editing, prompts to Save first. **Guard:** if the party name changed but **not** all items changed, push is blocked ("Party Name is Changed!! Change all the items") — prevents posting items against the wrong ledger.
4. `_activate`: overwrites `narration='Replara AI'`, UPDATEs `voucher_payload` on the row (`eq id`), then POSTs `/api/sync/push-queue/activate {job_id: rowId}` with `x-api-key`+`x-request-id`.
5. Backend flips `pending → push_now`, clears `error_message`, returns `{success, job}`. App enters `_awaitingPushResult` (spinner; sheet stays open).
6. Desktop TallyBridge pushes to TallyPrime and writes the outcome to `push_queue`. The `vd_status_<rowId>` UPDATE delivers it: success = `status=pushed` or `tally_response.created ≥ 1`; failure = `status=failed` or `created=0 && errors ≥ 1` (with `error_message`).
7. On terminal: `onPushed` re-fetches the queue (pushed rows drop to History, failed rows stay), the sheet closes, and **sale** vouchers show a success/fail dialog. A race where the terminal update arrived before the POST returned is handled by an immediate resolve.

### (d) Report fetch (Reorder-levels)
1. `ReportScreen` → tap a category with a `reportId`.
2. Web: always `ApiClient.getRaw('/api/sync/reorder-levels/<reportId>', {company_name:'K V ENTERPRISES', format:'csv'})` (no disk cache). Mobile: serve `<reportId>.csv` from app-documents cache if present, else fetch and write it.
3. Backend `requireClientApiKey` (vs `API_KEY_CLIENT`) reads the **client** Supabase and returns CSV; `400` bad key, `409` company lookup.
4. CSV parsed → `AppSpreadsheetSheet` (sum column = `closing_stock_amount`). Share is available on mobile only (relies on the disk file; `null` on web).

---

## 5. Mobile vs Web differences

| Aspect | Android (mobile) | Web |
|--------|------------------|-----|
| Document scanning | ML Kit `DocumentScanner` (the only scan path) | Disabled — snackbar "only available on Android" |
| Text scaling | Native | Global `TextScaler(1.4)`; sheet numeric columns widened ×1.4 |
| Report CSV cache | On-disk (`getApplicationDocumentsDirectory`) | No `path_provider` → always fetch, no disk cache |
| Invoice image cache | On-disk `InvoiceImageStore` keyed by row id | No on-disk cache (images fetched from backend) |
| File share (CSV) | `share_plus` via cached file | `onShare = null` (no file to share) |
| Navigation / layout | Bottom nav; single-pane sheet with Image/Summary tabs | ≥900 px → side rail + **two-pane** image+summary; sheet width ~90% |
| OTP entry | On-screen numeric keypad | Physical keyboard typing, Enter to verify, Ctrl/⌘-V paste |
| Session restore | Cold start with persisted session | Restored on refresh via `authStateChanges` StreamBuilder |
| Badge realtime | Climbs on own scan + every other client | Climbs/drains on others' scans; no scanning of its own |

---

## 6. Data-integrity & money-path hazards

- **Hardcoded report company.** Reorder-levels always sends `company_name='K V ENTERPRISES'`. Duplicate company rows in the client project (`ztugw`) surface as **`409` "Failed to load report"** (known incident).
- **Unguarded narration overwrite.** `_activate` UPDATEs `voucher_payload` with `eq id` only (no `status='pending'` guard, unlike Save). If another client/desktop already moved the row off `pending`, this can overwrite the payload underneath it just before the backend's pending check fails.
- **Realtime column omission (TOAST / cache lag).** UPDATE echoes can drop the large `voucher_payload` (History re-fetches the full row, else shows "Unknown") and can drop the freshly-added `edit_state` (queue keeps the prior value). Both are mitigated in code but depend on those guards staying correct.
- **Push result depends on Realtime.** The sheet stays in a "pushing…" state until `vd_status_<rowId>` delivers a terminal row. A dropped socket leaves the spinner up; `onPushed`'s queue re-fetch is the recovery path. Success/fail dialog is **sale-only**.
- **Client-side money math.** Queue/History amount = `abs(first ledger_entry.amount)`; charges total = `abs(party-entry amount)`. Edits recompute taxable, scale tax ledgers **proportionally** (`_taxRatios`), and re-derive discount/total in the client before the payload is pushed — rounding and proportional scaling happen app-side, not in Tally.
- **Party/items consistency guard.** Changing only the party (not the items) blocks the push; on save the party ledger line is renamed to match — a mismatch here would post a voucher against the wrong vendor ledger.
- **Edit window race.** Edits are accepted only while `status='pending'`; the Save UPDATE returns rows to confirm, and an empty result yields "may already be queued for push." A row claimed by the desktop between Save and Push is intentionally left untouched.
- **Badge is table-derived, not counted.** `+1` on `scan_jobs` INSERT, `−1` on the parser's DELETE; a missed DELETE is swept after 300 s. A scan whose parse silently fails leaves **no** queue row — the only signal was the badge, which auto-expires.
- **Shipped API keys.** A publishable-looking `x-api-key` is compiled into the client bundle and is the **sole** auth for the activate (push-to-Tally) call; `mrpApiKey` must double as `API_KEY_CLIENT` for reports. Security rests on RLS (Supabase) and these keys, not on the Firebase JWT for those paths.

---

# AI Accountant (Mobile + Web) ↔ Backend — Prioritized Manual Test Plan

> 494 manual test cases, sorted P0→P3. Platform tag = mobile | web | both. Run P0 first (money & data-integrity), then P1 (core flows), then P2/P3. Generated 2026-06-30 from a code-grounded 8-domain workflow.

## Coverage summary

- **Total cases:** 494
- **By priority:** P0 59 · P1 144 · P2 207 · P3 84
- **By platform:** mobile 66 · web 59 · both 369
- **By domain:** auth_session 50 · scan_parse 59 · queue_realtime 76 · voucher_edit_push 85 · history_images 57 · reports 48 · masters_caches 47 · crosscutting 72

## Test cases by priority

### P0 — Critical (money / data-integrity / total breakage) (59)

| ID       | Title                                                                                                         | Platform | Domain            | Category       | Preconditions                                                                                                                                                         | Steps                                                                                                                                                                                                                                                                            | Expected                                                                                                                                                                                                                                                                                                                                                                                                                                   | Integration points                                                                                                                                              |
| -------- | ------------------------------------------------------------------------------------------------------------- | -------- | ----------------- | -------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | --------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| AUTH-1   | Happy path: send OTP with a valid 10-digit number (mobile)                                                    | mobile   | auth_session      | happy          | App freshly launched on Android, signed out, LoginScreen visible, valid test phone number with SMS reception.                                                         | 1. Type a valid 10-digit mobile number into the phone field (note the +91 prefix is fixed and non-editable). 2. Tap 'Send OTP →'. 3. Observe the button/progress state, then wait for the SMS.                                                                                   | Button disables, a LinearProgressIndicator appears while loading; on codeSent the app slides to OtpScreen showing 'sent to +91 NNxxx xxxxx' (first 2 digits visible, rest masked). An SMS with a 6-digit code arrives.                                                                                                                                                                                                                     | firebase                                                                                                                                                        |
| AUTH-2   | Happy path: manual OTP entry + verify via on-screen keypad (mobile)                                           | mobile   | auth_session      | happy          | On OtpScreen (mobile, width < 900) after a successful Send OTP; valid 6-digit code received via SMS.                                                                  | 1. Tap the on-screen numeric keypad to enter all 6 digits of the received code. 2. Confirm focus advances box-to-box and the last box stops advancing at index 5. 3. Tap 'Verify →'.                                                                                             | LinearProgressIndicator shows during sign-in; on success the screen fades (pushReplacement) to SuccessScreen ('you're in!'), then the gate resolves to AccountantShell. No error banner.                                                                                                                                                                                                                                                   | firebase                                                                                                                                                        |
| AUTH-3   | Happy path: Android instant SMS auto-verify (verificationCompleted)                                           | mobile   | auth_session      | happy          | Android device/number where Firebase SMS auto-retrieval works; app on LoginScreen.                                                                                    | 1. Enter the valid 10-digit number and tap 'Send OTP →'. 2. Do NOT type the code; wait for Android to auto-retrieve the SMS.                                                                                                                                                     | verificationCompleted fires, signInWithCredential succeeds, and the app navigates to SuccessScreen WITHOUT visiting/typing on OtpScreen. (Note: this uses push not pushReplacement, so LoginScreen remains beneath — verify Back behavior after Success animation in the dedicated case.)                                                                                                                                                  | firebase                                                                                                                                                        |
| AUTH-4   | Web: OTP entry via physical keyboard, Enter to verify (web/desktop)                                           | web      | auth_session      | platform       | App in a desktop browser at width >= 900; on OtpScreen with a valid code received on the phone bound to that number.                                                  | 1. Confirm the on-screen numeric keypad is NOT rendered (wide screen hides _NumKeypad). 2. Type the 6 digits on the physical keyboard (0-9 only). 3. Press Enter (or NumpadEnter).                                                                                               | Digits fill the boxes left-to-right; pressing Enter triggers _verify and signs in, fading to SuccessScreen then AccountantShell. Non-digit keys are ignored.                                                                                                                                                                                                                                                                               | firebase                                                                                                                                                        |
| AUTH-22  | Happy path: Success animation completes and hands off to Shell                                                | both     | auth_session      | happy          | Just signed in (manual verify path); SuccessScreen visible.                                                                                                           | 1. Watch the 1400ms progress bar complete. 2. After it reaches 100%, press device/browser Back.                                                                                                                                                                                  | On AnimationStatus.completed, Navigator.popUntil(isFirst) discards Success/OTP/Login and lands on the reactive gate (AccountantShell). Pressing Back from the Shell exits the app (mobile) rather than returning to Login.                                                                                                                                                                                                                 | firebase                                                                                                                                                        |
| AUTH-24  | Session restore on cold start with a persisted session (mobile)                                               | mobile   | auth_session      | happy          | Previously signed in on the device; app fully killed (swiped from recents).                                                                                           | 1. Relaunch the app from the launcher. 2. Observe the first screen(s).                                                                                                                                                                                                           | StreamBuilder shows SplashScreen during ConnectionState.waiting, then authStateChanges emits the persisted User and the app opens directly to AccountantShell — no LoginScreen, no re-OTP.                                                                                                                                                                                                                                                 | firebase, supabase                                                                                                                                              |
| AUTH-25  | Session restore on web browser refresh                                                                        | web      | auth_session      | platform       | Signed in in a web browser tab.                                                                                                                                       | 1. Press F5 / browser refresh on the app tab. 2. Observe the screens.                                                                                                                                                                                                            | SplashScreen briefly during waiting, then the gate (auth stream, not a one-time currentUser read) restores the session and renders AccountantShell. User is NOT bounced to LoginScreen.                                                                                                                                                                                                                                                    | firebase, supabase                                                                                                                                              |
| AUTH-27  | Sign out clears caches and returns to Login                                                                   | both     | auth_session      | happy          | Signed in; on ProfileScreen.                                                                                                                                          | 1. Tap 'Sign out'. 2. Observe navigation and re-check cached data screens.                                                                                                                                                                                                       | FirebaseAuth.signOut() flips the auth stream to null; the gate renders LoginScreen at the base route (no manual navigation). The three caches are .clear()'d and Sentry user is nulled. Re-entering protected screens shows empty/reloading state.                                                                                                                                                                                         | firebase, supabase, stock_items, ledgers                                                                                                                        |
| AUTH-33  | SECURITY: activate push-to-Tally is authenticated by x-api-key only (no Bearer JWT)                           | both     | auth_session      | security       | Knowledge that POST /api/sync/push-queue/activate is hand-rolled in voucher_detail_sheet.dart with x-api-key=Config.activateApiKey and no Authorization header.       | 1. Trigger a voucher push and capture the activate request headers. 2. Separately, replay the activate POST with only the bundled x-api-key and NO Firebase Bearer (e.g. via curl) against the backend. 3. Confirm whether the call succeeds without a valid Firebase session.   | Defect to flag: the highest-value action (queue a Tally voucher push) succeeds with only the shipped x-api-key and no user JWT — a signed-out or key-holding client can enqueue money-moving pushes. Confirm and document; recommend requiring the Bearer JWT here.                                                                                                                                                                        | main-backend, /api/sync/push-queue/activate, requireApiKey, push_queue                                                                                          |
| SCAN-5   | Choose Sale routes to the sale parse URL and Sale badge tab                                                   | mobile   | scan_parse        | happy          | Android phone, logged in, Tally party + stock exist server-side for the invoice.                                                                                      | 1. Start capture, choose Sale 2. Complete a scan of a valid sale invoice 3. Watch the Queue Sale tab badge and (server-side) the parse target                                                                                                                                    | Parse POST goes to Config.saleParseUrl (/?type=sale&push=queue) with &job_id=<row id> appended; the +1 Processing badge appears on the Sale tab; after parse, a sale push_queue row appears.                                                                                                                                                                                                                                               | saleParseUrl, scan_jobs table, scan_jobs_live, push_queue                                                                                                       |
| SCAN-6   | Choose Purchase routes to the purchase parse URL and Purchase badge tab                                       | mobile   | scan_parse        | happy          | Android phone, logged in.                                                                                                                                             | 1. Start capture, choose Purchase 2. Complete a scan of a valid purchase invoice 3. Watch the Queue Purchase tab badge and parse target                                                                                                                                          | Parse POST goes to Config.purchaseParseUrl (/docstrange?purchase=all&source=runpod) with &job_id appended; the Processing badge appears on the Purchase tab; after parse, a purchase push_queue row appears.                                                                                                                                                                                                                               | purchaseParseUrl, scan_jobs table, scan_jobs_live, push_queue                                                                                                   |
| SCAN-9   | Android passes the platform gate and opens the ML Kit scanner                                                 | mobile   | scan_parse        | happy          | Android phone with camera permission granted.                                                                                                                         | 1. Open capture flow, choose a type 2. Observe the native UI that launches                                                                                                                                                                                                       | The Google ML Kit DocumentScanner opens (full mode, gallery-import enabled, pageLimit 20, jpeg+pdf formats).                                                                                                                                                                                                                                                                                                                               | DocumentScanner                                                                                                                                                 |
| SCAN-10  | Successful scan adds a capture and fires the enqueue                                                          | mobile   | scan_parse        | happy          | Android phone; scanner open.                                                                                                                                          | 1. Capture a document and confirm/finish in the scanner so a PDF is produced 2. Observe the capture tray thumbnail count and the processing snackbar                                                                                                                             | A CapturedShot is added (tray badge increments), the 'Your <type> voucher is being processed…' snackbar (2s) shows, a scan_jobs row is inserted and the parse POST is dispatched.                                                                                                                                                                                                                                                          | DocumentScanner, scan_jobs table, saleParseUrl/purchaseParseUrl                                                                                                 |
| SCAN-20  | Unauthenticated parse endpoint can be triggered by anyone with the URL                                        | both     | scan_parse        | security       | Tester knows the saleParseUrl/purchaseParseUrl; a sample PDF.                                                                                                         | 1. From an unauthenticated tool (curl/Postman) POST a PDF with Content-Type application/pdf to the parse URL 2. Check whether a push_queue voucher row is created                                                                                                                | Document this as a risk: the call is accepted with no API key/Bearer, the parser builds a voucher and inserts a push_queue row that can subsequently be pushed into TallyPrime. Confirms the only auth boundary is parsing-service→main-backend, not app→parsing-service.                                                                                                                                                                  | parsing-service, push_queue, main-backend ingest                                                                                                                |
| SCAN-24  | Happy path end-to-end: scan → parse → push_queue row → badge drains on every client                           | both     | scan_parse        | happy          | Android phone AND a web session both logged into the same Supabase project; invoice with a matchable party + stock.                                                   | 1. On the phone, scan a valid invoice 2. Watch both the phone and the web Processing badge climb 3. Wait for the parse to finish                                                                                                                                                 | Both clients show +1 immediately; when the parsing service inserts the push_queue row and DELETEs the scan_jobs row, both clients drain -1 within realtime latency and the new voucher row appears in the queue on both.                                                                                                                                                                                                                   | scan_jobs table, scan_jobs_live, push_queue, parsing-service, main-backend ingest                                                                               |
| SCAN-25  | SILENT INVOICE DROP: parse produces no voucher (no party/stock match)                                         | both     | scan_parse        | data-integrity | Android phone; scan an invoice whose party name does not match any ledger and/or whose line items match no stock (so queue_response.skipped=true).                    | 1. Scan such an invoice 2. Watch the badge for ~5 minutes 3. Check the queue for any new row and check for any error message                                                                                                                                                     | No push_queue row is ever created and the scan_jobs row is NOT deleted; the badge silently drains at the 300s TTL with NO error toast, dialog, or record. Confirms the highest-risk gap: a scanned money document is lost with zero user signal.                                                                                                                                                                                           | parsing-service, scan_jobs table, push_queue                                                                                                                    |
| SCAN-26  | SILENT DROP on purchase: push_queue_request_payload not produced                                              | mobile   | scan_parse        | data-integrity | Android phone; scan a purchase invoice the docstrange/RunPod pipeline cannot turn into a payload dict.                                                                | 1. Scan the unparseable purchase invoice 2. Wait past 300s; check queue and any error                                                                                                                                                                                            | skipped=true, no delete_scan_job, no push_queue row; badge drains at TTL with no user-facing error. Invoice silently lost.                                                                                                                                                                                                                                                                                                                 | purchaseParseUrl, scan_jobs table, push_queue                                                                                                                   |
| SCAN-27  | Backend push-queue ingest down: voucher never lands, scan_jobs not deleted                                    | both     | scan_parse        | error          | Stop/break the main backend push-queue ingest (so post_to_push_queue fails) while parsing still runs.                                                                 | 1. Scan a valid invoice 2. Wait past 300s; check queue and badge                                                                                                                                                                                                                 | Parser cannot insert the push_queue row; the scan_jobs row is not deleted; badge drains only at the 300s TTL; no voucher appears and the user gets no error. Documented as silent loss when the ingest is unavailable.                                                                                                                                                                                                                     | main-backend ingest, parsing-service, push_queue, scan_jobs table                                                                                               |
| SCAN-36  | Phone scan climbs the badge on an already-open web session                                                    | both     | scan_parse        | concurrency    | Web session and an Android phone both logged into the SAME Supabase project; web on the Queue screen.                                                                 | 1. On the phone, scan an invoice 2. Without touching the web session, watch its Processing badge                                                                                                                                                                                 | The web client's Processing badge climbs +1 via the scan_jobs_live INSERT echo even though the web client never scans, then drains -1 when the parse deletes the row.                                                                                                                                                                                                                                                                      | scan_jobs_live, scan_jobs table                                                                                                                                 |
| SCAN-38  | DUPLICATE ENQUEUE: double-tap capture creates two scan_jobs and two push_queue rows                           | mobile   | scan_parse        | data-integrity | Android phone.                                                                                                                                                        | 1. Scan the SAME invoice twice (or double-trigger the capture so two PDFs are produced) 2. Wait for both parses to complete and inspect the queue                                                                                                                                | Two scan_jobs rows and two parse requests are produced, yielding two push_queue voucher rows for the same invoice — a double-posting hazard. Confirms there is no client-side de-duplication of identical scans.                                                                                                                                                                                                                           | scan_jobs table, push_queue, parsing-service                                                                                                                    |
| SCAN-39  | DUPLICATE PURCHASE: invoice_exists flagged but voucher still enqueued                                         | mobile   | scan_parse        | data-integrity | Android phone; a purchase invoice whose invoice number already exists in the system.                                                                                  | 1. Scan the already-existing purchase invoice 2. Inspect the resulting push_queue row and any duplicate flag                                                                                                                                                                     | The purchase pipeline's duplicacy check sets invoice_exists=true but STILL enqueues the voucher; the duplicate is NOT blocked and relies on the user noticing the flag before pushing to Tally. Sales have no such gate at all.                                                                                                                                                                                                            | purchaseParseUrl, push_queue                                                                                                                                    |
| QUEUE-1  | Initial queue load renders active rows sorted newest-first, grouped by day                                    | both     | queue_realtime    | happy          | Signed in. push_queue contains several rows with status pending/push_now/failed spanning Today, Yesterday, and an older date; at least one 'pushed' row also exists.  | 1. Launch the app (mobile) or open the web URL and sign in. 2. Navigate to the Queue screen and stay on the Sale tab, then switch to Purchase tab.                                                                                                                               | Only rows with status pending/push_now/failed appear; the 'pushed' row is NOT in the queue. Rows are grouped under 'Today', 'Yesterday', then '<d Mon>' headers, each group ordered newest-first by created_at. Serial numbers run 1..N continuously across the whole active tab (not restarting per day group). Each row shows party name, abs(first ledger amount), time label.                                                          | supabase:push_queue, realtime:push_queue_live                                                                                                                   |
| QUEUE-5  | Multi-client realtime INSERT: scan on phone appears in web queue                                              | both     | queue_realtime    | concurrency    | Same tenant signed in on an Android phone AND a web browser, both on the Queue screen (Purchase tab).                                                                 | 1. On the phone, scan a purchase invoice (parsing service eventually inserts a push_queue row). 2. Watch the web session without refreshing.                                                                                                                                     | When the parsing service inserts the push_queue row, the push_queue_live INSERT echo prepends the new row on BOTH the phone and the web session live (no manual refresh). The row appears at the top of its day group.                                                                                                                                                                                                                     | supabase:push_queue, realtime:push_queue_live, parsing-service, supabase:scan_jobs                                                                              |
| QUEUE-9  | Realtime UPDATE to 'pushed' removes the row from the queue and it lands in History                            | both     | queue_realtime    | happy          | A push_now row whose desktop push succeeds.                                                                                                                           | 1. Wait for the desktop poller to push the voucher to TallyPrime and flip status to 'pushed'. 2. Observe the Queue and then open History.                                                                                                                                        | push_queue_live UPDATE with status=='pushed' removes the row from the live queue on every client; the row appears in History (history_push_queue), ordered by pushed_at desc.                                                                                                                                                                                                                                                              | supabase:push_queue, realtime:push_queue_live, realtime:history_push_queue                                                                                      |
| QUEUE-25 | Happy push: pending SALE -> activate -> push_now -> pushed shows success dialog and drops to History          | both     | queue_realtime    | happy          | Pending SALE row; desktop TallyBridge online and TallyPrime reachable.                                                                                                | 1. Open the SALE row, tap Push To Tally, confirm in the dialog. 2. Wait for the desktop to push and the realtime result to arrive.                                                                                                                                               | Pre-push: voucher_payload persisted (narration overwritten to 'Replara AI'), POST /api/sync/push-queue/activate returns 2xx, button keeps spinner (_awaitingPushResult). Backend flips pending->push_now. vd_status_<rowId> UPDATE reports status 'pushed' or tally_response.created>=1. onPushed re-fetches; after the sheet closes a _SalePushedDialog shows customer/amount/pushed-at. The row leaves the queue and appears in History. | supabase:push_queue, POST /api/sync/push-queue/activate, realtime:vd_status_<rowId>, main-backend                                                               |
| PUSH-15  | Party rename targets largest-abs ledger (wrong-ledger risk on tax-heavy voucher)                              | both     | voucher_edit_push | data-integrity | A multi-ledger voucher where the inventory/GST-Sale subtotal exceeds the party (debtor/creditor) line in absolute value.                                              | 1. Open the row, Edit, change the party 2. Save 3. Inspect voucher_payload.ledger_entries in push_queue (Supabase)                                                                                                                                                               | DEFECT WATCH: the renamed-to-new-party ledger must be the actual party line, not the GST/inventory line. If the largest-abs line is the inventory subtotal, the wrong ledger gets renamed and _editableTotal is assigned to it — the voucher would post against the wrong ledger/amount. Flag any mismatch.                                                                                                                                | supabase:push_queue                                                                                                                                             |
| PUSH-27  | amount>gross discount-clamp silently raises an untouched line on first edit                                   | both     | voucher_edit_push | data-integrity | A line where amount > qty*rate (price uplift / negative discount), so derived disc clamps to 0.                                                                       | 1. Open the row, Edit 2. Touch ANY cell on that line (e.g. change qty by +0 then back, or edit a different cell that triggers _recalcAmount) 3. Read the line amount                                                                                                             | DEFECT WATCH: because disc was clamped to 0, _recalcAmount=qty*rate*(1-0) silently LOWERS/changes the taxable amount to gross, changing the pushed value of an otherwise-untouched uplift line. Flag the silent value change.                                                                                                                                                                                                              | supabase:push_queue                                                                                                                                             |
| PUSH-28  | Proportional tax ledgers stay fixed when inventory ledger unmatched                                           | both     | voucher_edit_push | data-integrity | A voucher where the inventory/GST-Sale ledger name does not match so _taxRatios are not built (invLedger unmatched).                                                  | 1. Edit item amounts (change qty/rate) 2. Inspect CGST/SGST ledger amounts vs item totals in Charges and in the saved payload                                                                                                                                                    | DEFECT WATCH: CGST/SGST remain at original values while item amounts changed, so ledger_entries total no longer reconciles with the pushed item lines. Flag the imbalance.                                                                                                                                                                                                                                                                 | supabase:push_queue                                                                                                                                             |
| PUSH-30  | Save persists edits only while status=pending (status-guarded)                                                | both     | voucher_edit_push | happy          | Row in edit mode, status=pending.                                                                                                                                     | 1. Make an edit 2. Tap Save 3. Verify push_queue.voucher_payload in Supabase                                                                                                                                                                                                     | push_queue.update sets voucher_payload (with all __-prefixed keys stripped) where id=row AND status='pending'; .select('id') returns the row; success state; __-keys are not persisted.                                                                                                                                                                                                                                                    | supabase:push_queue                                                                                                                                             |
| PUSH-37  | Push happy path: confirm, activate 2xx, await realtime, sale success modal                                    | both     | voucher_edit_push | happy          | Sale pending row, not editing, backend API_KEY matches activateApiKey, desktop+Tally healthy.                                                                         | 1. Tap Push To Tally 2. Confirm 'Are You Sure' 3. Wait for the realtime result                                                                                                                                                                                                   | voucher_payload written (narration='Replara AI'), POST /api/sync/push-queue/activate {job_id} returns 2xx, spinner stays, status flips to push_now then pushed via vd_status_{rowId}; sheet pops; sale success dialog shown after sheet closes; onPushed triggers queue re-fetch.                                                                                                                                                          | supabase:push_queue, main-backend:POST /api/sync/push-queue/activate, realtime:vd_status_{rowId}                                                                |
| PUSH-42  | Activate pre-push write has NO status guard (race overwrites mid-push payload)                                | both     | voucher_edit_push | data-integrity | Two actors: this sheet about to push; desktop has already flipped the row to push_now/pushed.                                                                         | 1. Trigger Push on the sheet at the moment the desktop is mid-pushing the same row 2. Inspect the row after the activate POST returns 409                                                                                                                                        | DEFECT WATCH: _activate's update {voucher_payload, narration='Replara AI'} uses .eq('id') ONLY (no status guard) and is NOT rolled back when the POST returns 409; the row can be left with a mutated payload + forced narration that no longer matches its status. Flag the unrolled-back write.                                                                                                                                          | supabase:push_queue, main-backend:POST /api/sync/push-queue/activate                                                                                            |
| PUSH-43  | Activate returns 401 (api-key mismatch) shows only generic failure                                            | both     | voucher_edit_push | security       | Backend process.env.API_KEY does NOT equal the app's Config.activateApiKey (publishable key).                                                                         | 1. Tap Push, confirm 2. Observe result                                                                                                                                                                                                                                           | DEFECT WATCH: Push fails with generic snackbar 'Failed (401): ...'; the preceding voucher_payload+narration write is already committed and not rolled back; reportFailure fired. Every push is broken until the env keys match. Flag the coupling.                                                                                                                                                                                         | main-backend:POST /api/sync/push-queue/activate, supabase:push_queue                                                                                            |
| HIST-51  | No company scoping: a single project with two companies mixes all pushed vouchers                             | both     | history_images    | security       | A Supabase project whose push_queue contains pushed rows for TWO different companies and whose RLS does NOT segregate by company (or test user can see both).         | 1. Sign in and open History. 2. Inspect the parties/vouchers listed.                                                                                                                                                                                                             | History query is only .eq('status','pushed') with NO company_id/user filter, so it shows BOTH companies' pushed vouchers intermixed. Scoping relies ENTIRELY on RLS + per-tenant project. If RLS is mis-set, this is a cross-company data-exposure/integrity defect — verify RLS strictly limits visible rows to the signed-in tenant/company.                                                                                             | Supabase: push_queue select (no company filter), Supabase RLS                                                                                                   |
| RPT-3    | Open report R1 (act_now) on WEB returns CSV and renders spreadsheet                                           | web      | reports           | happy          | Signed in on web. Backend reachable; API_KEY_CLIENT set; client Supabase has a single 'K V ENTERPRISES' company with last_synced_at populated and stock/voucher data. | 1. On web, tap the 'act_now' (🚨) report row. 2. Observe the spinner on the tapped row. 3. Wait for the bottom sheet.                                                                                                                                                            | A single GET /api/sync/reorder-levels/R1?company_name=K V ENTERPRISES&format=csv is sent (headers x-api-key=mrpApiKey, x-request-id present, Authorization Bearer if signed in, no Content-Type). Backend returns 200 text/csv. AppSpreadsheetSheet opens at 92% height with column headers, data rows, footer '/act_now.csv', and a live Σ over closing_stock_amount. No share button on web.                                             | main-backend GET /api/sync/reorder-levels/:reportKey, supabase: companies (reorderSupabase), supabase: vouchers, supabase: voucher_items, supabase: stock_items |
| RPT-20   | 401 Unauthorized (x-api-key mismatch or API_KEY_CLIENT unset) -> 'Unauthorized' snackbar                      | both     | reports           | error          | Backend deployed with API_KEY_CLIENT unset OR app Config.mrpApiKey not matching backend API_KEY_CLIENT.                                                               | 1. Tap any report row. 2. Observe the snackbar.                                                                                                                                                                                                                                  | Backend requireClientApiKey rejects -> 401. getRaw maps 401 to Exception('Unauthorized'); snackbar reads 'Failed to load report: Exception: Unauthorized'. Reports are completely unusable. Verify this is independent of API_KEY used by other endpoints (a deploy setting API_KEY but forgetting API_KEY_CLIENT breaks ALL reports while the rest of the app works).                                                                     | main-backend GET /api/sync/reorder-levels/:reportKey, requireClientApiKey / API_KEY_CLIENT, Sentry                                                              |
| RPT-22   | 409 duplicate 'K V ENTERPRISES' rows -> every report tap fails (KNOWN ztugw P0)                               | both     | reports           | data-integrity | Client/prod Supabase (ztugw) contains two or more companies rows named 'K V ENTERPRISES' (e.g. the orphan guid-null row).                                             | 1. Tap any report row (any of R1-R7). 2. Observe the result for several reports.                                                                                                                                                                                                 | resolveCompanyLookup .limit(2) sees >1 rows -> 409 'Multiple companies share this name...'. App shows generic 'Failed to load report: Exception: Request failed: 409' for EVERY report. This is the known production breakage; fix is removing the duplicate company row in ztugw. The user cannot distinguish this from the not-yet-synced 409.                                                                                           | main-backend GET /api/sync/reorder-levels/:reportKey, supabase: companies (reorderSupabase)                                                                     |
| RPT-31   | PRIVACY: second user on same device sees first user's cached report                                           | mobile   | reports           | security       | Android shared device. User A signs in and opens a report (caches /reports/<id>.csv).                                                                                 | 1. User A opens R3 (caches it), then signs out. 2. User B signs in on the same device. 3. User B opens R3.                                                                                                                                                                       | Sign-out only clears the in-memory stock/customers/vendors caches, NOT the /reports/*.csv files. The cache-hit branch serves User A's inventory/value report to User B with no refetch. This is a cross-user data-leak / privacy defect — cached reports must be cleared on sign-out.                                                                                                                                                      | local-disk cache                                                                                                                                                |
| RPT-33   | Silent wrong-DB fallback: client env unset serves TESTING (yynuu) numbers                                     | both     | reports           | data-integrity | Backend deployed with SUPABASE_URL_Client / SUPABASE_SERVICE_KEY_CLIENT UNSET (so reorderSupabase falls back to the shared testing project).                          | 1. Tap a report. 2. Compare the returned numbers to the known client/prod (ztugw) values vs testing (yynuu) values.                                                                                                                                                              | With the client env unset, reorderSupabase silently falls back to the shared/testing Supabase with NO error. Reports return TESTING-project numbers to a production user (cross-tenant data exposure) with zero signal. Flag as a P0 config/data-integrity risk; mitigation is requiring the client env or failing loudly.                                                                                                                 | main-backend GET /api/sync/reorder-levels/:reportKey, supabase: companies (reorderSupabase fallback)                                                            |
| MAST-1   | Sign-in loads all three master caches (stock + customers + vendors)                                           | both     | masters_caches    | happy          | Logged out. Test Supabase project (yynuu) has stock_items (>0 rows), ledgers with Sundry Debtors and Sundry Creditors rows. Network up.                               | 1. Launch the app and complete phone-number OTP login (+91). 2. Open any PENDING voucher in the queue and tap Edit to enter edit mode. 3. Tap the party name to open the party picker. 4. Close it, tap Add Item to open the stock picker, and toggle the 'All items' switch ON. | Party picker lists customers (sale) / vendors (purchase); stock picker 'All items' view lists catalog stock with rates. None of the lists is empty and no 'download failed' snackbar appears.                                                                                                                                                                                                                                              | Firebase Auth (authStateChanges), Supabase: stock_items, Supabase: ledgers (Sundry Debtors / Sundry Creditors)                                                  |
| MAST-5   | User-switch isolation: sign out as A, sign in as B shows only B-scoped masters                                | both     | masters_caches    | security       | Two accounts A and B. (Note: in single-project-per-client model both may resolve to the same Supabase project; verify no in-memory leakage regardless.)               | 1. Sign in as user A, load caches, note a distinctive customer/stock row. 2. Sign out (caches cleared). 3. Sign in as user B and open the party + stock pickers.                                                                                                                 | Caches are cleared on A's sign-out and repopulated from B's session; no A-only in-memory rows appear before B's fetch resolves. RLS/anon scoping returns only data the active session is entitled to.                                                                                                                                                                                                                                      | Firebase Auth, Supabase: stock_items (RLS, anon key), Supabase: ledgers (RLS, anon key)                                                                         |
| MAST-8   | Empty stock_items / RLS misconfig returns 200 [] -> silently empty stock cache                                | both     | masters_caches    | data-integrity | Point the app at a project where the anon/publishable key or RLS yields zero stock_items rows (200 with empty array, not an error).                                   | 1. Sign in and wait for cache load. 2. Open a pending voucher, Edit, Add Item, toggle 'All items' ON. 3. Observe the stock list and whether any error/snackbar appeared.                                                                                                         | Stock picker is empty with NO 'download failed' snackbar and NO Sentry error (200 [] is not an exception). Confirms the silent-empty-cache risk where unitFor/partCodeFor will return '' -> 'NOS' default.                                                                                                                                                                                                                                 | Supabase: stock_items (RLS, anon key)                                                                                                                           |
| MAST-11  | Customer ledger > 1000 Sundry Debtors is silently truncated (no pagination)                                   | both     | masters_caches    | data-integrity | ledgers has > 1000 rows with group_name='Sundry Debtors' in the active project.                                                                                       | 1. Sign in and let CustomersCache load. 2. Open a pending SALE voucher, Edit, tap the party name to open the customer picker. 3. Search for a customer known to exist but sorted/positioned beyond the 1000th Sundry Debtor row.                                                 | DEFECT TO CONFIRM: the customer is missing because CustomersCache.fetch has no .range() and PostgREST caps the response at 1000 rows, so the party is unselectable (mis-parsed party cannot be fixed -> push may be blocked or pushed under wrong ledger).                                                                                                                                                                                 | Supabase: ledgers (Sundry Debtors, NOT paginated)                                                                                                               |
| MAST-12  | Vendor ledger > 1000 Sundry Creditors is silently truncated (no pagination)                                   | both     | masters_caches    | data-integrity | ledgers has > 1000 rows with group_name='Sundry Creditors'.                                                                                                           | 1. Sign in and let VendorsCache load. 2. Open a pending PURCHASE voucher, Edit, tap the party name to open the vendor picker. 3. Search for a vendor positioned beyond the 1000th Sundry Creditor row.                                                                           | DEFECT TO CONFIRM: the vendor is absent (1000-row cap, no pagination) and cannot be selected for a mis-parsed purchase party.                                                                                                                                                                                                                                                                                                              | Supabase: ledgers (Sundry Creditors, NOT paginated)                                                                                                             |
| MAST-20  | Empty customer cache blocks fixing a mis-parsed party (push stays blocked)                                    | both     | masters_caches    | data-integrity | CustomersCache empty (RLS 200 [] or fetch failed). A pending sale voucher whose parsed party is wrong.                                                                | 1. Open the sale voucher, Edit, tap the party name. 2. Observe the empty picker; attempt to change party to the correct one. 3. Attempt to push the voucher.                                                                                                                     | Picker is empty so the correct customer cannot be selected; combined with the party-changed/items-changed push guard, the voucher either cannot be corrected or risks being pushed under the wrong ledger. Confirms the data-integrity risk of silent empty caches.                                                                                                                                                                        | Supabase: ledgers, Push guard (_partyNameWasChanged && !_allItemsDifferentFromOriginal)                                                                         |
| MAST-25  | Add item from party-history carries that party's latest rate and discount onto the line                       | both     | masters_caches    | data-integrity | A pending sale voucher whose party has at least two historical vouchers for the same stock item at different rates/discounts.                                         | 1. Open the sale voucher, Edit, Add Item (party-history view, All items OFF). 2. Pick a stock item that the party bought/sold before. 3. Inspect the new line's rate, discount_pct, and computed amount.                                                                         | The line is seeded with the most-recent historical rate and discount_pct for that party (amount = qty*rate - qty*rate*discount/100). Tester must confirm this is the intended price, since pushing surfaces an outdated price/discount directly into the invoice/ledger totals.                                                                                                                                                            | Supabase: voucher_items + vouchers (inner join, latest-rate dedup)                                                                                              |
| MAST-29  | Unit backfill: party-history item with no unit resolves to catalog base unit                                  | both     | masters_caches    | data-integrity | A party-history item whose voucher_items.unit is empty but whose catalog stock_items.unit is set (e.g. 'Kgs').                                                        | 1. Open a pending voucher, Edit, Add Item (party-history). 2. Pick the item whose history unit is blank. 3. Inspect the line's unit field.                                                                                                                                       | unitFor() backfills the catalog unit (e.g. 'Kgs') via case-insensitive name match, so the line carries the correct Tally base unit rather than defaulting to 'NOS'.                                                                                                                                                                                                                                                                        | Supabase: voucher_items, Supabase: stock_items (unitFor)                                                                                                        |
| MAST-30  | Empty/stale cache forces unit to 'NOS' -> Tally rejects line on push                                          | both     | masters_caches    | data-integrity | StockItemsCache empty (RLS 200 [] or stale) OR the picked item not present in cache. Target Tally stock master uses a non-NOS base unit (e.g. Kgs/Pcs).               | 1. With an empty/stale stock cache, open a pending voucher, Edit, Add Item. 2. Add an item whose unit is unknown to the cache; confirm the line shows unit 'NOS'. 3. Push the voucher and observe the Tally push result over realtime (tally_response errors).                   | unitFor() returns '' so the line defaults to 'NOS'; on push Tally rejects the line because the stock master's base unit differs (e.g. Kgs). This money/line-integrity failure is invisible at add-time (no snackbar) and only surfaces as a push error.                                                                                                                                                                                    | Supabase: stock_items (unitFor), push_queue / activate -> Tally push, Supabase realtime: vd_status_<rowId>                                                      |
| MAST-31  | Interior-whitespace/punctuation name mismatch makes unitFor return '' -> 'NOS'                                | both     | masters_caches    | data-integrity | A stock item whose voucher_items name differs from the catalog name by interior whitespace or punctuation (e.g. double space vs single space).                        | 1. Open a pending voucher, Edit, Add Item from party-history for that item. 2. Inspect the resolved unit on the line.                                                                                                                                                            | DEFECT TO CONFIRM: unitFor/partCodeFor only match on trim().toLowerCase() equality (not interior-whitespace-normalized), so the lookup misses and unit falls back to 'NOS', producing a wrong-unit line that Tally may reject.                                                                                                                                                                                                             | Supabase: stock_items (unitFor/partCodeFor)                                                                                                                     |
| MAST-43  | Selecting a corrected party updates editable party name and feeds the push guard                              | both     | masters_caches    | happy          | A pending sale voucher with a mis-parsed but cache-present customer; the correct customer IS in the cache.                                                            | 1. Open the voucher, Edit, tap party name, select the correct customer. 2. Confirm the displayed party name updates. 3. Add/modify line items as required and push.                                                                                                              | _editablePartyName updates to the selected customer; on push the narration becomes 'Replara AI' and the voucher is activated under the corrected ledger. If party changed but not all items changed, the push is correctly BLOCKED by the guard (verify the block message).                                                                                                                                                                | Supabase: ledgers, push_queue / POST /api/sync/push-queue/activate                                                                                              |
| XCUT-4   | prod.json placeholder: real prod Firebase but TESTING Supabase/backend (wrong-env hazard)                     | both     | crosscutting      | data-integrity | env/prod.json as committed (_PROD_PENDING_CONFIRM). Do NOT ship to real users; verify in a controlled build.                                                          | 1. Inspect env/prod.json values. 2. Build with FLAVOR=prod and run in a sandbox. 3. Sign in with a real-prod phone account; create/read a push_queue row; load a report. 4. Confirm which Supabase project actually receives the writes.                                         | DEFECT TO FLAG: FLAVOR=prod authenticates real users against aiaccountant-b60ed Firebase, but SUPABASE_URL points to yynuu (TESTING) and BACKEND_BASE_URL/PARSE point to the stale 366926737745 project. Real users would read/write TESTING data. Build must be blocked from release until prod targets are confirmed.                                                                                                                    | firebase(prod aiaccountant-b60ed), supabase(yynuu TESTING), main-backend(366 stale), parsing-service                                                            |
| XCUT-5   | Android Dart FLAVOR must match gradle --flavor or native logins break                                         | mobile   | crosscutting      | platform       | Android device; ability to build with mismatched gradle --flavor vs Dart FLAVOR define.                                                                               | 1. Build Android with gradle --flavor testing but Dart-define FLAVOR=deployment (intentional mismatch). 2. Install and attempt phone-OTP login. 3. Repeat with matching flavor + define as control.                                                                              | With mismatch, native google-services.json (testing) and Dart FirebaseOptions (deployment) target different Firebase projects and Android login fails / OTP never verifies. With matched flavor+define, login succeeds. Confirms the firebase_options.dart warning.                                                                                                                                                                        | firebase, android-google-services                                                                                                                               |
| XCUT-11  | Web refresh (F5) restores Firebase session via authStateChanges stream                                        | web      | crosscutting      | platform       | Signed in on web.                                                                                                                                                     | 1. Sign in on the web app. 2. Press browser refresh / reload the tab. 3. Observe whether you stay signed in.                                                                                                                                                                     | SplashScreen shows briefly (ConnectionState.waiting) then AccountantShell — session is restored because home gates on the authStateChanges() stream, not a one-time currentUser read. User is NOT bounced to login.                                                                                                                                                                                                                        | firebase, web-browser                                                                                                                                           |
| XCUT-13  | Phone OTP login happy path with +91 prefix                                                                    | both     | crosscutting      | happy          | Valid Indian phone number with SMS access.                                                                                                                            | 1. Enter a 10-digit number on LoginScreen. 2. Tap continue; verifyPhoneNumber is called with '+91'+number. 3. Enter the received OTP on OtpScreen.                                                                                                                               | OTP verifies, FirebaseAuth signs in, app navigates to AccountantShell, and a JWT is obtainable via getIdToken(). Number is sent with hardcoded +91 country code.                                                                                                                                                                                                                                                                           | firebase                                                                                                                                                        |
| XCUT-25  | Non-401 non-2xx (409/500) get/post is reported but error body decoded as success                              | both     | crosscutting      | data-integrity | Ability to force the backend to return a 409 or 500 with a JSON body from an ApiClient.get/post call.                                                                 | 1. Trigger an ApiClient.get or post that returns HTTP 409/500 with a JSON error body. 2. Observe what the caller receives.                                                                                                                                                       | DEFECT TO FLAG: reportFailure logs to Sentry but the method then returns jsonDecode(res.body) (no throw for non-401). A 409/500 error JSON is handed to the caller as if it were valid data, masking the failure. getRaw/getBytes do throw on non-200, but get/post do not.                                                                                                                                                                | main-backend, sentry                                                                                                                                            |
| XCUT-33  | Activate happy path: payload narration overwritten then POST flips pending->push_now                          | both     | crosscutting      | happy          | A push_queue row with status='pending' and a valid voucher_payload; signed in.                                                                                        | 1. Open the voucher detail sheet for the pending row. 2. Tap Push. 3. Observe the Supabase update then the activate POST and resulting status.                                                                                                                                   | voucher_detail_sheet first updates push_queue.voucher_payload (narration forced to the fixed _pushNarration, '__' keys stripped) for that id, then POSTs activate; backend flips status pending->push_now and returns 2xx. UI enters awaitingPushResult.                                                                                                                                                                                   | supabase, push_queue, POST /api/sync/push-queue/activate                                                                                                        |
| XCUT-40  | Activate POST failure leaves payload narration mutated with status still pending (no rollback)                | both     | crosscutting      | data-integrity | A pending row; force the activate POST to fail (network/401/409) AFTER the Supabase update succeeds.                                                                  | 1. Open the detail sheet, tap Push. 2. Let the Supabase voucher_payload update succeed, then force the activate POST to fail. 3. Re-open the row and inspect voucher_payload/narration and status.                                                                               | DEFECT TO FLAG: the row keeps the overwritten narration (_pushNarration) and status stays 'pending' with no rollback. The voucher payload is edited without a successful push, so a later push uses the forced narration even if the user never intended it.                                                                                                                                                                               | supabase, push_queue, main-backend                                                                                                                              |
| XCUT-47  | Hardcoded company_name='K V ENTERPRISES' yields 409 when duplicate company rows exist                         | both     | crosscutting      | data-integrity | Client Supabase has TWO 'K V ENTERPRISES' rows (known ztugw duplicate-company issue).                                                                                 | 1. Open any report category. 2. Observe the load result.                                                                                                                                                                                                                         | DEFECT TO FLAG: resolveCompanyLookup returns 409 (company resolution conflict) because of the duplicate rows; the report silently fails to load. The app hardcodes the company name with no override, so the user cannot recover in-app.                                                                                                                                                                                                   | main-backend, supabase(client), GET /api/sync/reorder-levels/:reportKey                                                                                         |
| XCUT-67  | Cross-project JWT mismatch: prod app on b60ed against 366 backend -> Bearer routes 401, key routes still work | both     | crosscutting      | security       | FLAVOR=prod build (Firebase=aiaccountant-b60ed) hitting the 366 backend whose FIREBASE_SERVICE_ACCOUNT_B64 targets a different Firebase project.                      | 1. Sign in (real prod Firebase) on a prod build. 2. Call a Bearer-authed endpoint (image fetch via getBytes). 3. Then call an x-api-key-only endpoint (activate, reorder).                                                                                                       | DEFECT TO FLAG: the 366 backend cannot verify a b60ed-issued token, so Bearer-authed routes (image fetch) return 401, while x-api-key-only routes (activate, reorder) still succeed. Partial silent breakage where some features work and some 401 with no clear cause.                                                                                                                                                                    | main-backend, firebase, GET /api/sync/push-queue/:id/image/:page                                                                                                |
| XCUT-70  | Wrong SUPABASE_URL/flavor sends all direct reads/writes/realtime to the wrong project                         | both     | crosscutting      | data-integrity | A build whose SUPABASE_URL/anon key were misconfigured (e.g. prod.json placeholder, or a typo).                                                                       | 1. Build with a mismatched SUPABASE_URL (e.g. prod flavor pointing at yynuu). 2. Sign in and create a push_queue row / load history. 3. Verify which project actually stores the data.                                                                                           | DEFECT TO FLAG: every direct Supabase read/write and all realtime channels hit whatever SUPABASE_URL is compiled in. A wrong value (the prod placeholder pointing at yynuu) means a 'prod' user silently operates on TESTING data. Must be validated before any prod release.                                                                                                                                                              | supabase, push_queue, scan_jobs, realtime                                                                                                                       |

### P1 — Core flow broken or blocked (144)

| ID       | Title                                                                                                  | Platform | Domain            | Category       | Preconditions                                                                                                                                                                                  | Steps                                                                                                                                                                                                                                                                                             | Expected                                                                                                                                                                                                                                                                                                                                                       | Integration points                                                                                                     |
| -------- | ------------------------------------------------------------------------------------------------------ | -------- | ----------------- | -------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------- |
| AUTH-5   | Web: paste a 6-digit code via Ctrl/Cmd+V                                                               | web      | auth_session      | platform       | On OtpScreen in a desktop browser; clipboard contains a string with at least 6 digits (e.g. '123456' or 'Your code is 123456').                                                                | 1. Copy a code containing >=6 digits to the clipboard. 2. Focus the OTP screen and press Ctrl+V (or Cmd+V on macOS). 3. Observe the boxes and focus index.                                                                                                                                        | Non-digits are stripped; the first 6 digits populate all boxes, focus jumps to box 5. If the clipboard has fewer than 6 digits after stripping, the paste is ignored (no change).                                                                                                                                                                              | firebase                                                                                                               |
| AUTH-6   | Mobile: paste code via keypad 'paste' function key                                                     | mobile   | auth_session      | platform       | On OtpScreen on mobile (keypad visible); clipboard has >=6 digits.                                                                                                                             | 1. Copy a 6+ digit code to the clipboard. 2. Tap the 'paste' function key on the on-screen keypad.                                                                                                                                                                                                | Boxes fill with the first 6 digits, focus set to box 5. With <6 clipboard digits, nothing changes.                                                                                                                                                                                                                                                             | firebase                                                                                                               |
| AUTH-7   | Edge: phone shorter than 10 digits is rejected client-side                                             | both     | auth_session      | edge           | LoginScreen, signed out.                                                                                                                                                                       | 1. Type a 9-digit number. 2. Tap 'Send OTP →'.                                                                                                                                                                                                                                                    | Inline error 'Enter a valid 10-digit number' shown; verifyPhoneNumber is NOT called; no SMS sent; not navigated to OtpScreen.                                                                                                                                                                                                                                  | firebase                                                                                                               |
| AUTH-14  | BUG-WATCH: 'Resend code' link resets the timer but sends NO new SMS                                    | both     | auth_session      | error          | On OtpScreen, countdown reached 0, original SMS not received (or expired).                                                                                                                     | 1. Tap the 'Resend code' link. 2. Wait for a new SMS.                                                                                                                                                                                                                                             | Defect to flag: the link only restarts the 30s countdown (_startTimer); verifyPhoneNumber is NOT re-invoked so NO new SMS is sent. A user who never got the first code is stranded on this screen. Workaround: press Back to LoginScreen and Send OTP again (which uses forceResendingToken).                                                                  | firebase                                                                                                               |
| AUTH-16  | Error: wrong OTP code shows specific error                                                             | both     | auth_session      | error          | On OtpScreen with a valid verificationId.                                                                                                                                                      | 1. Enter 6 digits that are NOT the correct code. 2. Tap 'Verify →'.                                                                                                                                                                                                                               | FirebaseAuthException 'invalid-verification-code' caught; error banner reads 'Wrong code. Check and try again'; spinner clears (_loading=false); user can retry.                                                                                                                                                                                               | firebase                                                                                                               |
| AUTH-18  | BUG-WATCH: network drop during OTP verify hangs the spinner permanently                                | both     | auth_session      | error          | On OtpScreen with a valid code typed; ability to kill connectivity at the moment of Verify.                                                                                                    | 1. Type the 6 correct digits. 2. Disable Wi-Fi/data (or use a network throttle to force a non-FirebaseAuthException error), then tap 'Verify →'.                                                                                                                                                  | Defect to flag: _doVerify only catches FirebaseAuthException, so a network/PlatformException is uncaught — _loading stays true, the button is stuck on a spinner, and NO error is shown. User cannot retry without restarting the app. (Confirm whether restoring network recovers.)                                                                           | firebase                                                                                                               |
| AUTH-19  | Error: verificationFailed on Send OTP surfaces Firebase message                                        | both     | auth_session      | error          | Conditions to trigger verificationFailed: e.g. SMS quota exceeded (too-many-requests), billing not enabled, or app-not-authorized.                                                             | 1. Repeatedly request OTPs (or use a test project with billing disabled) to trigger the failure. 2. Tap 'Send OTP →'.                                                                                                                                                                             | verificationFailed callback fires; _loading clears; the inline error shows e.message (e.g. too-many-requests / billing-not-enabled text) or fallback 'Verification failed'. App does not navigate to OtpScreen and does not crash.                                                                                                                             | firebase                                                                                                               |
| AUTH-26  | Caches load on sign-in (auth side effect)                                                              | both     | auth_session      | happy          | Signed out; Supabase reachable (yynuu/ztugw per env); stock/customers/vendors tables populated.                                                                                                | 1. Complete sign-in. 2. Navigate to a screen using stock/customers/vendors (e.g. stock picker, party list) and/or watch logs for the three cache fetches.                                                                                                                                         | _wireSessionAndSentryUser listener fires on the non-null uid and calls StockItemsCache/CustomersCache/VendorsCache .fetch(); the three caches populate (stock paginates 1000-row pages). UI that depends on them is ready.                                                                                                                                     | firebase, supabase, stock_items, ledgers                                                                               |
| AUTH-30  | JWT attached as Bearer on backend calls (image stream / sync)                                          | both     | auth_session      | security       | Signed in; a backend call path that uses ApiClient (e.g. GET push-queue image).                                                                                                                | 1. Trigger an ApiClient.get/getBytes call (e.g. open a stored invoice image). 2. Capture the outgoing request headers (proxy/devtools network).                                                                                                                                                   | Headers include Authorization: Bearer <Firebase ID token> (from currentUser.getIdToken(), no forceRefresh), plus x-api-key=Config.mrpApiKey and a unique x-request-id. Backend requireApiKey verifies the Bearer JWT first.                                                                                                                                    | firebase, main-backend, requireApiKey                                                                                  |
| AUTH-31  | Backend 401 throws Unauthorized but does NOT sign the user out                                         | both     | auth_session      | error          | Signed in; ability to force a 401 (e.g. revoke/disable the Firebase user server-side so a cached token is rejected, or point at a backend that rejects the key).                               | 1. Revoke the user or otherwise make the next ApiClient call return 401. 2. Trigger a backend call (image fetch / report / activate). 3. Observe app behavior.                                                                                                                                    | ApiClient calls reportFailure to Sentry and throws Exception('Unauthorized'); the app does NOT auto-sign-out or route to Login — calls keep failing until a manual app restart. (Documents the no-re-auth risk; the failing call should surface a user-visible error rather than silently corrupt state.)                                                      | firebase, main-backend, requireApiKey                                                                                  |
| AUTH-34  | SECURITY: reorder-levels ignores the Firebase JWT (requireClientApiKey only)                           | both     | auth_session      | security       | GET /api/sync/reorder-levels/:reportKey is gated by requireClientApiKey (x-api-key vs API_KEY_CLIENT) and ignores the Bearer JWT.                                                              | 1. Trigger the reorder/inventory report and capture headers. 2. Replay the request with only the client x-api-key and no JWT. 3. Replay again with a wrong/missing key.                                                                                                                           | With the correct client key the report returns regardless of Firebase identity; with a missing/mismatched key the middleware returns 401 'Unauthorized' (length-mismatch short-circuits before timingSafeEqual). Document that report data is gated by a shared embedded secret, not per-user identity.                                                        | main-backend, /api/sync/reorder-levels/:reportKey, requireClientApiKey                                                 |
| AUTH-36  | Concurrency: sign in on web while already signed in on mobile (independent sessions)                   | both     | auth_session      | concurrency    | Same phone number; signed in on the mobile app.                                                                                                                                                | 1. Open the web app and complete OTP sign-in with the same number. 2. Use both clients (e.g. open queue/profile) and confirm both remain authenticated.                                                                                                                                           | Firebase phone auth allows the same user on multiple devices; both sessions are valid and independent. Each client loads its own caches. No session is invalidated by the other.                                                                                                                                                                               | firebase, supabase                                                                                                     |
| AUTH-42  | Platform: web/iOS never auto-verify — always manual OTP entry                                          | web      | auth_session      | platform       | Web (or iOS) client; valid number.                                                                                                                                                             | 1. Send OTP and wait without typing. 2. Confirm no auto sign-in occurs, then type the code manually.                                                                                                                                                                                              | verificationCompleted does not fire on web/iOS (no SMS auto-retrieval); the user must manually enter the code on OtpScreen. Manual entry then signs in normally.                                                                                                                                                                                               | firebase                                                                                                               |
| SCAN-1   | Open capture flow from bottom-nav center camera item shows Sale/Purchase dialog                        | both     | scan_parse        | happy          | Logged in; app on Queue screen (Android phone or web session).                                                                                                                                 | 1. Tap the center camera item in the bottom-nav (index 2), or on web/desktop the equivalent side-rail capture entry 2. Observe the modal that appears                                                                                                                                             | CaptureTypeDialog opens as a modal offering exactly two choices: Sale and Purchase. No scan_jobs row is created yet and the Processing badge does not change.                                                                                                                                                                                                  | app_shell._openTaggedCameraFlow, CaptureTypeDialog                                                                     |
| SCAN-7   | Web blocks scanning with the Android-only snackbar                                                     | web      | scan_parse        | platform       | Logged in via web browser.                                                                                                                                                                     | 1. Open the capture flow and choose Sale (or Purchase) 2. Observe                                                                                                                                                                                                                                 | A snackbar 'Document scanning is only available on Android.' is shown; no scanner opens, no scan_jobs row is created, no parse request fires. The _activeCaptureType tag is set but has no functional effect.                                                                                                                                                  | app_shell platform gate                                                                                                |
| SCAN-11  | Cancel the scanner produces no scan_jobs row and no stuck badge                                        | mobile   | scan_parse        | edge           | Android phone; scanner open.                                                                                                                                                                   | 1. Press cancel/back in the document scanner so it returns with no PDF (result.pdf?.uri is null/empty) 2. Return to the app and check the Queue Processing badge                                                                                                                                  | Flow returns early: no CapturedShot, no scan_jobs row, no parse request, Processing badge unchanged. No stuck/phantom badge.                                                                                                                                                                                                                                   | DocumentScanner, scan_jobs table                                                                                       |
| SCAN-12  | Scanner permission denied / cannot open shows error snackbar and closes cleanly                        | mobile   | scan_parse        | error          | Android phone with camera permission DENIED (revoke in system settings).                                                                                                                       | 1. Open capture flow and choose a type so the scanner attempts to launch 2. Observe                                                                                                                                                                                                               | scanDocument throws and is caught; snackbar 'Could not open the document scanner. Check camera permission and try again.' is shown; scanner.close() runs in finally; no scan_jobs row, no parse.                                                                                                                                                               | DocumentScanner                                                                                                        |
| SCAN-16  | startScan inserts scan_jobs row and badge climbs only once (optimistic + echo dedup)                   | mobile   | scan_parse        | happy          | Android phone, realtime connected.                                                                                                                                                             | 1. Perform one scan 2. Immediately watch the Sale/Purchase Processing count badge over the next few seconds                                                                                                                                                                                       | Badge increments by exactly +1 (the optimistic local add is deduped against the realtime INSERT echo by row id); it does NOT briefly show +2.                                                                                                                                                                                                                  | scan_jobs table, scan_jobs_live                                                                                        |
| SCAN-17  | startScan failure: parse still fires but without job_id (badge gap)                                    | mobile   | scan_parse        | error          | Android phone; force the scan_jobs INSERT to fail (e.g. block the Supabase REST host or use an invalid anon key) while leaving the parse host reachable.                                       | 1. Perform a scan 2. Observe the badge and confirm via logs/network that the parse POST still goes out                                                                                                                                                                                            | startScan returns null, reportHandledError('supabase.scan_jobs.startScan') is logged; the parse POST is sent to the BASE url with NO &job_id; that scan shows no optimistic badge on this device.                                                                                                                                                              | scan_jobs table, parsing-service, Sentry reportHandledError                                                            |
| SCAN-19  | Parse POST carries only Content-Type application/pdf and no auth header                                | mobile   | scan_parse        | security       | Android phone; ability to inspect the outgoing request (proxy/log).                                                                                                                            | 1. Perform a scan 2. Capture the outgoing parse POST headers                                                                                                                                                                                                                                      | Request contains header Content-Type: application/pdf and the raw PDF bytes; NO x-api-key and NO Authorization/Bearer header is present (app→parsing-service boundary is unauthenticated).                                                                                                                                                                     | parsing-service, scan_uploader.sendScanToParser                                                                        |
| SCAN-30  | 5s TTL sweep clears a stuck badge after 300s                                                           | both     | scan_parse        | edge           | A scan_jobs row exists whose DELETE will never arrive (e.g. parse produced no voucher).                                                                                                        | 1. Create the stuck condition (scan an unmatchable invoice) 2. Note the badge climb time 3. Wait just over 300s without backgrounding the app                                                                                                                                                     | At ~300s the periodic 5s _sweep drops the stale row and the Processing badge decrements to its correct value automatically, even though no DELETE was received.                                                                                                                                                                                                | scan_jobs table, ScanJobsService._sweep                                                                                |
| SCAN-31  | Foreground resync catches a DELETE missed while backgrounded                                           | both     | scan_parse        | concurrency    | A scan is in flight; realtime socket will be suspended by backgrounding.                                                                                                                       | 1. Start a scan so the badge climbs 2. Background the app/tab (lock phone or switch browser tabs) and keep it backgrounded while the parse completes and the scan_jobs row is deleted 3. Bring the app/tab back to the foreground                                                                 | On AppLifecycleState.resumed the app calls _scanService.resync() and pushQueueService.refresh(); the badge corrects to the true count and the new push_queue row appears, recovering the missed realtime DELETE.                                                                                                                                               | scan_jobs table, ScanJobsService.resync, push_queue                                                                    |
| SCAN-46  | Web observes badge changes but never inserts a scan_jobs row                                           | web      | scan_parse        | platform       | Web session logged in with no phone scanning.                                                                                                                                                  | 1. On web, open the capture flow and choose a type (hits the Android-only gate) 2. Confirm via DB/realtime that no scan_jobs row was created by the web client                                                                                                                                    | Web never calls startScan; it only subscribes to scan_jobs_live and reflects rows created by mobile clients. No web-originated scan_jobs row.                                                                                                                                                                                                                  | scan_jobs table, scan_jobs_live                                                                                        |
| SCAN-53  | Company coupling: parsed voucher is filed under the server-default company                             | mobile   | scan_parse        | data-integrity | Android phone where the operator's intended company differs from the parser's default (DEFAULT_PURCHASE_COMPANY / options company_name).                                                       | 1. Scan an invoice intended for company B while the parser default resolves to company A 2. Inspect the resulting push_queue row's company                                                                                                                                                        | Document the risk: the company is derived server-side, not from the scanning client; a mismatch files the voucher under the wrong company and is not validated in the scan→parse client path.                                                                                                                                                                  | parsing-service, push_queue, main-backend ingest                                                                       |
| QUEUE-2  | Sign-in subscribes to realtime and seeds the queue once                                                | both     | queue_realtime    | happy          | Signed out. push_queue has active rows.                                                                                                                                                        | 1. Sign in with phone OTP. 2. Open the Queue screen immediately after the shell loads.                                                                                                                                                                                                            | subscribe() runs refresh() first then attaches push_queue_live INSERT/UPDATE/DELETE handlers; the queue populates from the initial select with no duplicate rows and no flicker of empty-then-full beyond the single load.                                                                                                                                     | supabase:push_queue, realtime:push_queue_live                                                                          |
| QUEUE-8  | Realtime UPDATE pending->push_now keeps the row visible in the queue                                   | both     | queue_realtime    | happy          | A pending row visible in the queue.                                                                                                                                                            | 1. Push the row to Tally (or have the backend flip it) so status goes pending->push_now. 2. Watch the queue without closing the sheet.                                                                                                                                                            | The row stays visible (only 'pushed' removes it). Status reflects push_now. The row does not vanish-and-reappear.                                                                                                                                                                                                                                              | supabase:push_queue, realtime:push_queue_live, main-backend                                                            |
| QUEUE-10 | DATA INTEGRITY: TOASTed voucher_payload omitted on push_now UPDATE corrupts row to Unknown/₹0/Purchase | both     | queue_realtime    | data-integrity | A pending SALE row visible on the Sale tab; backend will flip it to push_now updating only status+error_message.                                                                               | 1. Activate the row so the backend UPDATE sets status=push_now and clears error_message (leaving the large voucher_payload column unchanged, so Realtime omits it). 2. Watch the queue row at the moment the realtime UPDATE echo arrives (before any refresh/onPushed reconcile).                | DEFECT WATCH: unlike History (which re-fetches on empty payload), the queue UPDATE handler does NOT re-fetch. rowToEntry parses an empty payload so the row momentarily renders party 'Unknown', amount ₹0, and type defaulting to Purchase — a SALE row can jump to the Purchase tab — until onPushed/refresh reconciles it. Record the transient corruption. | supabase:push_queue, realtime:push_queue_live                                                                          |
| QUEUE-13 | Realtime DELETE removes the row on all clients                                                         | both     | queue_realtime    | concurrency    | Same tenant signed in on two clients, both showing the same queue row.                                                                                                                         | 1. On client A, open the row and Delete it (or delete the push_queue row directly). 2. Observe client B without refreshing.                                                                                                                                                                       | push_queue_live DELETE fires on client B; the row with id 'supabase_<deletedId>' is filtered out and disappears from B's queue live.                                                                                                                                                                                                                           | supabase:push_queue, realtime:push_queue_live                                                                          |
| QUEUE-14 | Pull-to-refresh re-fetches authoritative rows and corrects any realtime drift                          | both     | queue_realtime    | happy          | Queue showing rows where one is momentarily mis-rendered (e.g. from a payload-omitted echo) or out of order.                                                                                   | 1. Pull down on the Queue list to trigger RefreshIndicator (widget.onRefresh = PushQueueService.refresh). 2. Observe the rows after the spinner completes.                                                                                                                                        | refresh() re-selects active rows and replaces _entries wholesale; any drift (Unknown/₹0, missing edit_state, stale order) is corrected from PostgREST. checked/status are preserved per id by the shell merge.                                                                                                                                                 | supabase:push_queue                                                                                                    |
| QUEUE-15 | Foreground resume re-syncs both queue and scan badge after backgrounding                               | both     | queue_realtime    | concurrency    | App/tab backgrounded long enough for the realtime socket to suspend; while backgrounded another client inserts/deletes a push_queue row and a scan_jobs row.                                   | 1. Background the app (mobile home button) or switch away from the web tab for ~1 minute while events occur on another client. 2. Return to the foreground / re-focus the tab. 3. Observe the queue and the Processing badge.                                                                     | didChangeAppLifecycleState(resumed) calls _scanService.resync() and _pushQueueService.refresh(); rows and the scan badge catch up to the events missed while the socket was suspended (which are NOT replayed by realtime).                                                                                                                                    | supabase:push_queue, supabase:scan_jobs, realtime:push_queue_live, realtime:scan_jobs_live                             |
| QUEUE-16 | Close mid-edit tags row 'under edit', floats it to top, persists edit_state                            | both     | queue_realtime    | happy          | A pending row in the queue.                                                                                                                                                                    | 1. Open the row, tap Edit, change something, then close the sheet WITHOUT saving (tap X / swipe down). 2. Observe the row in the queue.                                                                                                                                                           | Local _editMarkers tags it 'under edit' immediately; _persistEditState writes edit_state='under_edit' and bumps created_at=now(); after the write widget.onRefresh() reconciles so the 'under edit' tag and the float-to-top land durably. The row moves to the top of the Today group.                                                                        | supabase:push_queue                                                                                                    |
| QUEUE-17 | Save edits tags row 'invoice edited', floats to top, persists                                          | both     | queue_realtime    | happy          | A pending row in the queue.                                                                                                                                                                    | 1. Open the row, tap Edit, change an item, tap Save, then close the sheet. 2. Observe the row in the queue.                                                                                                                                                                                       | Row is tagged 'invoice edited' (everSaved), edit_state='edited' with created_at bumped; onRefresh reconciles so the tag persists and the row floats to the top of Today.                                                                                                                                                                                       | supabase:push_queue                                                                                                    |
| QUEUE-21 | Edit save on a pending row persists voucher_payload and confirms                                       | both     | queue_realtime    | happy          | A pending row.                                                                                                                                                                                 | 1. Open the row, tap Edit, change party/items/qty/rate, tap Save. 2. Observe the snackbar and reopen the row.                                                                                                                                                                                     | update push_queue set voucher_payload=cleanPayload .eq(id).eq(status,'pending') returns 1 row; snackbar 'Changes saved.'; __-prefixed keys are stripped from the persisted payload; reopening shows the edited values.                                                                                                                                         | supabase:push_queue                                                                                                    |
| QUEUE-22 | Edit save on a row already push_now/pushed returns 0 rows -> 'already queued' message                  | both     | queue_realtime    | error          | A row that flipped to push_now (or pushed) after the sheet was opened, OR concurrently activated from another client.                                                                          | 1. Open a pending row, enter Edit. From another client (or backend) flip the same row to push_now. 2. Tap Save on the first client.                                                                                                                                                               | The .eq(status,'pending') guard matches 0 rows; snackbar 'Couldn't save — this voucher may already be queued for push.' No payload change is persisted.                                                                                                                                                                                                        | supabase:push_queue                                                                                                    |
| QUEUE-24 | Push is blocked when party changed but not all items changed                                           | both     | queue_realtime    | data-integrity | A pending voucher with multiple items.                                                                                                                                                         | 1. Open the row, Edit, change the party (vendor/customer) but leave at least one item name unchanged, Save, then tap Push To Tally.                                                                                                                                                               | _partyNameWasChanged && !_allItemsDifferentFromOriginal is true: the Push button is dimmed (green at 0.45 alpha) and tapping it shows snackbar 'Party Name is Changed!!\nChange all the items'. No activate POST is sent. Same guard fires on Enter-on-Total (_submitFromTotal).                                                                               | supabase:push_queue                                                                                                    |
| QUEUE-26 | DATA INTEGRITY: every push overwrites narration to fixed 'Replara AI'                                  | both     | queue_realtime    | data-integrity | A pending voucher whose parsed/edited narration is a meaningful description (e.g. real invoice notes).                                                                                         | 1. Open the row, optionally edit narration to a custom string, Save. 2. Tap Push To Tally and confirm. 3. Inspect the persisted voucher_payload.narration and the resulting TallyPrime voucher narration.                                                                                         | DEFECT WATCH: _activate unconditionally sets clean['narration']='Replara AI' before activating, destroying the parsed/edited narration. The pushed Tally voucher loses its real description/audit text. Confirm the custom narration is gone.                                                                                                                  | supabase:push_queue, POST /api/sync/push-queue/activate                                                                |
| QUEUE-27 | Sale push failure shows the fail dialog with the Tally error                                           | both     | queue_realtime    | error          | Pending SALE row; desktop push will fail (e.g. ledger mismatch in Tally).                                                                                                                      | 1. Push the SALE row to Tally and confirm. 2. Wait for the desktop to report failure (status=failed or created==0 && errors>=1).                                                                                                                                                                  | _handlePushResult treats it as failed; the sheet closes and _SalePushFailedDialog shows error_message (or 'Push to Tally failed.' when empty). The row STAYS in the queue with status 'failed'.                                                                                                                                                                | supabase:push_queue, realtime:vd_status_<rowId>                                                                        |
| QUEUE-29 | DATA INTEGRITY: duplicate purchase invoice is only flagged visually and can still be pushed twice      | both     | queue_realtime    | data-integrity | Two purchase rows with the same voucher_payload.reference (same supplier invoice number) in the queue.                                                                                         | 1. On the Purchase tab, confirm the newer row shows the 'Duplicate: Already Exists In Queue' tag and is dimmed. 2. Open the dimmed duplicate row and tap Push To Tally, confirm. 3. Check TallyPrime for the result.                                                                              | DEFECT WATCH: isQueueDuplicate is purely visual; the push is NOT blocked. The same supplier invoice can be activated and double-posted into TallyPrime. Confirm a second voucher is created.                                                                                                                                                                   | supabase:push_queue, POST /api/sync/push-queue/activate                                                                |
| QUEUE-31 | Activate returns 409 (not pending) -> failure snackbar                                                 | both     | queue_realtime    | error          | A row that another client/desktop has already moved to push_now/pushed/failed.                                                                                                                 | 1. Open a row that looks pending locally; concurrently flip it to push_now elsewhere. 2. Tap Push To Tally and confirm.                                                                                                                                                                           | Backend returns 409 'Push queue job must be pending before activation (current status: X)'. App shows 'Failed (409): ...', spinner clears. No double-activation.                                                                                                                                                                                               | POST /api/sync/push-queue/activate, supabase:push_queue                                                                |
| QUEUE-34 | Network error / timeout during activate POST -> error snackbar, spinner cleared                        | both     | queue_realtime    | error          | A pending row.                                                                                                                                                                                 | 1. Disable network (or block the activate host). 2. Tap Push To Tally, confirm.                                                                                                                                                                                                                   | The pre-push voucher_payload update may also fail; the POST throws. catch shows snackbar 'Error: <e>', clears _isSubmitting, and ApiClient.reportFailure logs error+stack with the request id. No partial 'pushing…' hang.                                                                                                                                     | POST /api/sync/push-queue/activate, supabase:push_queue                                                                |
| QUEUE-40 | Async push result is delivered over the per-voucher vd_status channel while the sheet is open          | both     | queue_realtime    | happy          | A row sheet kept open after activate.                                                                                                                                                          | 1. Push the row, keep the sheet open, watch the Status chip and Push Details block update.                                                                                                                                                                                                        | vd_status_<rowId> UPDATE updates _status, _pushedAt, _errorMessage in place; the header Status chip changes colour (amber push_now -> green pushed / red failed) and the Push Details block appears on terminal status.                                                                                                                                        | realtime:vd_status_<rowId>, supabase:push_queue                                                                        |
| QUEUE-42 | Discard/Delete a queued row removes it locally then deletes server-side, syncing other clients         | both     | queue_realtime    | happy          | Two clients showing the same pending row.                                                                                                                                                      | 1. On client A, open the row, tap Delete, confirm in the dialog. 2. Observe client A and client B.                                                                                                                                                                                                | Client A drops the row from the in-memory list and pops the sheet immediately; then delete push_queue .eq(id) runs and shows 'Deleted.'. Client B receives the realtime DELETE and removes the row.                                                                                                                                                            | supabase:push_queue, realtime:push_queue_live                                                                          |
| QUEUE-45 | ANDROID ONLY: scanning a document inserts scan_jobs and raises the Processing badge                    | mobile   | queue_realtime    | happy          | Android device, signed in, Queue open on the relevant tab.                                                                                                                                     | 1. Tap the capture nav, pick Sale or Purchase, complete an ML Kit scan. 2. Observe the Processing tile/badge and timer ring.                                                                                                                                                                      | ScanJobsService.startScan inserts a scan_jobs {type} row, returns its id (used as parser job_id), optimistically adds locally (deduped vs echo). The QueueLoadingTile shows count=1 for the active type with a timer ring from oldestStartFor.                                                                                                                 | supabase:scan_jobs, realtime:scan_jobs_live, parsing-service                                                           |
| QUEUE-47 | Cross-client scan badge: phone scan raises and drains the badge on an open web session                 | both     | queue_realtime    | concurrency    | Same tenant on Android phone and web, both on Queue.                                                                                                                                           | 1. Scan on the phone. 2. Watch the web Processing badge climb (+1) then drain (-1) when the parsing service finishes and deletes the scan_jobs row.                                                                                                                                               | scan_jobs_live INSERT echo increments the badge on every client; when parsing deletes the row and inserts the push_queue row, the DELETE echo drains the badge everywhere and the new invoice appears in the queue.                                                                                                                                            | supabase:scan_jobs, realtime:scan_jobs_live, supabase:push_queue, parsing-service                                      |
| QUEUE-61 | WEB: session restored on page refresh re-subscribes the realtime channels                              | web      | queue_realtime    | platform       | Signed in on web with an active queue.                                                                                                                                                         | 1. Hard-refresh the browser tab (F5). 2. Wait for auth to restore and the Queue to render.                                                                                                                                                                                                        | authStateChanges restores the session; the shell re-creates and re-subscribes push_queue_live and scan_jobs_live; the queue and badge repopulate via the initial refresh without a re-login.                                                                                                                                                                   | firebase-auth, supabase:push_queue, realtime:push_queue_live, realtime:scan_jobs_live                                  |
| QUEUE-76 | Concurrent edit vs activate on the same row: edit save loses to a concurrent activation                | both     | queue_realtime    | concurrency    | Two clients on the same pending row.                                                                                                                                                           | 1. Client A enters Edit and changes items. Client B taps Push To Tally and confirms (flips to push_now). 2. Client A taps Save.                                                                                                                                                                   | Client B's activation flips status to push_now first; Client A's save .eq(status,'pending') matches 0 rows -> 'Couldn't save — this voucher may already be queued for push.' The pushed payload is B's (pre-A-edit) version. Confirm A's edits are not silently lost without notice.                                                                           | supabase:push_queue, POST /api/sync/push-queue/activate                                                                |
| PUSH-1   | Open pending voucher detail sheet from queue                                                           | both     | voucher_edit_push | happy          | Signed in; at least one push_queue row with status='pending' visible in the Queue tab.                                                                                                         | 1. Open the Queue tab 2. Tap a pending voucher row to open the detail sheet                                                                                                                                                                                                                       | Sheet opens; status pill shows 'pending'; Edit, Add, Revert (hidden until editing) and 'Push To Tally' controls are visible; if a stored/fresh image exists an Image tab/pane appears, otherwise Summary only.                                                                                                                                                 | supabase:push_queue, realtime:vd_status_{rowId}                                                                        |
| PUSH-2   | Open pushed voucher from History is read-only                                                          | both     | voucher_edit_push | happy          | At least one row with status='pushed' visible in History.                                                                                                                                      | 1. Open History 2. Tap a pushed voucher row                                                                                                                                                                                                                                                       | Sheet opens readOnly: Edit/Add/Revert/Push and per-row delete are hidden; a 'Push Details' block shows pushed date/time from pushed_at; voucher is view-only.                                                                                                                                                                                                  | supabase:push_queue                                                                                                    |
| PUSH-4   | Edit blocked when status is push_now                                                                   | both     | voucher_edit_push | edge           | A push_queue row currently status='push_now' (desktop poller about to pick it up).                                                                                                             | 1. Open the push_now row 2. Tap Edit                                                                                                                                                                                                                                                              | Edit is rejected with snackbar 'already queued for push and can't be edited'; sheet stays in view mode; no editable cells appear.                                                                                                                                                                                                                              | supabase:push_queue                                                                                                    |
| PUSH-8   | Enter edit mode on a pending row seeds editable copies                                                 | both     | voucher_edit_push | happy          | Pending row open; StockItemsCache loaded (not loading).                                                                                                                                        | 1. Tap Edit                                                                                                                                                                                                                                                                                       | Sheet enters edit mode; item cells (qty/rate/disc/amount), party dropdown, invoice#, narration become editable; Revert button appears; originals captured for later revert.                                                                                                                                                                                    | supabase:push_queue                                                                                                    |
| PUSH-11  | Purchase item missing discount_pct: derived discount keeps Disc/Amount/Charges consistent              | both     | voucher_edit_push | data-integrity | Purchase voucher whose item has amount < qty*rate but no discount_pct field.                                                                                                                   | 1. Open the purchase row 2. Tap Edit 3. Read the derived Disc% cell for that item and the Charges block                                                                                                                                                                                           | Disc% shows (1 - amount/(qty*rate))*100 clamped to 0..100; the displayed amount and Charges subtotal/discount reconcile with the line.                                                                                                                                                                                                                         | supabase:push_queue                                                                                                    |
| PUSH-12  | Change party for a SALE voucher uses customers cache                                                   | both     | voucher_edit_push | happy          | Sale pending row open in edit mode; CustomersCache loaded.                                                                                                                                     | 1. Tap the party-name dropdown 2. Search and pick a different customer (Sundry Debtor)                                                                                                                                                                                                            | Party field updates to chosen customer; on Save the largest-abs ledger line is renamed to the new party.                                                                                                                                                                                                                                                       | supabase:push_queue, supabase:ledgers                                                                                  |
| PUSH-13  | Change party for a PURCHASE voucher uses vendors cache                                                 | both     | voucher_edit_push | happy          | Purchase pending row open in edit mode; VendorsCache loaded.                                                                                                                                   | 1. Tap the party-name dropdown 2. Pick a different vendor (Sundry Creditor)                                                                                                                                                                                                                       | Party field updates to chosen vendor; vendor list, not customer list, is shown.                                                                                                                                                                                                                                                                                | supabase:push_queue, supabase:ledgers                                                                                  |
| PUSH-17  | Add item via party-aware sale history (voucher_items)                                                  | both     | voucher_edit_push | happy          | Sale row in edit mode; party has prior sale history in voucher_items.                                                                                                                          | 1. Tap Add 2. Observe the default picker list                                                                                                                                                                                                                                                     | Picker lists items from voucher_items joined vouchers!inner filtered by this party_name and voucher_type like %sale%, newest-first, deduped per stock_item_name keeping the latest rate; selecting one appends qty=1 with that rate.                                                                                                                           | supabase:voucher_items                                                                                                 |
| PUSH-18  | Add item for PURCHASE uses the catalog                                                                 | both     | voucher_edit_push | happy          | Purchase row in edit mode.                                                                                                                                                                     | 1. Tap Add 2. Observe picker source                                                                                                                                                                                                                                                               | Picker shows the StockItemsCache catalog (purchase uses catalog, not party history).                                                                                                                                                                                                                                                                           | supabase:stock_items                                                                                                   |
| PUSH-21  | Appended item rescales GST/tax ledgers and recomputes total                                            | both     | voucher_edit_push | data-integrity | Sale row in edit mode with a matched GST Sale inventory ledger and CGST/SGST tax ledgers.                                                                                                      | 1. Add a new item 2. Inspect the Charges block (GST Sale, CGST, SGST, Discount, Total)                                                                                                                                                                                                            | GST Sale = sum of item amounts; tax ledgers = GST Sale * stored ratio; discount and total recomputed; Charges reconcile with the new item set.                                                                                                                                                                                                                 | supabase:push_queue                                                                                                    |
| PUSH-23  | Qty change recomputes amount = qty*rate*(1-disc%)                                                      | both     | voucher_edit_push | happy          | Row in edit mode.                                                                                                                                                                              | 1. Edit an item's Qty cell to a new value                                                                                                                                                                                                                                                         | Amount cell updates to qty*rate*(1-disc/100); Charges block recomputes.                                                                                                                                                                                                                                                                                        | supabase:push_queue                                                                                                    |
| PUSH-24  | Rate change recomputes amount                                                                          | both     | voucher_edit_push | happy          | Row in edit mode.                                                                                                                                                                              | 1. Edit an item's Rate cell                                                                                                                                                                                                                                                                       | Amount = qty*rate*(1-disc/100) updates; Charges recompute.                                                                                                                                                                                                                                                                                                     | supabase:push_queue                                                                                                    |
| PUSH-25  | Disc change recomputes amount and discount total                                                       | both     | voucher_edit_push | happy          | Row in edit mode.                                                                                                                                                                              | 1. Edit an item's Disc% cell                                                                                                                                                                                                                                                                      | Amount recomputes; Charges Discount = sum(qty*rate*disc%/100) updates.                                                                                                                                                                                                                                                                                         | supabase:push_queue                                                                                                    |
| PUSH-31  | Save returns empty when row already flipped to push_now                                                | both     | voucher_edit_push | error          | Two clients: client A editing; client B (or desktop) flips the row to push_now.                                                                                                                | 1. On client A, after the flip, tap Save                                                                                                                                                                                                                                                          | Status-guarded update matches 0 rows; snackbar "Couldn't save — may already be queued for push"; no payload change in DB.                                                                                                                                                                                                                                      | supabase:push_queue                                                                                                    |
| PUSH-33  | Save writes to the correct payload shape                                                               | both     | voucher_edit_push | data-integrity | Rows of each shape: voucher_payload.items, tally_payload.voucher_payload.items, matched_items, items.                                                                                          | 1. For each shape, edit an item and Save 2. Inspect the persisted payload                                                                                                                                                                                                                         | Edits land in the same key the items were read from; ledger_entries rebuilt with party line=_editableTotal*sign and discount_total set.                                                                                                                                                                                                                        | supabase:push_queue                                                                                                    |
| PUSH-35  | Revert after a Save restores original via status-guarded write                                         | both     | voucher_edit_push | happy          | Row edited and Saved this session, still status=pending.                                                                                                                                       | 1. Tap Revert                                                                                                                                                                                                                                                                                     | _persistEditsToSupabase(_originalPayload) runs (status-guarded), payload restored to original, edit mode exits, _everSaved reset; row stays in the status-filtered queue.                                                                                                                                                                                      | supabase:push_queue                                                                                                    |
| PUSH-38  | Pushing while still editing forces Save-first dialog                                                   | both     | voucher_edit_push | edge           | Row in edit mode with unsaved edits.                                                                                                                                                           | 1. Tap Push To Tally while still editing                                                                                                                                                                                                                                                          | _SaveBeforePushDialog ('Kindly Save the invoice before pushing') shown; OK just dismisses; no save and no activate happen from the dialog.                                                                                                                                                                                                                     | supabase:push_queue                                                                                                    |
| PUSH-39  | Push blocked: party changed but not all items changed                                                  | both     | voucher_edit_push | edge           | Row where party was changed but at least one item still equals original.                                                                                                                       | 1. Exit edit (Save), tap Push To Tally (or press Enter on Total)                                                                                                                                                                                                                                  | Push blocked with snackbar 'Party Name is Changed!! Change all the items'; button renders disabled; _activate not called.                                                                                                                                                                                                                                      | supabase:push_queue                                                                                                    |
| PUSH-40  | Party-change guard is client-only and does NOT block Save/activate-by-other-means                      | both     | voucher_edit_push | data-integrity | Row with party changed, not all items changed.                                                                                                                                                 | 1. Save the edited payload (allowed) 2. Activate the row via another path (e.g. another client's Push, or backend activate directly)                                                                                                                                                              | DEFECT WATCH: the guard only gates this sheet's Push button; the edited payload can still be saved and later activated, pushing a party-mismatched voucher. Flag the bypass.                                                                                                                                                                                   | supabase:push_queue, main-backend:POST /api/sync/push-queue/activate                                                   |
| PUSH-41  | Narration is force-overwritten to 'Replara AI' discarding user narration                               | both     | voucher_edit_push | data-integrity | Row in edit mode.                                                                                                                                                                              | 1. Type a custom narration, Save (it persists), then Push 2. Inspect pushed voucher_payload.narration                                                                                                                                                                                             | narration is unconditionally set to 'Replara AI' at push, discarding the user-typed narration that was saved moments earlier; irreversible at push time. Confirm this is intended behavior; flag the data loss.                                                                                                                                                | supabase:push_queue                                                                                                    |
| PUSH-47  | Activate network down / timeout                                                                        | both     | voucher_edit_push | error          | Device offline or backend unreachable.                                                                                                                                                         | 1. Tap Push, confirm                                                                                                                                                                                                                                                                              | Catch branch: snackbar 'Error: <exception>'; reportFailure fired; _isSubmitting cleared; the preceding voucher_payload+narration write is NOT rolled back (on retry/desktop pickup the row carries forced narration).                                                                                                                                          | main-backend:POST /api/sync/push-queue/activate, supabase:push_queue                                                   |
| PUSH-49  | Partial Tally push (created>=1 AND errors>=1) reported as green success                                | both     | voucher_edit_push | data-integrity | Engineer a tally_response with created>=1 and errors>=1 on the pushed row.                                                                                                                     | 1. Push; let the realtime row carry created>=1 and errors>=1 2. Observe the result modal (sale)                                                                                                                                                                                                   | DEFECT WATCH: success=(created>=1) wins, so a green success modal is shown masking partially-failed line items. Flag that partial failures are hidden from the user.                                                                                                                                                                                           | realtime:vd_status_{rowId}, supabase:push_queue                                                                        |
| PUSH-50  | Push fails at Tally: realtime status=failed shows fail dialog (sale)                                   | both     | voucher_edit_push | error          | Sale row; Tally rejects the voucher (created=0, errors>=1 or status=failed).                                                                                                                   | 1. Push; wait for the realtime terminal update                                                                                                                                                                                                                                                    | _handlePushResult classifies failure; sale fail dialog shown after sheet closes; error_message surfaced; status pill reflects 'failed'.                                                                                                                                                                                                                        | realtime:vd_status_{rowId}, supabase:push_queue                                                                        |
| PUSH-53  | Delete voucher removes row and pops sheet                                                              | both     | voucher_edit_push | happy          | Pending row open with onDiscard provided and __row_id present.                                                                                                                                 | 1. Tap Delete 2. Confirm 'Are you sure You want to delete this Invoice'                                                                                                                                                                                                                           | onDiscard removes the row locally, sheet pops immediately, then push_queue.delete().eq('id') runs; 'Deleted.' snackbar; realtime DELETE syncs other clients.                                                                                                                                                                                                   | supabase:push_queue                                                                                                    |
| PUSH-54  | Delete failure leaves DB row (reappears on refresh)                                                    | both     | voucher_edit_push | data-integrity | Force the delete to throw (offline / RLS deny).                                                                                                                                                | 1. Tap Delete, confirm (sheet pops before awaiting) 2. Pull-to-refresh or wait for realtime                                                                                                                                                                                                       | DEFECT WATCH: sheet popped and row dropped locally before the awaited delete; on failure reportHandledError('supabase.push_queue.delete') + "Couldn't delete" snackbar, and the row reappears on next refresh (no compensating re-insert). Flag the optimistic-delete inconsistency.                                                                           | supabase:push_queue                                                                                                    |
| PUSH-65  | Two clients open the same pending row; one pushes                                                      | both     | voucher_edit_push | concurrency    | Phone and web both open the same pending voucher sheet.                                                                                                                                        | 1. On web, Push To Tally and confirm 2. Observe the phone's open sheet                                                                                                                                                                                                                            | The phone's vd_status_{rowId} subscription receives the status flip (push_now -> pushed/failed); its status pill updates; if the phone tries to Save after the flip it gets the empty-result 'may already be queued' message.                                                                                                                                  | realtime:vd_status_{rowId}, supabase:push_queue                                                                        |
| PUSH-66  | Desktop flips row to push_now while user mid-edit on phone                                             | both     | voucher_edit_push | concurrency    | Phone editing; desktop poller activates the same row.                                                                                                                                          | 1. Begin editing on phone 2. Let desktop flip status to push_now 3. Tap Save on phone                                                                                                                                                                                                             | Status-guarded Save matches 0 rows -> "Couldn't save — may already be queued for push"; phone edits are not persisted over the in-flight row.                                                                                                                                                                                                                  | supabase:push_queue                                                                                                    |
| PUSH-73  | Enter on Total field submits the push                                                                  | web      | voucher_edit_push | platform       | Web/desktop, row not in edit mode (or after save), physical keyboard.                                                                                                                          | 1. Tab/Enter to the Total field 2. Press Enter                                                                                                                                                                                                                                                    | _submitFromTotal runs: if party-changed-guard passes, the push confirm dialog opens; if blocked, the 'change all the items' snackbar shows; no push when still editing (Save-first dialog).                                                                                                                                                                    | supabase:push_queue, main-backend:POST /api/sync/push-queue/activate                                                   |
| PUSH-82  | RLS prevents editing another tenant's push_queue row                                                   | both     | voucher_edit_push | security       | Anon/publishable key with RLS; a row id belonging to a different tenant project.                                                                                                               | 1. Attempt a Save/activate write targeting a foreign row id (via crafted request)                                                                                                                                                                                                                 | RLS rejects the write (0 rows / error); no cross-tenant payload mutation occurs; handled error reported.                                                                                                                                                                                                                                                       | supabase:push_queue                                                                                                    |
| HIST-1   | History loads pushed vouchers grouped by month, newest first                                           | both     | history_images    | happy          | Signed in on testing project (yynuu). At least 3 push_queue rows with status='pushed' across 2 different months, each with a valid voucher_payload (party_name, voucher_type, ledger_entries). | 1. Open the app and navigate to the History tab. 2. Observe the list while it loads. 3. Note the month section headers and the order of cards within each section.                                                                                                                                | List renders cards grouped under full-month headers (e.g. 'June 2026'). Within and across groups, cards are sorted by pushed_at descending (newest at top). Each card shows party name, date label (e.g. '28 Jun'), and signed amount.                                                                                                                         | Supabase: push_queue select (status='pushed'), Realtime: history_push_queue                                            |
| HIST-3   | Initial history select failure is swallowed -> empty list, no error UI                                 | both     | history_images    | error          | Ability to force the initial Supabase select to fail (disable network before opening History, or temporarily break RLS/anon key).                                                              | 1. Disable the device/browser network (airplane mode / offline). 2. Open the History tab fresh (cold). 3. Observe the screen.                                                                                                                                                                     | Screen shows 'No records' with NO error banner/toast/dialog (catch{} swallows the error). Restoring network and re-entering the screen reloads the data. Document that a transient backend/RLS hiccup is indistinguishable from genuinely-empty history.                                                                                                       | Supabase: push_queue select (status='pushed')                                                                          |
| HIST-7   | voucher_type classification: 'SALE' -> Sale, blank/missing -> Purchase                                 | both     | history_images    | data-integrity | Pushed rows: (a) voucher_type='Sales', (b) voucher_type missing/blank, (c) voucher_type='Purchase'.                                                                                            | 1. Open History. 2. Locate each of the three rows. 3. Check which filter tab each appears under and the +/- sign / color of the amount.                                                                                                                                                           | Row (a) classified Sale (appears under 'Sale'). Rows (b) and (c) classified Purchase (appear under 'Purchase'). Confirm that a blank voucher_type silently buckets as Purchase and flips receivable vs payable presentation — flag as a data-integrity risk.                                                                                                   | Supabase: push_queue (voucher_payload.voucher_type)                                                                    |
| HIST-12  | Card amount = abs(first ledger_entries.amount)                                                         | both     | history_images    | data-integrity | Pushed sale row whose ledger_entries.first is the party/total ledger with a negative amount (e.g. -11800).                                                                                     | 1. Open History and locate the row. 2. Read the displayed amount.                                                                                                                                                                                                                                 | Card shows the absolute value (11800) of the FIRST ledger entry. Verify this equals the intended voucher total for a normal single-party voucher.                                                                                                                                                                                                              | Supabase: push_queue (voucher_payload.ledger_entries)                                                                  |
| HIST-13  | Multi-ledger tax-split voucher can show a wrong/partial amount                                         | both     | history_images    | data-integrity | Pushed row where ledger_entries[0] is a GST/rounding ledger (e.g. 900) and the true total is on a later entry (e.g. 5900).                                                                     | 1. Open History and find the row. 2. Compare the displayed amount with the actual invoice total.                                                                                                                                                                                                  | Card displays abs(ledger_entries[0].amount) = 900, NOT the 5900 total — a money-display integrity defect when the party/total ledger is not first. Display-only (no write). Record as confirmed risk.                                                                                                                                                          | Supabase: push_queue (voucher_payload.ledger_entries)                                                                  |
| HIST-15  | Tap a history card opens a read-only voucher detail sheet                                              | both     | history_images    | happy          | History list with at least one tappable card (non-empty rowId, row present in map).                                                                                                            | 1. Open History. 2. Tap a card. 3. Inspect the bottom sheet that opens.                                                                                                                                                                                                                           | VoucherDetailSheet opens with readOnly=true: shows Summary (party/items/charges) and, if applicable, an Image tab. There are NO edit/push/save controls (read-only). Synthetic keys __row_id/__status/__pushed_at/__error_message/__source_payload are populated (status defaults 'pushed').                                                                   | Supabase: push_queue (voucher_payload), Realtime: vd_status_<rowId>                                                    |
| HIST-18  | Single-page scanned invoice image loads via backend/GCS                                                | both     | history_images    | happy          | Pushed row from a real camera/OCR scan with source_payload.scan.pages=1 and the page-0.jpg present in GCS; INVOICE_BUCKET configured on backend.                                               | 1. Open History, tap the row. 2. On mobile tap the 'Image' tab; on wide web observe the left image pane. 3. Wait for the page to load.                                                                                                                                                            | A spinner shows briefly, then the invoice page renders (Image.memory, fit width). No 'Page X of N' header for a single page. The bytes came from GET /api/sync/push-queue/<rowId>/image/0.                                                                                                                                                                     | Main backend: GET /api/sync/push-queue/:id/image/:page, GCS invoice bucket, Firebase Auth (Bearer)                     |
| HIST-19  | Multi-page invoice shows 'Page X of N', lazy-loads, caches on scroll-back                              | both     | history_images    | happy          | Pushed row with source_payload.scan.pages=3 and all 3 page JPEGs present in GCS.                                                                                                               | 1. Open the row's Image view. 2. Scroll down through pages 1->2->3, watching each load. 3. Scroll back up to page 1.                                                                                                                                                                              | Each page lazily fetched once (separate GET per page index 0..2) with its own spinner; headers read 'Page 1 of 3', 'Page 2 of 3', 'Page 3 of 3'. Scrolling back up does NOT re-download (in-memory _pageFutures cache). Network trace shows exactly 3 image GETs.                                                                                              | Main backend: GET /api/sync/push-queue/:id/image/:page, GCS invoice bucket                                             |
| HIST-25  | Expired/invalid Firebase token still authorizes image GET via x-api-key                                | both     | history_images    | security       | A way to make getIdToken() return a stale/invalid token while Config.mrpApiKey is valid; row with scan.pages>0.                                                                                | 1. Force the Firebase token to be invalid/expired. 2. Open the row's Image view.                                                                                                                                                                                                                  | requireApiKey accepts the always-sent x-api-key (timingSafeEqual vs API_KEY) even though the Bearer token is bad, so the page still loads (no 401). Confirms ApiClient.getBytes sends BOTH x-api-key and Authorization, and strips Content-Type for binary GET.                                                                                                | Main backend: requireApiKey, Firebase Auth (Bearer), x-api-key                                                         |
| HIST-28  | Partial GCS upload: scan.pages overcounts -> some pages 404 while others render                        | both     | history_images    | data-integrity | Pushed row where source_payload.scan.pages = number REQUESTED (e.g. 3) but only 2 pages actually uploaded to GCS (uploadInvoicePages best-effort failure).                                     | 1. Open the row's Image view and scroll through all advertised pages.                                                                                                                                                                                                                             | Stored pages render; the missing page(s) show 'Couldn't load this page.' The header still advertises N pages (overcount). Document that scan.pages is the count requested, not persisted — users may think a stored image is corrupt when only some pages failed.                                                                                              | Supabase: push_queue (source_payload.scan.pages), Main backend: GET image (404), GCS invoice bucket                    |
| HIST-34  | Realtime INSERT of a pushed row appears in History live                                                | both     | history_images    | concurrency    | History open on client A. Ability to INSERT a push_queue row with status='pushed' (console, or another client completing a push).                                                              | 1. Keep History open on client A. 2. INSERT a new pushed row (or push a voucher to completion on client B). 3. Observe client A without manual refresh.                                                                                                                                           | history_push_queue INSERT handler ingests the row (status=='pushed') and it appears in A's list, grouped/sorted correctly, with no manual refresh.                                                                                                                                                                                                             | Realtime: history_push_queue (INSERT), Supabase: push_queue                                                            |
| HIST-36  | Realtime UPDATE to 'pushed' adds row; UPDATE away from 'pushed' removes it                             | both     | history_images    | concurrency    | History open on client A; a push_queue row currently visible/pushed and another currently pending.                                                                                             | 1. Flip the pending row's status -> 'pushed' (e.g. complete a push elsewhere). 2. Then flip a currently-shown pushed row's status -> 'failed' (or back to pending). 3. Observe client A after each change.                                                                                        | First change ingests the row into History (appears). Second change removes the row from History (no longer 'pushed') silently, with no tombstone/animation.                                                                                                                                                                                                    | Realtime: history_push_queue (UPDATE), Supabase: push_queue                                                            |
| HIST-38  | TOAST-omitted voucher_payload on realtime UPDATE triggers PostgREST re-fetch                           | both     | history_images    | data-integrity | History open. A row that flips to status='pushed' via UPDATE while its large voucher_payload column is unchanged (so Realtime omits it).                                                       | 1. Cause a status-only UPDATE -> 'pushed' on a row with an unchanged voucher_payload (e.g. set pushed_at/status without touching payload). 2. Observe the new card's party name and amount.                                                                                                       | _ingestPushedRow detects the empty parsed payload and re-fetches the full row by id from PostgREST, so the card shows the correct party/amount (NOT 'Unknown'/0).                                                                                                                                                                                              | Realtime: history_push_queue (UPDATE), Supabase: push_queue select by id (.maybeSingle)                                |
| HIST-41  | Phone scans+pushes a voucher while web History is open -> web updates live                             | both     | history_images    | concurrency    | Same tenant project. Web client signed in with History open. Android phone signed in, ready to scan an invoice. Parsing + push pipeline operational.                                           | 1. On the phone, scan/submit an invoice (creates scan_jobs row, parsing inserts a push_queue row). 2. On the phone, push the resulting queue voucher to completion (status -> pushed). 3. Watch the web History screen the whole time without refreshing.                                         | Web History does not show the row while it is pending/push_now (only 'pushed' qualifies); once the push completes, the pushed row appears at the top of web History via the history_push_queue realtime channel. Demonstrates cross-device realtime consistency.                                                                                               | Parsing service (Cloud Run), Supabase: scan_jobs/push_queue, Realtime: history_push_queue, Main backend: push activate |
| HIST-48  | Web refresh restores session and image GET re-auth still works                                         | web      | history_images    | platform       | Web build, signed in, History open with a scanned row.                                                                                                                                         | 1. Hard-refresh the browser tab. 2. Wait for session restore (authStateChanges), navigate to History. 3. Open the scanned row's Image view.                                                                                                                                                       | Session is restored via authStateChanges; a fresh Firebase token is obtained for getBytes, so image GETs authorize and pages render. No re-login required, no 401 cascade.                                                                                                                                                                                     | Firebase Auth (session restore), Main backend: GET image                                                               |
| HIST-52  | RLS limits History rows to the authorized tenant (anon/publishable key)                                | both     | history_images    | security       | Two tenant projects (yynuu testing vs another) OR two users with distinct RLS scope; pushed rows in each.                                                                                      | 1. Sign in as user/tenant A and note the History rows. 2. Sign in as user/tenant B and note the History rows.                                                                                                                                                                                     | Each session only sees rows its RLS policy permits; no rows from the other tenant/user leak. Confirms the app's reliance on RLS (it sends no scoping filter and uses the anon/publishable key) is sound for the tested policy.                                                                                                                                 | Supabase: push_queue, Supabase RLS, Firebase Auth (identity)                                                           |
| HIST-53  | History detail sheet is strictly read-only (no edit/push affordances)                                  | both     | history_images    | data-integrity | Any pushed row.                                                                                                                                                                                | 1. Open a history card's detail sheet. 2. Inspect Summary for any editable fields, item add/remove, party change, or a Push/Save button.                                                                                                                                                          | readOnly=true: no editable inputs, no item/party mutation, no Push or Save action. The sheet cannot write voucher_payload or call activate. Closing the sheet leaves the row unchanged in push_queue.                                                                                                                                                          | Supabase: push_queue (read-only), Main backend: activate (must NOT be called)                                          |
| RPT-1    | Insights list renders all 7 seed categories on open                                                    | both     | reports           | happy          | Signed in. App on the Report screen.                                                                                                                                                           | 1. Open the Report screen (bottom-nav/side-rail Report). 2. Confirm the 'Insights' tab is selected by default (tab index 0). 3. Observe the scrollable list.                                                                                                                                      | Exactly 7 rows appear in order: act_now (🚨), hero_sku_health (🏆), dead_capital (💀), buying_mistakes (⚠️), wind_down (🪟), risk_watch (👀), full_portfolio_health (📊). Each shows /<key>, emoji and its description. No network request is fired until a row is tapped (verify via proxy/devtools: zero GETs on load).                                      | local-disk (seedReportCategories)                                                                                      |
| RPT-4    | Open all 7 reports (R1-R7) in sequence on web each returns its filtered view                           | web      | reports           | happy          | Signed in on web; backend healthy; single synced 'K V ENTERPRISES'.                                                                                                                            | 1. Tap each of the 7 report rows one at a time, closing the sheet between each. 2. For each, note the URL reportId (R1..R7) and the footerMeta label. 3. Verify the rendered columns/rows differ per report key.                                                                                  | Each tap issues GET .../reorder-levels/R<n> with the same company_name/format query and returns 200. Each sheet shows that report's footerMeta (e.g. 'Live data · sorted by ₹ impact' for act_now) and the report-key-specific data. No tap reuses another report's payload.                                                                                   | main-backend GET /api/sync/reorder-levels/:reportKey, supabase: companies (reorderSupabase)                            |
| RPT-5    | Mobile cache miss: first open fetches and writes <appDocs>/reports/<reportId>.csv                      | mobile   | reports           | happy          | Android device, signed in, backend healthy. App data cleared so /reports/ has no cached CSV.                                                                                                   | 1. Tap 'dead_capital' (R3) for the first time. 2. Wait for the sheet to render. 3. Inspect app documents directory /reports/ (via device file explorer or adb).                                                                                                                                   | A GET .../reorder-levels/R3 is issued (cache miss path). On 200, the CSV is written to <appDocs>/reports/R3.csv. The sheet renders the data and a Share icon is present (mobile, reportId!=null).                                                                                                                                                              | main-backend GET /api/sync/reorder-levels/:reportKey, local-disk cache (/reports/R3.csv)                               |
| RPT-6    | Mobile cache hit: re-tap serves disk snapshot with NO network call                                     | mobile   | reports           | happy          | Android. R3 already opened once so /reports/R3.csv exists.                                                                                                                                     | 1. Start a network capture. 2. Tap 'dead_capital' (R3) again. 3. Observe the sheet and the capture.                                                                                                                                                                                               | No GET is sent. The sheet renders from the on-disk R3.csv (file.readAsString). Note the footer still says 'Live data ...' even though data is the frozen cached snapshot.                                                                                                                                                                                      | local-disk cache (/reports/R3.csv)                                                                                     |
| RPT-9    | Live Σ footer totals closing_stock_amount across all rows                                              | both     | reports           | happy          | Backend returns a CSV containing the 'closing_stock_amount' header with multiple numeric rows.                                                                                                 | 1. Open a report whose CSV has a closing_stock_amount column (e.g. R7). 2. Manually add the closing_stock_amount values from the visible rows. 3. Compare to the Σ figure in the footer.                                                                                                          | sumColumnIndex resolves to indexOf('closing_stock_amount'); footer shows 'Σ <total>' formatted en-IN currency and countUnit 'Items'. The displayed Σ equals the client-side sum of the column over all rows (note: sum of pre-rounded integers).                                                                                                               | main-backend GET /api/sync/reorder-levels/:reportKey                                                                   |
| RPT-15   | Renaming the closing_stock_amount header silently drops the Σ money figure                             | both     | reports           | edge           | Ability to make the backend emit a different header label for the closing-stock column (e.g. via a backend build with the column renamed) or a mocked CSV.                                     | 1. Force a CSV whose money column is NOT literally named 'closing_stock_amount'. 2. Open the report. 3. Inspect the footer.                                                                                                                                                                       | columnLabels.indexOf('closing_stock_amount') returns -1 -> sumColumnIndex=null -> footer falls back to static footerLeading and 'N rows' (countUnit 'rows'), NO Σ total displayed. This confirms tight, fragile backend↔app coupling on the exact header literal.                                                                                              | main-backend GET /api/sync/reorder-levels/:reportKey                                                                   |
| RPT-17   | Items with no purchase history emit blank closing_stock_amount and understate Σ                        | both     | reports           | data-integrity | Client Supabase has at least one stock item with closing_qty>0 but no purchase voucher (purchaseRateRaw null).                                                                                 | 1. Open R7 (full portfolio) for a company having such items. 2. Locate the no-purchase-history item row; note its closing_stock_amount cell. 3. Compare the headline Σ to a manual valuation that values those items by some non-zero rate.                                                       | closing_stock_amount = closing_qty * purchaseRate; with purchaseRate null the cell is blank, _cellToNum('')=0, so the item contributes ₹0 to the Σ. The displayed closing-stock value silently UNDERSTATES true stock value. Flag as a data-integrity defect.                                                                                                  | main-backend GET /api/sync/reorder-levels/:reportKey, supabase: stock_items, supabase: vouchers                        |
| RPT-19   | Network down: report tap surfaces a snackbar and clears the spinner                                    | both     | reports           | error          | Signed in. Device offline / backend unreachable (airplane mode or block the host).                                                                                                             | 1. Tap any report row. 2. Wait for the request to fail. 3. Observe the row spinner and any snackbar.                                                                                                                                                                                              | client.get throws (SocketException etc.); _openReport catch clears _loadingCategoryKey and shows snackbar 'Failed to load report: <exception>'. The sheet does NOT open. On mobile, nothing is written to disk (write only happens after a 200), so no poisoned cache. ApiClient.reportFailure also logs to Sentry.                                            | main-backend GET /api/sync/reorder-levels/:reportKey, Sentry                                                           |
| RPT-21   | 404 Company not found (name drift / wrong DB) -> 'Request failed: 404'                                 | both     | reports           | error          | Client Supabase has NO companies row exactly matching 'K V ENTERPRISES' (e.g. casing/whitespace differs, or pointing at a DB without that company).                                            | 1. Tap any report row. 2. Observe the snackbar.                                                                                                                                                                                                                                                   | resolveCompanyLookup .eq('name','K V ENTERPRISES') returns 0 rows -> 404. Snackbar 'Failed to load report: Exception: Request failed: 404'. Because the company name is hardcoded in the app, any DB name drift makes ALL reports fail for everyone.                                                                                                           | main-backend GET /api/sync/reorder-levels/:reportKey, supabase: companies (reorderSupabase)                            |
| RPT-23   | 409 company not yet synced (null last_synced_at) -> same generic 409 snackbar                          | both     | reports           | error          | Single 'K V ENTERPRISES' company exists in client Supabase but its last_synced_at is null (freshly added, no successful sync).                                                                 | 1. Tap any report row. 2. Observe the snackbar.                                                                                                                                                                                                                                                   | resolveCompanyLookup defaults requireSuccessfulSync=true here -> 409 'Company has not completed a successful sync yet'. App shows 'Failed to load report: Exception: Request failed: 409' — INDISTINGUISHABLE from the duplicate-company 409. Reports become available only after a full sync lands.                                                           | main-backend GET /api/sync/reorder-levels/:reportKey, supabase: companies (reorderSupabase)                            |
| RPT-29   | Mobile stale cache: data changes in Supabase but report shows old numbers                              | mobile   | reports           | data-integrity | Android. A report (e.g. R1) already cached on disk. You can change underlying stock/voucher data in client Supabase (or run a fresh Tally sync).                                               | 1. Open R1 on mobile (caches it). 2. Change the underlying data so the report should differ (e.g. add stock, complete a new sync). 3. Re-open R1 on mobile.                                                                                                                                       | The mobile report still shows the OLD cached numbers (no TTL, no refresh, no invalidation), yet the footer labels it 'Live data'. This is a correctness/trust issue — the only way to refresh is clearing app data. Document as a P1 staleness defect.                                                                                                         | local-disk cache, supabase: stock_items, supabase: vouchers                                                            |
| RPT-32   | Cache not cleared on sign-out (verify files persist)                                                   | mobile   | reports           | data-integrity | Android. At least one report cached to /reports/.                                                                                                                                              | 1. Open a report (caches it). 2. Sign out of the app. 3. Inspect <appDocs>/reports/ via file explorer/adb.                                                                                                                                                                                        | The /reports/<id>.csv file(s) still exist after sign-out (sign-out clears only in-memory caches). Confirms the missing-cleanup behavior underlying the privacy and staleness risks.                                                                                                                                                                            | local-disk cache                                                                                                       |
| RPT-34   | API-key coupling: API_KEY set but API_KEY_CLIENT forgotten breaks only Reports                         | both     | reports           | data-integrity | Backend deploy where API_KEY (used by every other endpoint) is set correctly but API_KEY_CLIENT is missing/wrong.                                                                              | 1. Exercise a normal flow (e.g. push-queue activate) and confirm it works. 2. Then tap a report.                                                                                                                                                                                                  | All non-report flows work, but every report tap returns 401 ('Unauthorized' snackbar) because reorder-levels uses its OWN key pair. Confirms the independent-key coupling is easy to miss in a deploy; add to deploy checklist.                                                                                                                                | main-backend GET /api/sync/reorder-levels/:reportKey, requireClientApiKey / API_KEY_CLIENT                             |
| MAST-2   | Web cold-start / page refresh restores session and refetches caches                                    | web      | masters_caches    | happy          | Already signed in on web.                                                                                                                                                                      | 1. With the app open and signed in, press browser Refresh (F5). 2. Wait for the splash to resolve to the shell. 3. Open a pending voucher, enter edit mode, and open the stock 'All items' picker and the party picker.                                                                           | Session is restored via authStateChanges (no re-login prompt) and all three caches are re-fetched fresh; pickers are populated after a brief load.                                                                                                                                                                                                             | Firebase Auth (authStateChanges), Supabase: stock_items, Supabase: ledgers                                             |
| MAST-4   | Sign-out clears all caches (no stale master data left in memory)                                       | both     | masters_caches    | happy          | Signed in with populated caches.                                                                                                                                                               | 1. From Profile, sign out. 2. Sign back in as the SAME user but immediately (before fetch completes) open a voucher and inspect pickers, then again after fetch completes.                                                                                                                        | On sign-out caches are emptied (items=[]); after re-login they repopulate. No party/stock rows from the previous session persist before the new fetch lands.                                                                                                                                                                                                   | Firebase Auth (authStateChanges), Supabase: stock_items, Supabase: ledgers                                             |
| MAST-6   | Stock catalog pagination loads the full ~13k item set (beyond 1000)                                    | both     | masters_caches    | happy          | stock_items has well over 1000 rows (e.g. ~13k).                                                                                                                                               | 1. Sign in and wait for stock load to finish. 2. Open a pending voucher, Edit, Add Item, toggle 'All items' ON. 3. Read the 'count :' value, then search for a stock item known to sort late alphabetically (e.g. starting with 'Z').                                                             | Count reflects the full catalog (~13k, not capped at 1000); the late-alphabetical item is present, proving .range() pagination looped past the first 1000-row page.                                                                                                                                                                                            | Supabase: stock_items (.range pagination)                                                                              |
| MAST-13  | Customer fetch failure produces NO visible snackbar (scaffoldKey never wired)                          | both     | masters_caches    | error          | Ability to make the ledgers Sundry Debtors fetch throw (network off at sign-in / block host). Sentry observable.                                                                               | 1. Disable network and sign in so CustomersCache.fetch throws. 2. Watch the app scaffold for any snackbar. 3. Check the Sentry dashboard for a 'supabase.customers.fetch' event.                                                                                                                  | DEFECT TO CONFIRM: no user-visible snackbar appears (CustomersCache.scaffoldKey is never assigned in main.dart), so the failure is silent client-side; only a Sentry reportHandledError event records it. Party picker is empty.                                                                                                                               | Supabase: ledgers (Sundry Debtors), Sentry (reportHandledError only)                                                   |
| MAST-14  | Vendor fetch failure produces NO visible snackbar (scaffoldKey never wired)                            | both     | masters_caches    | error          | Ability to make the ledgers Sundry Creditors fetch throw. Sentry observable.                                                                                                                   | 1. Disable network and sign in so VendorsCache.fetch throws. 2. Watch for a snackbar; then check Sentry for 'supabase.vendors.fetch'.                                                                                                                                                             | DEFECT TO CONFIRM: no snackbar (VendorsCache.scaffoldKey never wired); only Sentry records it. Vendor picker is empty with no on-screen warning.                                                                                                                                                                                                               | Supabase: ledgers (Sundry Creditors), Sentry                                                                           |
| MAST-15  | RLS/anon-key misconfig returns 200 [] for ledgers -> empty party picker, no error                      | both     | masters_caches    | data-integrity | App pointed at a project where the anon/publishable key or RLS yields zero ledgers rows for the group filters (200 empty array).                                                               | 1. Sign in and wait. 2. Open a pending sale voucher, Edit, tap party name. 3. Repeat on a purchase voucher for vendors.                                                                                                                                                                           | Both party pickers are empty with no exception, no snackbar, no Sentry event (mirrors the parsing service_role-key memory note). User cannot reselect a party and gets no indication why.                                                                                                                                                                      | Supabase: ledgers (RLS, anon key)                                                                                      |
| MAST-16  | Open customer picker on a pending SALE voucher lists Sundry Debtors                                    | both     | masters_caches    | happy          | Signed in, caches loaded, a PENDING sale voucher in queue.                                                                                                                                     | 1. Open the sale voucher and tap Edit. 2. Tap the party name (shows dropdown caret) to open _CustomerPickerSheet. 3. Type part of a known customer name.                                                                                                                                          | Picker opens with the customer list (Sundry Debtors); case-insensitive name.contains filtering narrows results per keystroke; tapping a row sets the editable party name.                                                                                                                                                                                      | Supabase: ledgers (Sundry Debtors)                                                                                     |
| MAST-17  | Open vendor picker on a pending PURCHASE voucher lists Sundry Creditors                                | both     | masters_caches    | happy          | Signed in, caches loaded, a PENDING purchase voucher in queue.                                                                                                                                 | 1. Open the purchase voucher and tap Edit. 2. Tap the party name to open the picker (search hint reads 'Search vendors…'). 3. Filter and select a vendor.                                                                                                                                         | Picker lists vendors (Sundry Creditors); selecting one updates the editable party name.                                                                                                                                                                                                                                                                        | Supabase: ledgers (Sundry Creditors)                                                                                   |
| MAST-18  | Party picker tap while ledger cache is still loading shows 'still downloading' and does not open       | both     | masters_caches    | edge           | Sign-in with a slow/throttled connection so CustomersCache.isLoading is still true when the voucher is opened.                                                                                 | 1. Immediately after login, open a pending sale voucher and tap Edit. 2. Tap the party name while the customer cache is still loading.                                                                                                                                                            | A snackbar 'Please wait, customer list is still downloading…' appears and the picker does NOT open (guard at the party tap). Same with 'vendor list' wording on a purchase voucher.                                                                                                                                                                            | Supabase: ledgers, UI guard (CustomersCache/VendorsCache.isLoading)                                                    |
| MAST-21  | Stock picker defaults to party-history view and toggles to full catalog                                | both     | masters_caches    | happy          | Signed in, caches loaded, a pending sale voucher with a party that has prior voucher_items.                                                                                                    | 1. Open the sale voucher, Edit, tap Add Item. 2. Observe the initial list (All items switch OFF = party-specific history). 3. Toggle 'All items' ON and observe the list change to the full catalog.                                                                                              | Default view shows party-specific items (source same_party, last rates); toggling 'All items' switches to the cached catalog. The 'count :' header updates with each view/filter.                                                                                                                                                                              | Supabase: voucher_items + vouchers (inner join), Supabase: stock_items (cache)                                         |
| MAST-28  | Party with no prior history shows empty party view; catalog toggle still works                         | both     | masters_caches    | edge           | A pending voucher whose party has zero matching voucher_items (new customer/vendor).                                                                                                           | 1. Open the voucher, Edit, Add Item (party-history view). 2. Observe the empty list, then toggle 'All items' ON.                                                                                                                                                                                  | Party-history view is empty (count : 0) but does not error; toggling to 'All items' shows the full catalog so the user can still add a line.                                                                                                                                                                                                                   | Supabase: voucher_items, Supabase: stock_items                                                                         |
| MAST-33  | Cache staleness: item synced by desktop AFTER sign-in is invisible until re-login                      | both     | masters_caches    | data-integrity | Signed in on the app. A desktop TallyBridge sync adds a NEW stock item (and a new ledger) AFTER the app's sign-in.                                                                             | 1. Note that the app caches loaded at sign-in. 2. On a separate machine, run a desktop sync that inserts a new stock item and a new Sundry Debtor. 3. Without re-logging-in, open a pending voucher, Edit, open the stock 'All items' picker and the party picker and search for the new entries. | The new item/ledger are ABSENT (caches refresh only at sign-in; no realtime/polling). After signing out and back in (or a web refresh), they appear. Adding a not-yet-cached line resolves unit/partCode to '' -> 'NOS'.                                                                                                                                       | Supabase: stock_items, Supabase: ledgers, (no realtime subscription in this domain)                                    |
| MAST-34  | Concurrency: phone scan + desktop sync while web session open keeps web masters stale                  | web      | masters_caches    | concurrency    | Web app signed in with caches loaded. A phone scan and/or desktop sync introduces new masters.                                                                                                 | 1. Keep the web app open and signed in. 2. On the phone, scan a new invoice (parsing inserts a push_queue row) and/or run a desktop sync that adds masters. 3. On web, open a voucher and check the stock/party pickers for the new masters without refreshing.                                   | Web pickers do NOT reflect the newly added masters until a refresh/re-login (in-memory caches, no realtime). Note distinction: the scan badge/queue update via realtime, but masters do not.                                                                                                                                                                   | Supabase: stock_items, Supabase: ledgers, Supabase realtime (queue/scan only, NOT masters)                             |
| MAST-36  | Open voucher before caches finish -> entering edit mode blocked with stock-loading snackbar            | both     | masters_caches    | edge           | Throttled network so StockItemsCache.isLoading is still true shortly after sign-in.                                                                                                            | 1. Immediately after login, open a pending voucher. 2. Tap Edit (which would seed the edit session / add-item path) while stock is still loading.                                                                                                                                                 | A snackbar 'Loading of stock items in process, please wait' appears and edit mode is NOT entered (guard at !_isEditing && StockItemsCache.isLoading), preventing add-item against an incomplete catalog.                                                                                                                                                       | Supabase: stock_items, UI guard (StockItemsCache.isLoading)                                                            |
| MAST-45  | Customer/Vendor caches truncated at 1000 give no on-screen indication of incompleteness                | both     | masters_caches    | data-integrity | ledgers Sundry Debtors slightly over 1000 rows.                                                                                                                                                | 1. Open the customer picker and read whether any 'more results' / truncation indicator exists. 2. Scroll to the end of the list.                                                                                                                                                                  | DEFECT TO CONFIRM: exactly 1000 rows load with NO truncation warning or 'showing 1000 of N' hint, so the tester/user cannot tell the list is incomplete.                                                                                                                                                                                                       | Supabase: ledgers (1000-row cap)                                                                                       |
| XCUT-1   | Testing flavor build targets yynuu Supabase + 828 backend + testing Firebase                           | both     | crosscutting      | happy          | Source checkout; env/testing.json present; able to build with --flavor testing --dart-define-from-file=env/testing.json.                                                                       | 1. Build the app with FLAVOR=testing and run it. 2. Sign in with a test phone number and complete OTP. 3. Open any screen that reads Supabase (queue/history) and one that calls the main backend (a report). 4. Inspect Sentry events / network logs to confirm hostnames.                       | Supabase requests go to yynuuysvjeipawzfbeme.supabase.co; main-backend calls go to tallybridge-backend-828647628834; Firebase auth uses tallybridge-testing-env-636d4. No cross-project leakage; data shown matches the testing project.                                                                                                                       | firebase, supabase(yynuu), main-backend(828), parsing-service                                                          |
| XCUT-2   | Deployment flavor build targets ztugw Supabase + 822 backend                                           | both     | crosscutting      | happy          | env/deployment.json present; build with --flavor deployment --dart-define-from-file=env/deployment.json.                                                                                       | 1. Build with FLAVOR=deployment and run. 2. Sign in and load queue, history, and a report. 3. Confirm endpoint hostnames via logs.                                                                                                                                                                | Supabase = ztugwhevemibdrzqafyw (client/prod); backend/activate/parse all on 822222628942; SUPABASE_ANON_KEY = sb_publishable_IRsM... Data reflects the ztugw project consistently.                                                                                                                                                                            | supabase(ztugw), main-backend(822), parsing-service, firebase                                                          |
| XCUT-9   | Cold start signed-out shows LoginScreen                                                                | both     | crosscutting      | happy          | No persisted Firebase session (fresh install / signed out).                                                                                                                                    | 1. Launch the app cold. 2. Observe initial route after splash.                                                                                                                                                                                                                                    | authStateChanges emits no user -> LoginScreen is shown. No data caches loaded; no Supabase reads attempted as a user.                                                                                                                                                                                                                                          | firebase                                                                                                               |
| XCUT-10  | Cold start with persisted session restores into AccountantShell                                        | both     | crosscutting      | happy          | Previously signed in (persisted session exists).                                                                                                                                               | 1. Launch the app cold. 2. Observe route after splash.                                                                                                                                                                                                                                            | authStateChanges emits the persisted user -> AccountantShell; caches (stock/customers/vendors) load; no re-login required.                                                                                                                                                                                                                                     | firebase, supabase                                                                                                     |
| XCUT-17  | Sign-in warms StockItems/Customers/Vendors caches                                                      | both     | crosscutting      | happy          | Signed out; valid account with stock/ledger data in the active Supabase project.                                                                                                               | 1. Sign in. 2. Open SALE stock picker, customer list, and vendor list shortly after.                                                                                                                                                                                                              | _wireSessionAndSentryUser fetches StockItemsCache/CustomersCache/VendorsCache on the uid transition; pickers/lists populate from cache without per-open round trips.                                                                                                                                                                                           | supabase, stock_items, ledgers                                                                                         |
| XCUT-18  | Sign-out clears all session caches                                                                     | both     | crosscutting      | happy          | Signed in with caches loaded.                                                                                                                                                                  | 1. Sign out. 2. (If possible) inspect that StockItems/Customers/Vendors caches are emptied. 3. Sign back in as a different user and confirm fresh data loads.                                                                                                                                     | On sign-out the three caches clear; no stale data from the previous user leaks into the next session.                                                                                                                                                                                                                                                          | supabase, stock_items, ledgers                                                                                         |
| XCUT-22  | Signed-in backend GET sends Bearer + x-api-key + x-request-id                                          | both     | crosscutting      | happy          | Signed in; build with non-empty DSN to inspect breadcrumbs, or proxy the request.                                                                                                              | 1. Trigger any ApiClient.get/post (e.g. open an image or report). 2. Capture the outgoing request headers via a proxy or Sentry breadcrumb.                                                                                                                                                       | Headers include Content-Type: application/json (get/post), x-api-key: Config.mrpApiKey, x-request-id: <hex-hex>, and Authorization: Bearer <getIdToken()>. Backend requireApiKey verifies the Bearer first and ignores x-api-key.                                                                                                                              | main-backend, firebase, sentry                                                                                         |
| XCUT-24  | Backend 401 surfaces as Exception('Unauthorized')                                                      | both     | crosscutting      | error          | Ability to force a 401 (expired token or wrong key).                                                                                                                                           | 1. Make an ApiClient.get/getRaw/getBytes call with an invalid/expired Bearer and a non-matching x-api-key. 2. Observe the thrown error and that a Sentry failure event is recorded.                                                                                                               | reportFailure(status:401) fires, then the call throws Exception('Unauthorized'). Caller sees an auth error rather than parsing a body.                                                                                                                                                                                                                         | main-backend, sentry                                                                                                   |
| XCUT-27  | Offline / network down reports failure then rethrows                                                   | both     | crosscutting      | error          | Signed in; ability to disable network.                                                                                                                                                         | 1. Turn off Wi-Fi/data. 2. Trigger an ApiClient call (report/image/activate).                                                                                                                                                                                                                     | The SocketException/timeout is caught, reportFailure(error) records a Sentry event tagged with the request_id and endpoint, then the error is rethrown to the caller. UI shows a network error, not silent success.                                                                                                                                            | main-backend, sentry                                                                                                   |
| XCUT-28  | No HTTP timeout: hung backend stalls the call indefinitely                                             | both     | crosscutting      | error          | Ability to point at a backend that accepts the connection but never responds (or sleep).                                                                                                       | 1. Configure a slow/hung backend endpoint. 2. Trigger an ApiClient.get and an activate POST. 3. Wait several minutes.                                                                                                                                                                             | DEFECT TO FLAG: Neither ApiClient nor the activate POST sets a timeout (default http.Client has none), so the call hangs indefinitely with no user feedback. (Contrast: parsing uploads in another domain do set 300s.)                                                                                                                                        | main-backend                                                                                                           |
| XCUT-30  | getBytes returns image bytes on 200 and throws on non-200                                              | both     | crosscutting      | happy          | A push_queue row with a stored invoice image; signed in.                                                                                                                                       | 1. Open the two-pane image viewer for a row that has an image. 2. Then attempt one for a row whose image is missing (force 404).                                                                                                                                                                  | 200 returns the JPEG bytes and the image renders; 404/500 calls reportFailure and throws 'Request failed' (401 -> Unauthorized). No bytes returned on error.                                                                                                                                                                                                   | main-backend, GET /api/sync/push-queue/:id/image/:page                                                                 |
| XCUT-32  | Firebase token expires mid-session -> Bearer routes 401, re-auth recovers                              | both     | crosscutting      | error          | Signed in; ability to invalidate/expire the ID token (e.g. revoke session or wait past expiry without refresh).                                                                                | 1. Force the current ID token to be invalid/expired. 2. Trigger a Bearer-authed call (image fetch). 3. Then let the app refresh the token (re-fetch getIdToken) and retry.                                                                                                                        | With an expired token the backend returns 401 ('Invalid or expired token') and the call throws Unauthorized. After a fresh getIdToken the retry succeeds. App should surface re-auth rather than crash.                                                                                                                                                        | main-backend, firebase                                                                                                 |
| XCUT-34  | Activate sends x-api-key only, NO Authorization Bearer                                                 | both     | crosscutting      | security       | Signed in; proxy to inspect the activate request.                                                                                                                                              | 1. Tap Push and capture the activate POST headers.                                                                                                                                                                                                                                                | Headers are manual {Content-Type, x-api-key: Config.activateApiKey, x-request-id} with NO Bearer. Backend requireApiKey matches x-api-key==API_KEY via timingSafeEqual. (Unlike ApiClient.get which adds Bearer.)                                                                                                                                              | main-backend, POST /api/sync/push-queue/activate                                                                       |
| XCUT-37  | Activate 409 when row is not in 'pending' status                                                       | both     | crosscutting      | error          | A push_queue row whose status is already push_now/pushed/failed.                                                                                                                               | 1. Open the detail sheet for a non-pending row (or change status server-side then push). 2. Tap Push.                                                                                                                                                                                             | Backend returns 409 'must be pending (current status: X)'; push is rejected; no double-activation. UI surfaces the failure.                                                                                                                                                                                                                                    | main-backend, push_queue                                                                                               |
| XCUT-38  | Activate 409 when job belongs to a different company                                                   | both     | crosscutting      | error          | Backend configured with company-identity check; a row whose company_id/guid/name mismatches the request.                                                                                       | 1. Trigger activate with a company identity that does not match the row. 2. Observe response.                                                                                                                                                                                                     | Backend returns 409 'job does not belong to requested company'; the wrong-company voucher is NOT activated, preventing a cross-company push.                                                                                                                                                                                                                   | main-backend, push_queue                                                                                               |
| XCUT-39  | Activate 401 on x-api-key mismatch                                                                     | both     | crosscutting      | security       | Build with an ACTIVATE_API_KEY that does not equal backend API_KEY.                                                                                                                            | 1. Tap Push. 2. Observe response.                                                                                                                                                                                                                                                                 | Backend requireApiKey returns 401 Unauthorized (length/timingSafeEqual mismatch). Activate fails cleanly; payload was still mutated (see rollback case).                                                                                                                                                                                                       | main-backend, POST /api/sync/push-queue/activate                                                                       |
| XCUT-41  | Signed-out client with shipped publishable key can call activate                                       | both     | crosscutting      | security       | Knowledge that ACTIVATE_API_KEY == shipped Supabase publishable key; ability to replay the activate request without a Firebase session.                                                        | 1. Capture a valid activate request. 2. Replay it with x-api-key but no Bearer (signed-out).                                                                                                                                                                                                      | DEFECT TO FLAG: activate succeeds on API-key only because the backend API_KEY equals the publishable key compiled into the app. Anyone holding the shipped key can activate pushes without authenticating. Security risk to record.                                                                                                                            | main-backend, POST /api/sync/push-queue/activate                                                                       |
| XCUT-42  | Activate terminal result arrives over realtime vd_status_<rowId>                                       | both     | crosscutting      | happy          | Successful activate (status push_now); the row is processed by the push pipeline.                                                                                                              | 1. Tap Push and stay on the detail sheet. 2. Wait for the Tally push to complete.                                                                                                                                                                                                                 | The vd_status_<rowId> channel (push_queue UPDATE filtered eq id) delivers the terminal result; sale shows a success/fail dialog when status becomes pushed/failed with tally_response. UI does not poll.                                                                                                                                                       | supabase, push_queue, realtime:vd_status_<rowId>                                                                       |
| XCUT-44  | Reorder report happy path returns CSV from client Supabase                                             | both     | crosscutting      | happy          | Signed in; client Supabase (SUPABASE_URL_Client) has a single 'K V ENTERPRISES' company with reorder data; backend API_KEY_CLIENT == mrpApiKey.                                                | 1. Open a report category with a valid reportId. 2. Wait for the CSV to load and render.                                                                                                                                                                                                          | getRaw('/api/sync/reorder-levels/<reportId>', {company_name:'K V ENTERPRISES', format:'csv'}) returns 200 CSV; report renders. Data is sourced from reorderSupabase, not the app's active SUPABASE_URL.                                                                                                                                                        | main-backend, GET /api/sync/reorder-levels/:reportKey, supabase(client SUPABASE_URL_Client)                            |
| XCUT-45  | Reorder route ignores Firebase Bearer and authenticates by API_KEY_CLIENT only                         | both     | crosscutting      | security       | Signed in (valid Bearer) but build where mrpApiKey != API_KEY_CLIENT.                                                                                                                          | 1. Open a report. 2. Capture the request headers and response.                                                                                                                                                                                                                                    | requireClientApiKey checks ONLY x-api-key vs API_KEY_CLIENT and ignores the Bearer; if mrpApiKey != API_KEY_CLIENT the route returns 401 even though the user is validly signed in. Confirms Bearer is not honored here.                                                                                                                                       | main-backend, GET /api/sync/reorder-levels/:reportKey                                                                  |
| XCUT-46  | mrpApiKey (client publishable key) is used for reorder in ALL flavors                                  | both     | crosscutting      | data-integrity | Testing-flavor build (app otherwise on yynuu).                                                                                                                                                 | 1. On a testing build, open a report. 2. Inspect which Supabase project the data comes from.                                                                                                                                                                                                      | Even on the testing build, mrpApiKey is the ztugw/client publishable key (sb_publishable_IRsM...) and reorder reads from the SEPARATE client Supabase. The report reflects the client project's inventory, inconsistent with the yynuu queue/history the user otherwise sees.                                                                                  | main-backend, supabase(client), GET /api/sync/reorder-levels/:reportKey                                                |
| XCUT-55  | Camera/scanner tab hidden on web in BOTH bottom-nav and side-rail                                      | web      | crosscutting      | platform       | Web build, signed in.                                                                                                                                                                          | 1. View AppBottomNav (narrow window) and AppSideNav (wide window). 2. Look for the camera tab (index 2).                                                                                                                                                                                          | The camera index-2 tab is hidden in both navs on web via !(kIsWeb && index==2); other tabs keep their index mapping and remain navigable. No 'scan' entry point on web.                                                                                                                                                                                        | web-browser                                                                                                            |
| XCUT-56  | Camera/scanner tab visible and functional on mobile                                                    | mobile   | crosscutting      | platform       | Android build, signed in.                                                                                                                                                                      | 1. View the bottom nav. 2. Tap the camera tab (index 2).                                                                                                                                                                                                                                          | On Android the camera tab is present and opens the ML Kit document scanner. (Document scanner is Android-only; web/iOS show 'only available on Android' if reached.)                                                                                                                                                                                           | android-mlkit                                                                                                          |
| XCUT-69  | Both clients on the SAME flavor see each other's realtime changes (phone + web)                        | both     | crosscutting      | concurrency    | Same flavor (same Supabase project), same user, signed in on Android and web at once.                                                                                                          | 1. Sign in on Android and on web (same flavor). 2. On Android, push a voucher (activate -> push_now). 3. Observe the web client's queue/status without manual refresh.                                                                                                                            | Because both point at the same Supabase project, realtime (e.g. push_queue / vd_status_<rowId>) propagates the change to the other client live. No env mismatch; data stays consistent across devices.                                                                                                                                                         | supabase, push_queue, realtime, firebase                                                                               |

### P2 — Important edge / error handling (207)

| ID | Title | Platform | Domain | Category | Preconditions | Steps | Expected | Integration points |
|----|-------|----------|--------|----------|---------------|-------|----------|--------------------|
| AUTH-8 | Edge: 11+ digit / pasted non-digit phone is NOT rejected and flows to Firebase | both | auth_session | edge | LoginScreen. Validation is length<10 only — no exact-10 check, no inputFormatters, no digit-strip. | 1. Type or paste a 12-digit number (or content with spaces/letters making length >= 10). 2. Tap 'Send OTP →'. | Client passes validation and calls verifyPhoneNumber with '+91<raw>'. Firebase returns verificationFailed (invalid number) and the error message is shown. Confirm no crash and the loading state clears. (Documents weak validation / wasted SMS-quota risk.) | firebase |
| AUTH-9 | Edge: +91 country code is hardcoded for every number | both | auth_session | edge | LoginScreen. | 1. Inspect the phone field — confirm there is no country-code picker and the '+91' prefix is a fixed, non-editable cell. 2. Enter a valid Indian 10-digit number and send OTP. | Number is always prefixed '+91'. A non-Indian number cannot be entered with a different country code (by design / limitation). | firebase |
| AUTH-10 | Edge: Verify with fewer than 6 digits returns silently | both | auth_session | edge | On OtpScreen with 0-5 boxes filled. | 1. Enter only 4 digits. 2. Tap 'Verify →' (or press Enter on web). | _doVerify returns immediately with no spinner, no error, no network call — no feedback shown. (Documents the silent no-op.) | firebase |
| AUTH-13 | Resend timer counts down 30s and reveals 'Resend code' | both | auth_session | happy | Just landed on OtpScreen. | 1. Observe the resend label immediately and over 30 seconds. | Shows 'resend in 0:30' counting down each second to 0:00, then switches to an underlined 'Resend code' link. | firebase |
| AUTH-15 | Resend the real code by returning to LoginScreen (forceResendingToken) | both | auth_session | happy | On OtpScreen after a prior Send OTP (resendToken captured). | 1. Tap the back arrow (or device Back) to return to LoginScreen. 2. Tap 'Send OTP →' again with the same number. | verifyPhoneNumber is called again with forceResendingToken set, Firebase treats it as an explicit resend, a new SMS arrives, and a fresh OtpScreen opens with a new verificationId. | firebase |
| AUTH-17 | Error: expired/invalid verificationId (session-expired) shows generic error | both | auth_session | error | On OtpScreen; leave it open long enough that the code/session expires (or otherwise force a non-invalid-code FirebaseAuthException such as session-expired). | 1. Enter any 6 digits after expiry. 2. Tap 'Verify →'. | Non-'invalid-verification-code' FirebaseAuthException maps to 'Verification failed. Try again'; spinner clears. (User must go back and resend.) | firebase |
| AUTH-20 | Error: Android auto-verify sign-in failure | mobile | auth_session | error | Android path where verificationCompleted fires but signInWithCredential fails (e.g. disabled account). | 1. Trigger Send OTP and let auto-verify fire while the account is in a failing state. | catch block sets _loading=false and error 'Auto-verification failed' on LoginScreen; no navigation to Success. | firebase |
| AUTH-21 | Double-tap guard on Send OTP and Verify | both | auth_session | edge | LoginScreen / OtpScreen. | 1. Tap 'Send OTP →' twice rapidly. 2. On OtpScreen, after entering 6 digits, tap 'Verify →' twice rapidly. | While _loading is true the handlers early-return (no duplicate verifyPhoneNumber / signInWithCredential), and the disabled/spinner state prevents a second submit. No duplicate SMS or double sign-in attempt. | firebase |
| AUTH-23 | Edge: after Android auto-verify, Login remains beneath Success (push vs pushReplacement) | mobile | auth_session | edge | Android auto-verify path (uses Navigator.push, not pushReplacement). | 1. Complete sign-in via auto-verify and let SuccessScreen finish. 2. Observe the back-stack behavior. | popUntil(isFirst) on Success completion still collapses the stack to the gate, so the user lands on the Shell and Back exits. Confirm there is no lingering LoginScreen reachable via Back. (Validates the push-not-pushReplacement difference is harmless.) | firebase |
| AUTH-28 | Auth re-emit of the same user does not reload caches (uid==lastUid guard) | both | auth_session | edge | Signed in; instrument or log cache .fetch() calls. | 1. Trigger an authStateChanges re-emission of the same user (e.g. token refresh / stream re-emit) without signing out. 2. Watch for redundant cache fetches. | Sentry scope is re-set, but because uid==lastUid the listener early-returns and the three caches are NOT re-fetched (no redundant Supabase load). | firebase, supabase |
| AUTH-29 | Sentry scope is tagged with uid on sign-in and nulled on sign-out | both | auth_session | happy | Sentry DSN configured (non-empty) for the test build. | 1. Sign in, then trigger a captured backend failure (e.g. force a non-2xx call) and check the Sentry event. 2. Sign out, then trigger another captured failure. | Events while signed in carry SentryUser(id=uid); after sign-out the user is null on events. No phone number/PII is attached (sendDefaultPii=false). | firebase |
| AUTH-32 | Stale token after account disable: getIdToken uses no forceRefresh | both | auth_session | security | Signed in with a cached, not-yet-expired ID token; disable/revoke the account in Firebase console. | 1. Immediately (before token expiry) trigger a backend call. 2. Then wait past the SDK token refresh window and trigger another call. | Because getIdToken() passes no forceRefresh, the first call may still send the stale (cached) token and could succeed/fail depending on backend revocation checks; once the SDK refreshes, verifyIdToken rejects -> 401 'Invalid or expired token'. App keeps showing protected UI (no forced logout). | firebase, main-backend, requireApiKey |
| AUTH-35 | SECURITY: Firebase identity is not propagated to Supabase (anon key + RLS) | both | auth_session | security | App initialized with Config.supabaseUrl + anon/publishable key; signed in. | 1. Capture a direct Supabase read (e.g. a cache fetch on stock_items/ledgers) and inspect the auth context. 2. Confirm no Firebase token is forwarded to Supabase. | Supabase calls use only the anon key under RLS — the Firebase JWT is never sent. The two trust contexts are independent; document that the embedded anon key + activate x-api-key alone suffice to read ledgers/stock and enqueue pushes. | supabase, stock_items, ledgers, firebase |
| AUTH-37 | Concurrency: signing out on one platform does not sign out the other | both | auth_session | concurrency | Signed in on both mobile and web with the same number. | 1. On mobile, tap Sign out. 2. On the web client (without refreshing), continue using the app, then refresh. | FirebaseAuth.signOut() is local to one device; the web session persists (Firebase persistence is per-client). Mobile returns to Login and clears its caches; web stays signed in until it independently signs out. | firebase, supabase |
| AUTH-38 | Concurrency: phone document-scan in flight while web session is open (auth not disrupted) | both | auth_session | concurrency | Signed in on both; mobile starts an Android document scan / upload. | 1. On mobile, start a scan (inserts scan_jobs row). 2. Keep the web client open and idle, then refresh web. 3. Confirm both clients remain authenticated throughout. | Auth/session is unaffected by the scan flow; web refresh still restores the session via authStateChanges, and the mobile session stays valid while the scan/parse runs. | firebase, supabase, scan_jobs |
| AUTH-39 | Platform: numeric keypad visibility flips at the 900px breakpoint | both | auth_session | platform | On OtpScreen; ability to resize the window (web/desktop) or compare phone vs tablet/web. | 1. View OtpScreen at width < 900 (mobile). 2. View/resize to width >= 900 (desktop/web). | Below 900px the on-screen _NumKeypad is rendered; at/above 900px it is hidden and entry relies on the physical-keyboard Focus handler. The transition is clean with no overflow. | firebase |
| AUTH-45 | SplashScreen shown during auth-stream waiting state | both | auth_session | happy | Cold start or web refresh with slow auth-state resolution. | 1. Launch/refresh on a slow network or throttled device and watch the first frame. | While snapshot.connectionState == waiting, SplashScreen (fade animation, 3 progress dots) is shown; it is replaced by Shell (hasData) or Login (no data) once the stream emits. No flash of the wrong screen. | firebase |
| AUTH-47 | Back navigation from OtpScreen preserves number and stack | both | auth_session | edge | On OtpScreen reached from LoginScreen via Send OTP. | 1. Tap the in-screen back arrow (or device Back on mobile). 2. Observe LoginScreen state. | Returns to LoginScreen (the route beneath). The phone field retains its controller value; _resendToken is retained so a subsequent Send OTP is treated as a resend. | firebase |
| AUTH-48 | Data-integrity: cache fetch on sign-in fails (Supabase down) does not break auth | both | auth_session | data-integrity | Signed-in attempt while Supabase is unreachable or returns an error (e.g. block the Supabase host). | 1. Block Supabase connectivity, then complete sign-in. 2. Observe the app state and any error reporting. | Auth still succeeds and the Shell renders; the three cache .fetch() calls fail and are reported via reportHandledError to Sentry, leaving caches empty (dependent screens show empty/loading) — but the user is NOT blocked from being signed in. No crash; foreground resync should recover when Supabase returns. | firebase, supabase, stock_items, ledgers |
| SCAN-2 | Open capture flow from camera screen + button (onCaptureRequested(null)) shows dialog | mobile | scan_parse | happy | Android phone, logged in, on Camera screen. | 1. Tap the small + button (bottom-right of the camera shutter row) 2. Observe | CaptureTypeDialog opens (preferredType is null so a type must be chosen). No badge change until a scan is sent. | camera_screen.onCaptureRequested, CaptureTypeDialog |
| SCAN-3 | Tapping the shutter with an active capture type skips the dialog | mobile | scan_parse | happy | Android phone; a capture type (e.g. Sale) already chosen so the 'Sale batch' chip shows on the Camera screen. | 1. Tap the large circular shutter button (which calls onCaptureRequested(activeCaptureType)) 2. Observe | No CaptureTypeDialog appears; the document scanner opens directly using the already-selected type. | camera_screen.onCaptureRequested, DocumentScanner |
| SCAN-4 | Dismiss the capture type dialog without choosing | both | scan_parse | edge | CaptureTypeDialog is open. | 1. Tap outside the dialog or press the system back button to dismiss it without selecting Sale or Purchase 2. Observe | Dialog returns null; flow returns early. No scanner opens, no scan_jobs row is inserted, no parse request is sent, the Processing badge is unchanged. | app_shell._showCaptureTypeDialog |
| SCAN-8 | iOS blocks scanning with the Android-only snackbar | mobile | scan_parse | platform | App running on iOS (if an iOS build is available). | 1. Open the capture flow and choose a type 2. Observe | Snackbar 'Document scanning is only available on Android.' shown; no scan, no scan_jobs row. (Gate is !kIsWeb && platform==android, so iOS is excluded.) | app_shell platform gate |
| SCAN-14 | Gallery import path produces a parseable PDF | mobile | scan_parse | edge | Android phone with an invoice image/PDF in the gallery. | 1. Open the scanner, use the gallery-import option (isGalleryImport:true) to import an existing image 2. Finish so a PDF is produced | Imported document is converted to a PDF, a CapturedShot is added and the enqueue/parse fires exactly as for a live camera scan. | DocumentScanner, scan_jobs table, parsing-service |
| SCAN-18 | startScan returns null but row was actually inserted server-side: badge sticks to TTL | mobile | scan_parse | data-integrity | Simulate the case where the scan_jobs INSERT succeeds server-side but the response is lost (network drop on the reply), so the client treats it as null. | 1. Trigger a scan under the lossy-reply condition 2. Observe the badge climb via realtime echo and whether it ever drains | The realtime INSERT echo climbs the badge on every client, but the parser received no job_id so it can never DELETE that row; the badge stays elevated until the 300s TTL sweep clears it (no push_queue/badge correlation for that scan). | scan_jobs table, scan_jobs_live, parsing-service |
| SCAN-21 | Parse upload non-2xx response is logged only, no user signal | mobile | scan_parse | error | Android phone; force the parsing service to return a non-2xx (e.g. point parse URL at an endpoint returning 500, or send an oversized/empty body causing 400 'Bad request size'). | 1. Perform a scan that yields a non-2xx parse reply 2. Observe app UI | Only a debugPrint('Parse send failed: HTTP <code>') is logged; NO error toast/dialog is shown to the user; the badge remains (driven by scan_jobs, not the HTTP reply) and drains only at the 300s TTL. | parsing-service, scan_uploader.sendScanToParser |
| SCAN-22 | Parse upload 300s timeout is caught and does not abort server-side parse | mobile | scan_parse | error | Android phone; induce a parse cold-start / slow response > 300s. | 1. Scan an invoice when the parsing service is cold 2. Wait past 300s and observe the app and later the queue | The client http POST times out after 300s, logged via debugPrint only; the server-side parse continues and may still insert a push_queue row and delete the scan_jobs row later. The badge was already swept at 300s, so the row appears with no live badge (confusing but not data loss). | parsing-service, scan_jobs table, push_queue |
| SCAN-28 | Exception in parser do_POST before delete leaves badge to TTL | mobile | scan_parse | error | Induce a parser-side exception after upload but before delete_scan_job (e.g. a corrupt PDF that crashes parsing). | 1. Scan the corrupt document 2. Observe badge and queue over 5 minutes | Parser returns an error payload; scan_jobs row is never deleted, no push_queue row created; badge sticks until the 300s TTL sweep clears it; no user-facing error. | parsing-service, scan_jobs table |
| SCAN-32 | Initial subscribe fetches only rows newer than 300s | both | scan_parse | edge | scan_jobs table contains both fresh (<300s) and stale (>300s) rows. | 1. Cold-start the app / recreate the shell so subscribe()/_refresh() runs 2. Observe the badge count | Only rows with created_at >= now-300s are loaded; stale rows are ignored so the badge reflects genuinely in-flight scans, not historical leftovers. | scan_jobs table, ScanJobsService._refresh |
| SCAN-33 | Per-tab badge split: sale scan only affects Sale tab | mobile | scan_parse | happy | Android phone on the Queue screen. | 1. Scan one Sale invoice 2. Switch between the Sale and Purchase tabs and read the Processing badge on each | countFor(sale) shows the +1 on the Sale tab only; the Purchase tab badge is unaffected (count is split by the row's type). | scan_jobs table, ScanJobsService.countFor |
| SCAN-35 | Multiple concurrent in-flight scans increment the count correctly | mobile | scan_parse | concurrency | Android phone. | 1. Scan three Sale invoices in quick succession (each parse still pending) 2. Read the Sale tab Processing count | The Sale Processing badge shows 3; as each parse completes (scan_jobs DELETE) the count decrements one at a time back toward 0. | scan_jobs table, scan_jobs_live |
| SCAN-37 | Two phones scanning simultaneously: every client sees the combined count | mobile | scan_parse | concurrency | Two Android phones on the same project, plus optionally a web session. | 1. Have both phones scan a Sale invoice at nearly the same time 2. Read the Sale Processing count on each device | All clients converge to a count of 2 (sum of both scan_jobs rows); each drains independently as its parse completes. | scan_jobs table, scan_jobs_live |
| SCAN-47 | Web refresh restores session and re-subscribes to the badge | web | scan_parse | platform | Web session with an in-flight scan visible (started from a phone). | 1. Refresh the browser tab (F5) 2. After the session restores via authStateChanges, observe the Processing badge | Session is restored on web refresh; ScanJobsService.subscribe()/_refresh() re-runs and the badge correctly reflects the current in-flight scan_jobs rows (initial fetch + resubscribe), not a stale or zeroed value. | Firebase Auth authStateChanges, scan_jobs table, scan_jobs_live |
| SCAN-50 | Cold-start parse exceeding 300s: row appears later without a live badge | both | scan_parse | edge | Parsing service is cold so the round-trip exceeds 300s for one invoice. | 1. Scan a valid invoice while the service is cold 2. Watch the badge cross the 300s mark (it sweeps to 0) 3. Keep the app open and wait for the parse to finish later | Badge is swept at 300s (no in-flight indicator), then the push_queue voucher row appears with no accompanying Processing badge. The voucher is not lost but the badge/reality desync is observable. | scan_jobs table, push_queue, parsing-service |
| SCAN-52 | scan_jobs RLS is fully permissive — multi-company project would leak badges | both | scan_parse | security | Knowledge of the scan_jobs RLS policy (FOR ALL USING(true) WITH CHECK(true)); single-project-per-client assumed. | 1. Review the policy and confirm there is no company_id scoping on scan_jobs 2. Reason about a hypothetical multi-company project | Document that isolation relies entirely on one-Supabase-project-per-client; if a project ever held multiple companies, every client would see every company's Processing badge. No per-row tenant scoping exists. | scan_jobs table RLS |
| SCAN-54 | Testing (yynuu) vs prod (ztugw): scans do not cross projects | both | scan_parse | data-integrity | One client pointed at testing Supabase (yynuu), another at prod (ztugw). | 1. Scan an invoice from the testing-configured client 2. Watch the prod-configured client's Processing badge and queue | The prod client's badge and queue are unaffected (scan_jobs/realtime are per-project); scans are isolated to the configured Supabase project. | scan_jobs table, scan_jobs_live |
| SCAN-57 | Phone goes offline after scan but before parse completes | mobile | scan_parse | error | Android phone; airplane mode toggled right after the scan_jobs INSERT succeeds and the parse POST is in flight. | 1. Scan an invoice 2. Immediately enable airplane mode so the parse upload cannot complete from this device 3. Re-enable connectivity after a minute and observe | The parse upload fails/times out (logged); since the parsing service may not have received the PDF, no push_queue row is created and the scan_jobs row drains at the 300s TTL. Foreground resync on reconnect corrects the badge. Invoice may be silently lost (no retry). | scan_jobs table, parsing-service, ScanJobsService.resync |
| SCAN-58 | Realtime DELETE drains the exact row by id (not all rows) | both | scan_parse | concurrency | Two in-flight scans of different types (one Sale, one Purchase). | 1. Let the Sale parse complete first (its scan_jobs row is deleted) while the Purchase scan is still in flight 2. Read both tab badges | Only the Sale badge decrements (removeById matches the deleted row's id); the Purchase badge remains at its in-flight count. A DELETE removes exactly one row by id. | scan_jobs_live, ScanJobsService._removeById |
| QUEUE-3 | Empty queue shows no rows and remains scroll/refresh-capable | both | queue_realtime | edge | push_queue has zero active rows for the signed-in tenant. | 1. Open the Queue screen. 2. Pull down to refresh. | Both Sale and Purchase tabs render empty (no day groups, no rows). The list still accepts the pull-to-refresh gesture (AlwaysScrollableScrollPhysics) and no error/crash occurs. | supabase:push_queue |
| QUEUE-4 | refresh() failure (network down) leaves queue stale and reports to Sentry, no crash | both | queue_realtime | error | Queue already showing rows. | 1. Disable network / block the Supabase host. 2. Pull to refresh on the Queue screen. 3. Re-enable network and pull to refresh again. | No crash and no user-facing error banner; the previously loaded rows stay visible (list left as-is). reportHandledError('supabase.push_queue.refresh') fires to Sentry. After network returns, refresh re-fetches and the list reconciles. | supabase:push_queue, sentry |
| QUEUE-6 | Realtime INSERT with status 'pushed' is ignored by the queue | both | queue_realtime | edge | Queue open. Ability to insert a push_queue row already at status='pushed' (e.g. via parsing/back-office). | 1. Insert a push_queue row with status='pushed' directly. 2. Observe the live queue. | isActiveStatus() guard rejects it; the row does NOT appear in the live queue (it belongs to History). It surfaces only on the History screen via history_push_queue. | supabase:push_queue, realtime:push_queue_live, realtime:history_push_queue |
| QUEUE-7 | Realtime INSERT has no dedup-by-id: redelivered/echoed INSERT can duplicate a row | both | queue_realtime | data-integrity | Queue open showing a row that was already loaded by refresh(). | 1. Trigger a duplicate INSERT echo for an id already present (e.g. force a reconnect/replay or insert a row whose id collides with one already loaded by refresh). 2. Observe the queue list. | DEFECT WATCH: the INSERT handler prepends without checking for an existing id (unlike scan_jobs._add), so the same row can appear twice until the next authoritative refresh() collapses it. Document whether a real duplicate row is observed. | supabase:push_queue, realtime:push_queue_live |
| QUEUE-11 | Realtime UPDATE omitting edit_state preserves the prior tag (column-cache lag) | both | queue_realtime | edge | A row already tagged 'under edit' or 'invoice edited' in the visible queue. | 1. Trigger an UPDATE to that row whose echo omits the edit_state column (e.g. a backend status flip). 2. Observe the row's tag chip. | Because the row already exists and the payload lacks the edit_state key, the handler re-applies the prior editState; the 'under edit'/'invoice edited' tag is NOT wiped. A subsequent refresh() confirms the tag from PostgREST authoritatively. | supabase:push_queue, realtime:push_queue_live |
| QUEUE-18 | View-then-close an untagged row writes nothing | both | queue_realtime | edge | An untagged pending row. | 1. Open the row, do not touch Edit, close the sheet. 2. Open it again, tap Edit, then immediately Revert back to original, close. | First close: touched=false so no write and no tag. Second close: edited-then-reverted on an untagged row clears the marker but, because the row was untagged when opened (marker==none), NO server write fires (no spurious update / no float-to-top). | supabase:push_queue |
| QUEUE-19 | Clearing a tag writes edit_state=NULL without bumping created_at | both | queue_realtime | edge | A row currently tagged 'under edit' or 'invoice edited'. | 1. Open the tagged row, tap Edit then Revert to original (back to untagged), close the sheet. 2. Inspect the row order and tag. | _persistEditState(none, bump:false) writes edit_state=NULL with NO created_at bump; the tag clears but the row keeps its original position rather than floating to top. | supabase:push_queue |
| QUEUE-20 | DATA INTEGRITY: edit float-to-top bump rewrites created_at, relocating an old voucher into Today | both | queue_realtime | data-integrity | A pending voucher whose created_at is several days old (shows under an older date group). | 1. Open the old voucher, tap Edit, save (or close mid-edit), close the sheet. 2. Observe which day group the voucher now appears under. | DEFECT WATCH: created_at is reused as both sort key and day-group key; the bump to now() relocates the voucher into 'Today', misrepresenting when the invoice was actually captured. Confirm the day label changes from the real capture date to Today. | supabase:push_queue |
| QUEUE-23 | Edit save throws (RLS/permission/network) -> Sentry + generic snackbar | both | queue_realtime | error | A pending row; ability to force the update to throw (block network or simulate RLS denial). | 1. Open the row, Edit, make a change, disable network, tap Save. | reportHandledError('supabase.push_queue.update') fires; snackbar 'Couldn't save changes. Please try again.' The user is told it failed (but note: a swallowed RLS denial would still show the same generic message — verify the row is unchanged server-side). | supabase:push_queue, sentry |
| QUEUE-28 | Purchase push closes the sheet silently with no result modal | both | queue_realtime | platform | Pending PURCHASE row; desktop reachable. | 1. Open the PURCHASE row, Push To Tally, confirm, wait for the result. | On terminal result the sheet closes and onPushed re-fetches; NO success/fail dialog is shown for purchase (sale-only). A pushed purchase drops to History; a failed one stays in the queue. | supabase:push_queue, realtime:vd_status_<rowId> |
| QUEUE-30 | Activate returns 404 (job not found) -> failure snackbar | both | queue_realtime | error | A queue row whose push_queue id has been deleted server-side after the sheet opened. | 1. Open the row, delete the underlying push_queue row from another client, then tap Push To Tally and confirm. | Backend returns 404 'Push queue job not found'. App shows snackbar 'Failed (404): Push queue job not found', clears the spinner (_isSubmitting=false), ApiClient.reportFailure logs the POST failure with x-request-id. | POST /api/sync/push-queue/activate, supabase:push_queue |
| QUEUE-32 | Activate lost-race returns 409 'could not be activated' (TOCTOU) | both | queue_realtime | concurrency | Two clients open the same pending row and push near-simultaneously. | 1. On client A and client B, tap Push To Tally and confirm at nearly the same time. | The backend update is guarded .eq(status,'pending'); the winner flips it to push_now, the loser's update matches 0 rows and returns 409 'Push queue job could not be activated'. Exactly one activation occurs (no double push). | POST /api/sync/push-queue/activate, supabase:push_queue |
| QUEUE-33 | Activate returns 500 (Supabase lookup/update error) -> failure snackbar | both | queue_realtime | error | Force a backend Supabase error on activate (e.g. simulated DB outage). | 1. Tap Push To Tally and confirm while the backend's Supabase is failing. | Backend returns 500 with an error body; app shows 'Failed (500): <body>', clears spinner, reports the failure to Sentry/ApiClient with the request id. | POST /api/sync/push-queue/activate |
| QUEUE-35 | Push while in edit mode prompts Save-before-push (no activate sent) | both | queue_realtime | edge | A pending row opened with unsaved edits (edit mode on). | 1. Open the row, tap Edit, change something (do not Save), tap Push To Tally. | _confirmAndActivate sees _isEditing and shows the _SaveBeforePushDialog; tapping OK just dismisses it (no save, no activate). The user must Save first, then Push follows the normal flow. No POST is sent while editing. | supabase:push_queue, POST /api/sync/push-queue/activate |
| QUEUE-36 | Push confirm dialog Cancel aborts the push | both | queue_realtime | happy | A pending row. | 1. Tap Push To Tally, then tap Cancel/No in the confirm dialog. | No voucher_payload write, no activate POST; the sheet stays open unchanged, button not spinning. | POST /api/sync/push-queue/activate |
| QUEUE-38 | Terminal result already arrived before activate POST returns is resolved immediately | both | queue_realtime | concurrency | A fast desktop that pushes and flips status to pushed before the activate HTTP response returns. | 1. Push a row where the vd_status UPDATE (pushed) lands before the POST resolves. | After 2xx, _activate checks _status=='pushed'/'failed' and calls _handlePushResult immediately with the captured status, rather than hanging in 'pushing…' waiting for an event already consumed. | POST /api/sync/push-queue/activate, realtime:vd_status_<rowId> |
| QUEUE-39 | Push-result hang: errors>=1 with created==null and status not 'failed' stays in 'pushing…' | both | queue_realtime | error | Ability to write a tally_response with errors>=1 but created==null while leaving status not 'failed'. | 1. Activate a row, then have the desktop write tally_response={errors:1} (no 'created' key) and NOT set status='failed'. 2. Observe the sheet. | DEFECT WATCH: _handlePushResult resolves only on success (pushed/created>=1) or failed (failed / created==0 && errors>=1). With created==null the failed branch is false, so the sheet stays stuck in 'pushing…' with the spinner indefinitely until manually closed. | realtime:vd_status_<rowId>, supabase:push_queue |
| QUEUE-41 | Narration-overwrite update in _activate is NOT guarded by status='pending' | both | queue_realtime | data-integrity | A row that the backend may be transitioning concurrently. | 1. Trigger _activate while the backend is mid-transition on the same row (race the pre-push voucher_payload write against a status flip). | DEFECT WATCH: unlike _persistEditsToSupabase, the _activate pre-push update is only .eq(id) with no .eq(status,'pending'); it can rewrite voucher_payload (narration='Replara AI') on a row already moving to push_now. Safe only because the app only invokes it from pending UI. Document any payload rewrite on a non-pending row. | supabase:push_queue, POST /api/sync/push-queue/activate |
| QUEUE-43 | Delete failure reports to Sentry and the row reappears on next refresh | both | queue_realtime | error | A pending row; force the delete to throw (block network or RLS deny). | 1. Open the row, disable network, tap Delete, confirm. 2. Re-enable network and pull to refresh. | Local list drops the row and the sheet closes optimistically; the delete throws -> reportHandledError('supabase.push_queue.delete') + snackbar 'Couldn't delete. Please try again.' Because the server row still exists, the next refresh/realtime re-adds it. | supabase:push_queue, sentry |
| QUEUE-46 | WEB/iOS: document scanning is unavailable and shows the not-supported message | web | queue_realtime | platform | Web session (or iOS), signed in. | 1. Tap the capture / add nav and choose a type. | A snackbar 'Document scanning is only available on Android.' is shown; no scan_jobs row is created. Web is a view/push-only consumer of the queue. | supabase:scan_jobs |
| QUEUE-48 | Scan badge: 300s max-age sweep expires a missed DELETE so the badge can't wedge | both | queue_realtime | edge | A scan_jobs row whose DELETE is never delivered (e.g. scanning device went offline before its invoice landed). | 1. Create a scan_jobs row, then prevent the matching DELETE/parse completion. 2. Wait >300 seconds and watch the badge. | The 5s periodic _sweep (and _fresh cutoff) drops the row past 300s; the Processing badge for that scan disappears even though no DELETE arrived. No permanently stuck badge. | supabase:scan_jobs, realtime:scan_jobs_live |
| QUEUE-49 | Scan badge undercount: a parse taking >300s drains the badge before the invoice lands | both | queue_realtime | edge | A slow/cold-start parse that exceeds the 300s window before inserting the push_queue row. | 1. Scan an invoice during a parsing cold start so the round trip exceeds 300s. 2. Watch the badge between ~300s and when the invoice finally lands. | DEFECT WATCH: the max-age sweep expires the scan_jobs row at 300s while the invoice is still in flight, so the Processing badge drops to 0 even though processing continues; the row then appears later via push_queue INSERT. Document the undercount window. | supabase:scan_jobs, parsing-service, supabase:push_queue |
| QUEUE-50 | startScan insert failure returns null: scan proceeds but no badge for that scan | mobile | queue_realtime | error | Android; force the scan_jobs insert to fail (block network at the moment of scan). | 1. Scan a document while the scan_jobs insert will fail. 2. Observe the Processing badge and whether the parse still runs. | startScan catches the error, reports to Sentry ('supabase.scan_jobs.startScan'), returns null; the scan/parse still proceeds (with a null job_id) but the Processing badge never reflects this scan. | supabase:scan_jobs, parsing-service, sentry |
| QUEUE-52 | Purchase duplicate detection flags only newer copies of a reference, oldest stays original | both | queue_realtime | edge | Three purchase rows: two share reference 'INV-100', one has 'INV-200'. | 1. Open the Purchase tab and inspect the duplicate tags. | Iterating oldest-first, the first 'INV-100' is the original (untagged); the newer 'INV-100' row is tagged 'Duplicate: Already Exists In Queue' and dimmed. 'INV-200' is never flagged. | supabase:push_queue |
| QUEUE-55 | Type classification defaults to Purchase when voucher_type is missing/blank | both | queue_realtime | data-integrity | A push_queue row whose voucher_payload.voucher_type is missing or does not contain 'SALE' though it is actually a sale. | 1. Load the queue and check which tab the row shows under. | DEFECT WATCH: rowToEntry defaults type to Purchase unless voucher_type contains 'SALE'; a malformed sale row appears under the Purchase tab and is missing from the Sale tab. | supabase:push_queue |
| QUEUE-58 | WEB: numeric item columns are scaled 1.4x to match web text scaling (no overflow) | web | queue_realtime | platform | Web session; a voucher with large numeric values (qty/rate/amount). | 1. Open a voucher detail sheet on web, enter Edit, view the items table numeric columns (Qty/Disc/Rate/List Price/Amount). | On web the numeric column widths are *1.4 (kIsWeb) so the 1.4x-scaled numbers don't overflow their cells; values render fully without clipping or wrapping. |  |
| QUEUE-59 | DESKTOP/WIDE WEB: voucher sheet opens ~90% width with side-by-side Image+Summary (no tab pill) | web | queue_realtime | platform | Web/desktop browser >=900px wide; a voucher row that has a stored scanned image (source_payload.scan.pages>0). | 1. Resize the browser to >=900px, open the voucher detail sheet. | The sheet uses ~90% of screen width and shows a two-pane layout: image (flex 5) on the left, summary (flex 6) on the right with the items table sized to the pane; the Image/Summary tab pill is hidden. | GET /api/sync/push-queue/:id/image/:page |
| QUEUE-62 | MOBILE: scan_jobs subscription survives shell teardown/recreate via the process-level singleton | mobile | queue_realtime | platform | Android; a scan is in flight (badge showing). | 1. Open the native ML Kit scanner (which can tear down/recreate the shell) and return. 2. Observe the Processing badge across that transition. | Because ScanJobsService is a process-level singleton (not a widget State), the scan_jobs subscription stays alive while the native scanner is in front; the badge count is preserved/correct on return rather than resetting. | supabase:scan_jobs, realtime:scan_jobs_live |
| QUEUE-63 | WEB: stored invoice image streams from GCS with no on-disk cache | web | queue_realtime | platform | Web session; a voucher row with stored scanned pages. | 1. Open the voucher detail sheet and view the Image pane; navigate pages if multiple. | Images are streamed via GET /api/sync/push-queue/:id/image/:page from GCS each time (no path_provider on web, so no local disk cache and no file-share option). Pages load over the network without relying on a cached file. | GET /api/sync/push-queue/:id/image/:page |
| QUEUE-64 | Stored invoice image 404 when storage disabled / page not found | both | queue_realtime | error | A row whose stored image is missing or storage is disabled, or an out-of-range page index. | 1. Open the voucher sheet Image view for a row with no stored page / request an invalid page. | The image endpoint returns 404 (missing/disabled) or 400 (bad id/page); the Image pane shows a placeholder/error state rather than crashing the sheet. | GET /api/sync/push-queue/:id/image/:page |
| QUEUE-65 | Backgrounded tab suspends the realtime socket; missed events recovered only on resume | web | queue_realtime | concurrency | Web queue open; another client will insert/update/delete rows while this tab is backgrounded. | 1. Switch to another browser tab for a while; have another client modify the queue (insert, push-to-history, delete). 2. Do NOT interact; then refocus the tab. | While backgrounded no events are applied (socket suspended, no replay). On refocus, AppLifecycleState.resumed triggers refresh()+resync() and the queue/badge catch up. Verify nothing is silently lost after resume. | realtime:push_queue_live, supabase:push_queue, supabase:scan_jobs |
| QUEUE-68 | SECURITY: activate sends no company identity, bypassing the backend company-ownership guard | both | queue_realtime | security | Understanding that each client uses a single per-tenant Supabase project under RLS (anon key). | 1. Capture the POST /api/sync/push-queue/activate request body during a push (network inspector / proxy). 2. Confirm the body contains only {job_id} with no company_id/company_guid/company_name. | The app sends only {job_id}, so the backend's hasCompanyIdentity branch is skipped and any pending row is flipped by id regardless of company. Safe ONLY under correct single-tenant RLS; a misconfigured RLS policy or a multi-company project (cf. reorder 409 duplicate-company incident) would allow cross-company activation. Record the missing company fields. | POST /api/sync/push-queue/activate, supabase:push_queue |
| QUEUE-70 | Activate rejects a wrong/absent API key (auth) | both | queue_realtime | security | Ability to call the endpoint with a tampered x-api-key. | 1. Send POST /api/sync/push-queue/activate with an incorrect x-api-key and a valid job_id. | requireApiKey (timingSafeEqual vs API_KEY) rejects the request (401/403) before any push_queue lookup; no row is activated. | POST /api/sync/push-queue/activate, main-backend |
| QUEUE-71 | Reopen an 'under edit' row restores in-progress items even after a reload wiped the local stash | both | queue_realtime | edge | A row tagged 'under edit' whose _savedEdits stash was lost (e.g. app/tab reloaded since tagging). | 1. Reload the app, open the 'under edit' row from the queue. | The sheet opens straight into edit mode (initialIsEditing) and _seedEditSession reseeds items from the persisted voucher_payload, so the items still show; if the in-session stash survived, those in-progress items take precedence. | supabase:push_queue |
| QUEUE-72 | Editing is blocked on a non-pending (push_now/pushed) row with a clear message | both | queue_realtime | edge | A row at status push_now or pushed opened in the sheet. | 1. Open the row and tap Edit. | _toggleEdit guard fires: snackbar 'This voucher is already queued for push and can't be edited.'; the sheet stays in view mode and no edit session starts. | supabase:push_queue |
| QUEUE-74 | History boundary: a just-pushed row with an omitted TOASTed payload is re-fetched (renders correctly, not 'Unknown') | both | queue_realtime | data-integrity | A row flips to 'pushed' via an UPDATE that omits the unchanged voucher_payload column. | 1. Push a voucher to completion and open the History screen as it lands. | history_push_queue detects the empty voucher_payload and re-fetches the full row before rendering, so the History card shows the real party/amount (NOT 'Unknown'). Contrast with the live queue, which does not re-fetch (see the queue corruption case). | realtime:history_push_queue, supabase:push_queue |
| QUEUE-75 | Revert after a saved edit writes the original payload back; revert without a save does not touch the server | both | queue_realtime | edge | A pending row. | 1. Case A: Open, Edit, change+Save, then tap Revert. 2. Case B: Open, Edit, change (no Save), tap Revert. | Case A (didSave true): _persistEditsToSupabase writes the original payload with snackbar 'Reverted to original.' Case B (didSave false): no server write (avoids re-touching status which would briefly drop the row from the status-filtered queue); only the 'Reverted to original.' snackbar shows. | supabase:push_queue |
| PUSH-3 | Open failed voucher shows error reason | both | voucher_edit_push | edge | A push_queue row with status='failed' and a non-empty error_message. | 1. Open the failed row from Queue (status=failed) or History 2. Inspect the header Push Details block | Status pill shows 'failed' with failure styling; Push Details shows the Tally error_message and timestamp. | supabase:push_queue |
| PUSH-5 | Email-sourced row shows Summary only (no Image tab) | both | voucher_edit_push | edge | A push_queue row whose source_payload.fetched_from == 'email'. | 1. Open the email-sourced row | Only the Summary tab/pane renders; no Image tab on phone and no left image pane on web/desktop. | supabase:push_queue |
| PUSH-6 | Row with stored scan images shows Image + Summary | both | voucher_edit_push | happy | A push_queue row with source_payload.scan.pages > 0 (stored GCS images), not email-sourced. | 1. Open the row 2. Confirm tab/pane layout | Phone shows an Image/Summary tab switcher; web/desktop (>=900px) shows image pane + summary pane side-by-side. | supabase:push_queue, main-backend:GET /api/sync/push-queue/:id/image/:page |
| PUSH-7 | Reopen an under-edit row re-seeds edit session and overlays stash | both | voucher_edit_push | edge | A row previously closed mid-edit (edit_state=under-edit, in-memory _savedEdits stash present for this session). | 1. From Queue, reopen the row tagged 'under edit' 2. Observe items and edit state | Sheet reopens already in editing mode with the previously-stashed editable items overlaid (not the original values); user can continue editing. | supabase:push_queue |
| PUSH-9 | Edit blocked while StockItemsCache is loading | both | voucher_edit_push | edge | Sign in fresh so StockItemsCache.isLoading is true (large ~13k catalog still paginating). | 1. Immediately open a pending row before caches finish 2. Tap Edit | Edit rejected with snackbar 'Loading of stock items in process'; sheet stays view-only until cache finishes. | supabase:stock_items |
| PUSH-10 | Tapping Add auto-enters edit mode | both | voucher_edit_push | happy | Pending row open, caches loaded. | 1. Tap Add (without first tapping Edit) | Sheet enters edit mode automatically then opens the stock item picker; if edit is blocked (status!=pending or cache loading) the corresponding snackbar shows and the picker does not open. | supabase:push_queue, supabase:voucher_items |
| PUSH-14 | Party picker blocked while customer/vendor cache loading | both | voucher_edit_push | edge | CustomersCache or VendorsCache still downloading. | 1. Enter edit mode quickly 2. Tap party dropdown before cache finishes | Picker does not open; snackbar 'customer/vendor list is still downloading' shown. | supabase:ledgers |
| PUSH-16 | Party-changed flag stays sticky across multiple saves | both | voucher_edit_push | edge | Sale pending row in edit mode. | 1. Change the party 2. Save 3. Change one item 4. Save again 5. Attempt Push without changing all items | _partyNameWasChanged remains true after the first change; push remains blocked until every item differs from original, even across intermediate saves. | supabase:push_queue |
| PUSH-19 | 'All items' toggle forces the full catalog in sale picker | both | voucher_edit_push | edge | Sale row in edit mode; party-aware list shown. | 1. Open Add picker 2. Toggle 'All items' | List switches from party-aware history to the full StockItemsCache catalog. | supabase:stock_items, supabase:voucher_items |
| PUSH-20 | Party-aware query error silently falls back to catalog | both | voucher_edit_push | error | Force a voucher_items query failure (e.g. revoke select via RLS in a test project, or network blip). | 1. Open Add on a sale row 2. Trigger the party-aware fetch failure | Picker silently falls back to StockItemsCache catalog (no crash, no error toast); a handled error may be reported to Sentry. | supabase:voucher_items, supabase:stock_items |
| PUSH-22 | Re-picking a high-confidence/historical item shows a warning first | both | voucher_edit_push | edge | Item flagged Purchase_Matching (green Historical) or Matching score>=90. | 1. In edit mode tap the item name to re-pick 2. Choose a different stock item | A confirm warning dialog appears before the item is overwritten; only on confirm are name/rate/discount/unit rewritten and amount recomputed. | supabase:stock_items |
| PUSH-26 | Direct Amount edit overrides recompute (no qty*rate recalc) | both | voucher_edit_push | edge | Row in edit mode. | 1. Type a value directly into the Amount cell of an item | Amount is set verbatim (no qty*rate recompute for that field); Charges recompute from the new amount. | supabase:push_queue |
| PUSH-29 | Delete an item line with confirmation | both | voucher_edit_push | happy | Row in edit mode with >=2 items. | 1. Tap an item row trash icon 2. Confirm 'Are you sure you want to delete <name>?' | Line removed; Charges rescale (GST Sale, tax, discount, total recompute). | supabase:push_queue |
| PUSH-32 | Save Supabase error reports handled error and offers retry | both | voucher_edit_push | error | Force a Supabase write failure (offline, or RLS deny in a test project). | 1. Edit, tap Save while the write will fail | reportHandledError('supabase.push_queue.update') fired to Sentry; a retry snackbar shown; sheet stays in edit mode so user can retry. | supabase:push_queue |
| PUSH-34 | Unexpected payload shape: edits silently not pushed | both | voucher_edit_push | data-integrity | A push_queue row whose item list lives under an unrecognized key. | 1. Edit an item, Save, then Push 2. Inspect what Tally receives | DEFECT WATCH: edits may be written to the wrong field and silently not reflected in the pushed voucher. Flag any case where saved edits do not reach the pushed payload. | supabase:push_queue |
| PUSH-36 | Revert with no prior Save is UI-only (no DB touch) | both | voucher_edit_push | edge | Row in edit mode with unsaved changes only. | 1. Make edits without Saving 2. Tap Revert | UI restores original and shows a snackbar; NO Supabase write occurs (so the status-filtered refresh doesn't briefly drop the row). | supabase:push_queue |
| PUSH-44 | Activate returns 404 job not found | both | voucher_edit_push | error | Row deleted server-side between open and push (job_id no longer exists). | 1. Tap Push, confirm | Snackbar 'Failed (404): Push queue job not found'; _isSubmitting cleared; reportFailure fired. | main-backend:POST /api/sync/push-queue/activate, supabase:push_queue |
| PUSH-45 | Activate returns 409 must be pending | both | voucher_edit_push | error | Row already non-pending server-side when activate runs. | 1. Tap Push, confirm | Snackbar shows 'Failed (409): ... must be pending before activation' (or 'could not be activated'); spinner clears; payload write not rolled back (see race case). | main-backend:POST /api/sync/push-queue/activate, supabase:push_queue |
| PUSH-46 | Activate returns 500 | both | voucher_edit_push | error | Backend Supabase lookup/activation failure (simulate 500). | 1. Tap Push, confirm | Snackbar 'Failed (500): ...'; reportFailure fired; sheet stays open, _isSubmitting cleared. | main-backend:POST /api/sync/push-queue/activate |
| PUSH-48 | Race: terminal status arrives before activate POST returns | both | voucher_edit_push | concurrency | Fast desktop that pushes and flips the row to pushed before the activate HTTP response lands. | 1. Tap Push; ensure the realtime pushed update arrives during the in-flight POST | On 2xx, since _status is already pushed/failed, _handlePushResult resolves immediately; _awaitingPushResult guard prevents the realtime callback from double-firing the result modal. | main-backend:POST /api/sync/push-queue/activate, realtime:vd_status_{rowId} |
| PUSH-52 | Realtime channel unsubscribed on sheet dispose | both | voucher_edit_push | edge | Open a row (subscribes vd_status_{rowId}). | 1. Open then close the sheet repeatedly 2. Monitor Supabase realtime channels / no leaked subscriptions | Each open subscribes one vd_status_{rowId} channel; dispose() unsubscribes it; no accumulation of stale channels across opens. | realtime:vd_status_{rowId} |
| PUSH-56 | View stored invoice pages streamed from GCS | both | voucher_edit_push | happy | Row with source_payload.scan.pages>0; backend invoiceStorageEnabled; signed in (Firebase token valid). | 1. Open the Image tab/pane 2. Scroll through pages 0..N-1 | Each page fetched via GET /api/sync/push-queue/{id}/image/{page} with Firebase Bearer + x-api-key(mrpApiKey); image/jpeg rendered; per-page Future cached so re-scroll doesn't refetch. | main-backend:GET /api/sync/push-queue/:id/image/:page, firebase |
| PUSH-57 | Image fetch 404 when storage disabled | both | voucher_edit_push | error | Backend invoiceStorageEnabled=false. | 1. Open the Image tab of a row that claims stored pages | Backend returns 404 'invoice image storage not configured'; the image tile shows a graceful error/placeholder, not a crash. | main-backend:GET /api/sync/push-queue/:id/image/:page |
| PUSH-58 | Image fetch 404 not found / 400 bad page | both | voucher_edit_push | error | Row whose claimed page index exceeds stored pages, or a malformed page param. | 1. Open Image tab and navigate to an out-of-range page | Backend returns 404 'image not found' (or 400 for non-int/negative); UI shows error placeholder for that page without breaking other pages. | main-backend:GET /api/sync/push-queue/:id/image/:page |
| PUSH-59 | Image fetch 401 unauthorized (expired/invalid Firebase token) | both | voucher_edit_push | security | Firebase token expired/invalid when image is requested. | 1. Let token expire, open Image tab | Backend returns 401; image tile shows error; app does not silently render a stale/other-tenant image. | main-backend:GET /api/sync/push-queue/:id/image/:page, firebase |
| PUSH-60 | Fresh-scan image renders from in-memory bytes (no network) | both | voucher_edit_push | edge | Sheet opened with widget.imageBytes set (immediately after a scan). | 1. Open the freshly-scanned voucher's Image view | Image.memory renders the in-memory bytes; no GET to the backend image endpoint is made. | main-backend:GET /api/sync/push-queue/:id/image/:page |
| PUSH-61 | Close mid-edit stashes edits, marks under-edit, bumps created_at | both | voucher_edit_push | edge | Row in edit mode with changes. | 1. Close the sheet while still editing (back/swipe down) 2. Observe the queue row | queue_screen stashes _savedEdits[id], marks the row 'under edit', and _persistEditState writes edit_state + created_at=now (errors swallowed); the row floats to top via newest-first sort. | supabase:push_queue |
| PUSH-62 | Close after Save marks edited and floats row to top | both | voucher_edit_push | edge | Row edited and Saved, then closed. | 1. Edit, Save, close the sheet 2. Observe ordering and the row tag | Row marked 'invoice-edited' (purple 'Item Edited') and created_at bumped so it sorts to the top; the local _editMarkers overlay re-applies the tag even if a realtime echo omits edit_state. | supabase:push_queue |
| PUSH-63 | Edited-then-reverted clears the edit marker correctly | both | voucher_edit_push | edge | A row previously tagged edited/under-edit. | 1. Open it, Revert to original, close | queue_screen clears the marker; a clearing write is issued only if the row was previously tagged; the row no longer shows an edit badge after refresh. | supabase:push_queue |
| PUSH-64 | Realtime echo omitting edit_state does not lose the local tag | both | voucher_edit_push | concurrency | A row tagged edited; trigger a push_queue UPDATE whose realtime payload omits edit_state (PostgREST column cache lag). | 1. Edit+save on client A 2. Cause an UPDATE echo lacking edit_state 3. Observe client A's row tag | Local _editMarkers overlay keeps the prior edit_state value; the row's edit badge does not flicker off; onRefresh later reconciles from PostgREST. | supabase:push_queue, realtime:vd_status_{rowId} |
| PUSH-67 | Delete on one client syncs via realtime DELETE to the other | both | voucher_edit_push | concurrency | Two clients showing the same pending row. | 1. Delete the row on client A 2. Observe client B's queue | Realtime DELETE removes the row from client B's queue without a manual refresh. | supabase:push_queue |
| PUSH-68 | Web/desktop two-pane image+summary at >=900px | web | voucher_edit_push | platform | Open a row with a stored image in a browser window >=900px wide. | 1. Open the voucher sheet 2. Observe layout | Image pane (flex 5) and summary pane (flex 6) render side-by-side via the scoped MediaQuery; no Image/Summary tab switcher. | main-backend:GET /api/sync/push-queue/:id/image/:page |
| PUSH-69 | Phone shows Image/Summary tab switcher | mobile | voucher_edit_push | platform | Android phone; row with an image. | 1. Open the voucher sheet 2. Observe layout | _TabPill Image/Summary switcher shown (single column); no side-by-side panes; switcher absent if no image exists. | main-backend:GET /api/sync/push-queue/:id/image/:page |
| PUSH-72 | Enter-to-move chain advances through fields (hardware keyboard) | web | voucher_edit_push | platform | Web/desktop with a physical keyboard; row in edit mode. | 1. Focus Invoice # 2. Press Enter repeatedly | Focus walks Invoice# -> Narration -> per-item Item/Qty/Rate/Disc/Amount -> Charges ledgers -> Discount -> Total in order (OrderedTraversalPolicy). | supabase:push_queue |
| PUSH-78 | Web has no on-disk image cache but still renders images | web | voucher_edit_push | platform | Web build (no path_provider). | 1. Open a voucher image, close, reopen 2. Check network calls | Images render in-memory (Image.memory / fetched bytes); on web there is no on-disk cache so a fresh open refetches from GCS, but rendering still works and no file-share/save options appear. | main-backend:GET /api/sync/push-queue/:id/image/:page |
| PUSH-79 | ML Kit document scanner is Android-only (entry to this domain) | mobile | voucher_edit_push | platform | Web/iOS build vs Android. | 1. On web/iOS attempt to scan a document that would create a queue row 2. On Android perform the scan | Web/iOS show 'only available on Android'; only Android can scan to create push_queue rows that later open this sheet. Confirm web users can still open/edit/push existing queue rows. | supabase:scan_jobs, supabase:push_queue |
| PUSH-80 | Stock picker dedup keeps the latest rate per item | both | voucher_edit_push | data-integrity | A party with the same stock_item_name sold at different rates on different dates. | 1. Open Add on a sale row for that party 2. Find the duplicated item in the list | Only one entry per stock_item_name appears, carrying the rate from the most recent parent voucher date (sorted desc, deduped). | supabase:voucher_items |
| PUSH-81 | Sale 'quantity' vs purchase 'qty' key handled on read and write | both | voucher_edit_push | edge | A sale row (uses 'quantity') and a purchase row (uses 'qty'). | 1. Edit a qty cell on each, Save, inspect persisted item maps | Reads accept both keys; the qty onChanged writes whichever key already exists on the item; newly Added items always write 'quantity'. No duplicate/zeroed qty fields introduced. | supabase:push_queue |
| PUSH-85 | Double-tap Push is debounced by _isSubmitting | both | voucher_edit_push | edge | Pending row ready to push. | 1. Tap Push To Tally twice rapidly (or press Enter on Total twice) | _isSubmitting/_submitFromTotal early-return prevents a second activate; only one voucher_payload write + one POST is issued. | main-backend:POST /api/sync/push-queue/activate, supabase:push_queue |
| HIST-2 | Empty history shows 'No records' | both | history_images | happy | Signed-in account/project whose RLS-visible push_queue has zero rows with status='pushed'. | 1. Open the History tab. 2. Wait for load to complete. | Centered muted text 'No records' is displayed; no cards, no error, no infinite spinner. | Supabase: push_queue select (status='pushed') |
| HIST-4 | Re-entering History re-fetches the list (foreground resync) | both | history_images | happy | History previously loaded with N rows; ability to push/insert one more pushed row while on another tab. | 1. Open History, note the rows. 2. Navigate away to another tab. 3. Insert/push one more row to status='pushed' (e.g. via Supabase console on yynuu). 4. Return to the History tab. | On re-entry initState re-runs _fetchAndSubscribe and the new pushed row appears (also covered live by realtime). No duplicate cards for existing rows. | Supabase: push_queue select (status='pushed'), Realtime: history_push_queue |
| HIST-6 | Filter tabs All / Sale / Purchase partition the list client-side | both | history_images | happy | History contains at least one row with voucher_type containing 'SALE' and one purchase row. | 1. Open History (tab 'All' selected). 2. Tap 'Sale' and review the list. 3. Tap 'Purchase' and review the list. 4. Tap 'All' again. | 'All' shows every pushed row. 'Sale' shows only rows whose voucher_type (uppercased) contains 'SALE'. 'Purchase' shows the rest. Switching tabs does not re-query the backend (purely client-side filter of in-memory rows). | Supabase: push_queue (in-memory filter) |
| HIST-8 | Typo'd voucher_type silently classifies as Purchase | both | history_images | edge | Pushed row with voucher_type='Slae' (typo) or 'Tax Invoice' (no 'SALE' substring) but is actually a sale. | 1. Open History and find the row. 2. Observe which tab it appears under and the sign of the amount. | Row is bucketed as Purchase (since uppercased type lacks 'SALE'), misrepresenting a sale. No warning shown. Document as misclassification edge. | Supabase: push_queue (voucher_payload.voucher_type) |
| HIST-9 | Date label and month grouping prefer pushed_at | both | history_images | happy | Pushed row with pushed_at and created_at in DIFFERENT months. | 1. Open History. 2. Find the row and read its date label and the month-section header it sits under. | Both the card date label and the month group are derived from pushed_at (converted to local time), not created_at. | Supabase: push_queue (pushed_at/created_at) |
| HIST-10 | pushed_at null falls back to created_at for date/grouping | both | history_images | edge | Pushed row with pushed_at=null and a valid created_at. | 1. Open History and find the row. 2. Verify its date label and month grouping. | Date label and month group come from created_at. Sort key is created_at epoch ms. | Supabase: push_queue (pushed_at/created_at) |
| HIST-11 | Both timestamps null/garbage -> bucketed under current month, sorted to top | both | history_images | edge | Pushed row with pushed_at=null and created_at=null or unparseable. | 1. Open History and locate the row. | Row is grouped under the CURRENT month and, because its sortKey is DateTime.now(), sorts to the very top of the list. No crash. Flag the misleading placement. | Supabase: push_queue (pushed_at/created_at) |
| HIST-14 | Empty ledger_entries renders amount 0.00 | both | history_images | edge | Pushed row with ledger_entries=[] or missing. | 1. Open History and find the row. | Card shows 0.00 (or +0.00 / -0.00 per type) without crashing. | Supabase: push_queue (voucher_payload.ledger_entries) |
| HIST-17 | Row removed from map between list render and tap -> sheet does not open | both | history_images | concurrency | Two clients. Card visible on client A. | 1. On client A keep History open with a card visible. 2. On client B (or Supabase console) flip that row's status away from 'pushed' (or delete it) so a realtime UPDATE/DELETE removes it from A's map. 3. Immediately tap the (now stale) card on client A. | _openSheet looks up the row, finds null, and early-returns — no sheet, no crash. The card is also removed from the list on the next frame. | Realtime: history_push_queue, Supabase: push_queue |
| HIST-20 | Image tab shown only when not email AND scan.pages > 0 | both | history_images | edge | Two pushed rows: one with scan.pages>0 (scanned), one with scan.pages=0. | 1. Open each row's detail sheet. 2. Check whether an Image tab/pane is offered. | Scanned row (pages>0) offers Image + Summary. pages=0 row offers Summary only (no Image tab, no image GET fired). In History imageBytes is always null, so the Image tab depends purely on scan.pages>0. | Supabase: push_queue (source_payload.scan.pages), Main backend: image endpoint |
| HIST-21 | Storage disabled (INVOICE_BUCKET unset) -> every page 404 -> 'Couldn't load this page.' | both | history_images | error | Backend running with invoiceStorageEnabled=false (INVOICE_BUCKET unset) but a row whose scan.pages>0. | 1. Open the row's Image view. 2. Wait for each page request to resolve. | Backend returns 404 {error:'Invoice image storage is not configured'} for every page. Each page slot shows the 120px tile 'Couldn't load this page.'. App reports the non-200 to Sentry (status only). No crash. | Main backend: GET /api/sync/push-queue/:id/image/:page (404), Sentry |
| HIST-22 | Image object missing for page -> 404 'Image not found' -> error tile | both | history_images | error | Pushed row with scan.pages=2 but only page-0.jpg stored in GCS (page-1 missing). | 1. Open the row's Image view and scroll to page 2. | Page 1 renders; Page 2 GET returns 404 (invoicePageExists false) and shows 'Couldn't load this page.' Other pages remain unaffected (independent futures). | Main backend: GET /api/sync/push-queue/:id/image/:page (404), GCS invoice bucket |
| HIST-24 | GCS read failure -> backend 500 -> error tile | both | history_images | error | Force a GCS read-stream/exists() error (e.g. revoke bucket permission or corrupt object) for a row with scan.pages>0. | 1. Open the row's Image view and wait. | Backend returns 500 ('Image read failed' or thrown message). App's getBytes throws 'Request failed: 500'; tile shows 'Couldn't load this page.'; non-200 reported to Sentry. No app crash. | Main backend: GET image (500), GCS invoice bucket, Sentry |
| HIST-26 | 401 on image GET surfaces as error tile when neither credential is valid | both | history_images | error | Force both a bad Firebase token AND a wrong x-api-key (e.g. misconfigured Config.mrpApiKey) for a scanned row. | 1. Open the row's Image view. | Backend returns 401; getBytes throws Exception('Unauthorized'); tile shows 'Couldn't load this page.'; reported to Sentry. Note the rest of the app (Supabase anon reads) is unaffected. | Main backend: requireApiKey (401), Firebase Auth, x-api-key, Sentry |
| HIST-27 | Network down during image fetch -> spinner then error tile | both | history_images | error | Scanned row with scan.pages>0; ability to drop network after opening the sheet. | 1. Open the row's Image view. 2. Immediately disable network so the GET cannot complete. 3. Wait. | Page slot shows the spinner then transitions to 'Couldn't load this page.' (getBytes throws). Summary tab (already-loaded payload) remains viewable. | Main backend: GET image, Firebase Auth |
| HIST-29 | Skew: scan.pages>0 recorded but bucket later unset -> Image tab shows, all pages 404 | both | history_images | edge | Row enqueued while storage enabled (scan.pages=2), then backend redeployed with INVOICE_BUCKET unset. | 1. Open the row's detail sheet. 2. Confirm the Image tab is offered, then open it. | Because pages>0, the Image tab is still shown, but every page GET returns 404 'Invoice image storage is not configured' -> all tiles show 'Couldn't load this page.' The app cannot distinguish this from a missing image. | Supabase: push_queue (scan.pages), Main backend: GET image (404) |
| HIST-30 | A failed page future is cached and does NOT auto-retry until the sheet reopens | both | history_images | edge | Scanned row; ability to make page-0 fail once (network blip) then restore. | 1. Open the Image view so page 0 errors -> 'Couldn't load this page.' 2. Restore network. 3. Scroll the page in/out of view within the SAME open sheet. 4. Close the sheet and reopen it from the same card. | Within the same sheet instance the failed future is cached, so the error tile persists (no auto-retry). Closing and reopening the sheet creates a new _StoredInvoiceImages instance and re-downloads — page now loads. Confirms in-memory-per-instance caching. | Main backend: GET image, App: _StoredInvoiceImages _pageFutures cache |
| HIST-32 | Email-sourced row (fetched_from='email') is Summary-only, no image call | both | history_images | edge | Pushed row with source_payload.fetched_from='email' (scan.pages may be 0 or absent). | 1. Open the row's detail sheet. 2. Check the available tabs and watch the network for any image GET. | _availableModes = [Summary] only; no Image tab; zero requests to the image endpoint. Summary renders party/items/charges from voucher_payload. | Supabase: push_queue (source_payload.fetched_from) |
| HIST-33 | Digital-PDF / pre-feature row (scan.pages=0) is Summary-only | both | history_images | edge | Pushed row that is non-scanned/native-PDF or pre-storage-feature with source_payload.scan.pages absent or 0. | 1. Open the detail sheet and check tabs. | Summary-only, no Image tab, no image GET. | Supabase: push_queue (source_payload.scan.pages) |
| HIST-35 | Realtime INSERT with non-pushed status is ignored by History | both | history_images | concurrency | History open. Ability to INSERT a push_queue row with status='pending' or 'push_now'. | 1. INSERT a row with status='pending'. 2. Observe History. | Row does NOT appear in History (insert handler returns unless status=='pushed'). | Realtime: history_push_queue (INSERT) |
| HIST-37 | Realtime DELETE removes the card from History | both | history_images | concurrency | History open with a visible pushed card; ability to DELETE that push_queue row. | 1. DELETE the row from push_queue. 2. Observe client A. | The card disappears from History (delete handler removes oldRecord.id). No crash. | Realtime: history_push_queue (DELETE), Supabase: push_queue |
| HIST-39 | TOAST re-fetch failure leaves a payload-less 'Unknown' / +0.00 card with no retry | both | history_images | data-integrity | Same TOAST-omission scenario, but force the PostgREST re-fetch to fail (network drop right after the realtime event, or RLS blip). | 1. Trigger a status-only UPDATE -> 'pushed' while voucher_payload is omitted. 2. Make the subsequent re-fetch fail. 3. Observe the card. | catch{} swallows the re-fetch error and the payload-less row is shown: party 'Unknown', amount +0.00, type defaults to Purchase. There is NO automatic retry — re-entering the screen (which re-selects full rows) is the only recovery. Flag as a real-transaction-hiding risk. | Realtime: history_push_queue (UPDATE), Supabase: push_queue re-fetch (fails) |
| HIST-44 | Web desktop (width >= 900): sheet capped at 90% width with two-pane image+summary | web | history_images | platform | Web build, browser window width >= 900. A scanned pushed row (scan.pages>0). | 1. Maximize the browser (>=900px) and open History. 2. Tap a scanned row. | The modal sheet is constrained to ~90% of the screen width and shows the two-pane layout: image pane on the left, summary on the right (no Image/Summary toggle on wide screens). 900 matches the bottom-nav->side-rail breakpoint. | Main backend: GET image, Supabase: push_queue |
| HIST-45 | Web narrow (width < 900): full-width sheet with Image/Summary toggle | web | history_images | platform | Web build, browser window narrowed to < 900px. A scanned pushed row. | 1. Resize the browser to < 900px and open History. 2. Tap a scanned row. 3. Toggle between Image and Summary. | Sheet uses full width (double.infinity cap) and presents the Image/Summary tab switcher (single-pane). Toggling shows the GCS image vs the summary. Resizing across 900 flips the layout. | Main backend: GET image, Supabase: push_queue |
| HIST-47 | Web has no on-disk image cache (no path_provider) but images still load from GCS | web | history_images | platform | Web build; scanned pushed row. | 1. Open the row's Image view on web and let it load. 2. Close and reopen the same sheet. | Image loads correctly from the backend/GCS (path_provider/disk cache is irrelevant for History since it is GCS-only). Reopening re-downloads (no persistent cache on web). Functionally identical to mobile. | Main backend: GET image, GCS invoice bucket |
| HIST-49 | Mobile: orphaned InvoiceImageStore is unused -> History images are always GCS, nothing on disk | mobile | history_images | platform | Android build. A freshly scanned invoice just pushed to 'pushed' but GCS upload still propagating, vs after propagation. | 1. On Android, scan and push an invoice. 2. Immediately open History and the just-pushed row's Image view (before GCS upload/propagation completes). 3. Then wait and reopen after upload completes. 4. Optionally inspect app documents directory for any cached .jpg for the rowId. | History never receives in-memory fresh-scan imageBytes, so the Image view depends entirely on GCS via the backend: it shows spinner/'Couldn't load this page.' until the parsing service has stored the pages, then renders. No .jpg is written to disk (InvoiceImageStore is dead code, never imported/called). Confirms the documented on-disk cache does NOT exist. | Main backend: GET image, GCS invoice bucket, Parsing service (Cloud Run) |
| HIST-54 | voucher_payload accepted as Map or JSON string; unparseable string degrades gracefully | both | history_images | edge | Three pushed rows: (a) voucher_payload as JSON object, (b) as a JSON STRING, (c) as an unparseable string ('not json'). | 1. Open History. 2. Inspect each row's card and open its detail sheet. | (a) and (b) parse and render party/amount/items correctly. (c) yields {} -> card shows party 'Unknown', amount 0.00, type defaults Purchase; sheet opens without crashing. | Supabase: push_queue (voucher_payload) |
| HIST-55 | Status flips pushed->failed via realtime while list is open -> silent removal | both | history_images | edge | History open with a visible pushed card; ability to UPDATE that row's status to 'failed'. | 1. Update the visible row's status from 'pushed' to 'failed'. 2. Observe the History list. | The card is silently removed (no longer status=='pushed') with no tombstone/animation/toast. It would reappear in the Queue (failed) but not in History. | Realtime: history_push_queue (UPDATE), Supabase: push_queue |
| RPT-7 | Mobile share: share icon exports the cached CSV file | mobile | reports | happy | Android. A report (e.g. R7) opened so /reports/R7.csv exists. Sheet open. | 1. In the open spreadsheet sheet, tap the Share icon. 2. Choose a target app (e.g. Files/Gmail/WhatsApp). | The OS share sheet opens with one attachment named for the cached file, subject 'full_portfolio_health.csv'. Sharing delivers the actual CSV file. If the cache file were somehow missing, share is a no-op (guarded by file.exists()). | local-disk cache (/reports/R7.csv), OS share |
| RPT-8 | Web has NO share button on the report sheet | web | reports | platform | Signed in on web. A report opened so the sheet is showing. | 1. Open any report (R1-R7) on web. 2. Inspect the spreadsheet sheet toolbar/footer for a share control. | No Share button is rendered (onShare=null when kIsWeb). All other controls (Close, search, Σ footer) work. No path_provider usage is attempted on web. | (web platform gate) |
| RPT-10 | Spreadsheet search filters rows by stock name and recomputes Σ live | both | reports | happy | A report with several rows is open in the sheet. | 1. Type a substring of a stock_item_name (row[0]) into the sheet's search box. 2. Observe the filtered rows and the Σ footer. | Only rows whose row[0] (lowercased) contains the query remain visible; the Σ recomputes over only the visible/filtered rows (it is NOT a server-authoritative total). Clearing the search restores all rows and the full Σ. | (client-side AppSpreadsheetSheet) |
| RPT-11 | Empty report (header-only CSV) renders headers with zero rows and ₹0 Σ | both | reports | edge | Backend can return a header-only CSV (e.g. a company with no qualifying stock for that report key). Use a test company or mock to force this. | 1. Open a report key that yields no data rows. 2. Observe the sheet. | CsvToListConverter parses 1 row -> columnLabels set, rows=[] (all-blank lines dropped), footerTrailing '0 rows', Σ shows ₹0 (formatCurrency(0)). The sheet shows headers and no data. No crash. | main-backend GET /api/sync/reorder-levels/:reportKey |
| RPT-12 | Truly empty body (zero bytes) yields a degenerate but non-crashing sheet | both | reports | edge | Backend/mock returns an empty 200 body for the report. | 1. Force a 200 response with an empty body (proxy/mock). 2. Tap the report. 3. Observe. | parsed.isEmpty branch: columnLabels=[], rows=[], columnWidths=[42], footerTrailing=''. The bottom sheet opens degenerate (no columns/rows) without throwing. App does not crash and the spinner clears. | main-backend GET /api/sync/reorder-levels/:reportKey |
| RPT-16 | en-IN grouped / quoted numeric cells parse correctly into the Σ | both | reports | edge | CSV contains values like "1,36,433" (quoted, grouped). | 1. Open a report whose closing_stock_amount values are en-IN grouped (e.g. 1,36,433). 2. Verify the Σ against the un-grouped sum. | _cellToNum strips all but [0-9.\-] so "1,36,433" -> 136433; non-numeric/blank cells -> 0. Σ equals the sum of the de-grouped integers. | main-backend GET /api/sync/reorder-levels/:reportKey |
| RPT-24 | 500 server/Supabase failure -> 'Request failed: 500' snackbar | both | reports | error | Force a server error: e.g. client Supabase unreachable, or an invalid format value reaching the handler (parseInventoryResponseFormat throws inside try -> caught as 500). | 1. Trigger a backend 500 (block reorderSupabase, or via a tampered request sending an invalid format). 2. Tap a report (or replay the tampered request). 3. Observe. | Backend catch returns 500 {error: message}. App shows 'Failed to load report: Exception: Request failed: 500'. Note a bad format yields 500 (not 400) by design. Sentry captures the failure with request_id/status/body. | main-backend GET /api/sync/reorder-levels/:reportKey, supabase: companies (reorderSupabase), Sentry |
| RPT-26 | Request timeout / slow backend: getRaw has no explicit timeout | both | reports | error | Backend that stalls the response (e.g. throttle the connection or a sleeping proxy). | 1. Tap a report row. 2. Observe the row spinner while the request hangs. 3. Eventually let the socket time out at the OS/http layer. | getRaw issues a plain client.get with no .timeout(); the row spinner stays until the underlying http client/OS aborts the socket, then the catch shows 'Failed to load report: <exception>'. There is no app-level timeout countdown for reports (unlike the 300s parsing call). Note as a UX gap. | main-backend GET /api/sync/reorder-levels/:reportKey, Sentry |
| RPT-27 | Malformed/non-CSV 200 body surfaces a parse-failure snackbar | both | reports | error | Backend/mock returns 200 with a body that breaks CsvToListConverter or a downstream parse. | 1. Force a 200 with malformed CSV (e.g. via proxy injecting garbage). 2. Tap the report. 3. Observe. | If parsing throws, the catch clears the spinner and shows 'Failed to load report: <exception>'. On mobile, note that the malformed body was already written to disk BEFORE parsing (write happens right after fetch on cache-miss) — so a subsequent re-tap will re-read the poisoned cache. Flag this caching-of-bad-body risk. | main-backend GET /api/sync/reorder-levels/:reportKey, local-disk cache |
| RPT-28 | Mobile poisoned-cache persistence: bad CSV stays cached after a parse error | mobile | reports | data-integrity | Android. Force a 200 with a body that writes to disk but fails to parse/render usefully (see previous case). | 1. Tap a report; the bad body is written to /reports/<id>.csv and rendering fails or shows garbage. 2. Restore a healthy backend. 3. Re-tap the same report. | Because the file already exists, the cache-hit branch reads the stale/poisoned disk file and never refetches — the report stays broken until app data/cache is cleared. Confirms there is no TTL/invalidation and bad bodies are persisted. | main-backend GET /api/sync/reorder-levels/:reportKey, local-disk cache |
| RPT-30 | Mobile vs Web divergence: same user, same time, different numbers | both | reports | data-integrity | Same account on Android (with a cached report) and on web. Underlying data changed since the mobile cache was written. | 1. On mobile open R7 (cached, stale). 2. On web open R7 (always refetch, fresh). 3. Compare the Σ and rows side by side. | Web shows fresh values; mobile shows the frozen cached snapshot. The identical report displays different totals on the two platforms simultaneously. Confirms web-refetch vs mobile-cache platform difference. | main-backend GET /api/sync/reorder-levels/:reportKey, local-disk cache |
| RPT-35 | Concurrency: phone runs a Tally sync while web has the report open | both | reports | concurrency | Web has a report sheet open. A new sync/data change occurs server-side (no realtime channel exists for this domain). | 1. Open R1 on web (sheet showing). 2. Trigger a sync/data change that alters R1's underlying data. 3. Watch the open web sheet, then close and re-open R1 on web. | The open sheet does NOT live-update (Reports use no realtime channels — it is a one-shot GET). Closing and re-tapping on web refetches and shows the new numbers. On mobile, re-tap would still show the stale cache. Confirms no realtime coupling for this domain. | main-backend GET /api/sync/reorder-levels/:reportKey |
| RPT-36 | Concurrency: rapid double-tap / tapping a second report while one is loading | both | reports | concurrency | Backend slightly slow so the spinner is visible. | 1. Tap report A; while its spinner is showing, tap report B. 2. Observe the spinners and which sheet opens. | _loadingCategoryKey is a single value, so the spinner moves to the most recently tapped row. Both in-flight requests complete; each opens its own bottom sheet in turn (modal stacking). No crash and the correct CSV maps to the correct sheet (each call captures its own category). Verify no cross-wiring of A's data into B's sheet. | main-backend GET /api/sync/reorder-levels/:reportKey |
| RPT-37 | Concurrency: report opened mid-sync returns a partially-updated snapshot | web | reports | concurrency | A sync is actively writing vouchers/stock to client Supabase while the report query runs. | 1. Begin a server-side sync that mutates vouchers/voucher_items/stock_items. 2. During the write window, open R7 on web. 3. Inspect the totals for internal consistency. | The report reflects whatever rows are committed at query time (paginated reads via fetchAllPages are not a single snapshot); numbers may be transiently inconsistent but the request still returns 200 and renders. No crash; re-open after sync completes yields the final figures. | main-backend GET /api/sync/reorder-levels/:reportKey, supabase: vouchers, supabase: voucher_items, supabase: stock_items |
| RPT-40 | WEB attempts no path_provider / disk access for reports | web | reports | platform | Web build with console open. | 1. Open a report on web. 2. Watch the browser console for MissingPluginException / path_provider errors. | On web the kIsWeb branch fetches directly and never calls _cacheFile/getApplicationDocumentsDirectory, so there is no path_provider invocation and no MissingPluginException. No on-disk caching occurs. | main-backend GET /api/sync/reorder-levels/:reportKey |
| RPT-43 | Hardcoded company/report params: no other tenant or report can be viewed | both | reports | edge | App as shipped (company_name='K V ENTERPRISES', reportIds R1-R7 baked in). | 1. Inspect the outgoing query for every report tap. 2. Confirm company_name is always 'K V ENTERPRISES' and format always 'csv'. 3. Confirm there is no UI to choose a different company or pass company_guid/company_id. | Every request hard-codes company_name='K V ENTERPRISES' and format=csv. No other tenant is reachable from the app; any DB-side name drift (case/whitespace) breaks all reports. Confirms the single-tenant hardcoding limitation. | main-backend GET /api/sync/reorder-levels/:reportKey |
| RPT-44 | User cannot distinguish 404 vs the two 409s vs 500 from the snackbar | both | reports | error | Ability to reproduce a 404, a duplicate-company 409, a not-synced 409, and a 500 (separately). | 1. Reproduce each error condition in turn and tap a report. 2. Record the exact snackbar text for each. | 404/409/500 all surface as 'Failed to load report: Exception: Request failed: <code>' and 401 as 'Exception: Unauthorized'. The two distinct 409 causes (duplicate company vs not-yet-synced) are indistinguishable to the user. Recommend differentiated messaging; meanwhile Sentry body carries the real reason. | main-backend GET /api/sync/reorder-levels/:reportKey, Sentry |
| RPT-46 | GST SALE 6-month / purchase 1-month windows shape the report correctly | both | reports | edge | Client Supabase has sales older than 6 months, sales within 6 months, purchases older than 1 month, and purchases within 1 month, plus some is_cancelled=true vouchers. | 1. Open R1/R4 (which use sale_6m and purchase_1m windows). 2. Cross-check that only GST SALE vouchers in the last 6 calendar months and purchases in the last 1 calendar month (excluding cancelled and order-type) contributed. | Server-side: sales filtered to voucher_type='GST SALE', is_cancelled=false, date within last 6 calendar months; purchases to is_cancelled=false within last 1 calendar month and isPurchaseVoucherType (excludes 'order'). Cancelled/out-of-window vouchers are excluded from the report aggregates. Verify the rendered figures reflect those windows. | main-backend GET /api/sync/reorder-levels/:reportKey, supabase: vouchers, supabase: voucher_items |
| MAST-3 | uid guard suppresses redundant cache reload on token refresh / same-user re-emit | both | masters_caches | edge | Signed in. Ability to observe network (web devtools Network tab or proxy). | 1. Sign in and let caches load; note the stock_items/ledgers requests in the network log. 2. Leave the app idle long enough for a Firebase token refresh, or trigger any authStateChanges re-emit for the same user. 3. Inspect the network log for new stock_items / ledgers fetches. | No second round of stock_items/ledgers fetches fires for the same uid (lastUid guard); caches stay populated with no flicker. | Firebase Auth (authStateChanges), Supabase: stock_items, Supabase: ledgers |
| MAST-7 | Stock pagination boundary at an exact 1000-row multiple | both | masters_caches | edge | stock_items row count is an exact multiple of 1000 (e.g. exactly 2000) in a test project. | 1. Sign in and let stock load. 2. Open the 'All items' stock picker and read 'count :'. | All rows are present (e.g. 2000) and the fetch loop terminates cleanly without hanging or duplicating the last page (a full final page triggers one extra empty/short page that ends the loop). | Supabase: stock_items (.range pagination) |
| MAST-9 | Network down during stock fetch -> 'download failed' snackbar shown | both | masters_caches | error | Ability to disable network right at sign-in (airplane mode / block Supabase host). | 1. Disable network, then sign in (or trigger authStateChanges with network off). 2. Observe the app's scaffold for a snackbar. 3. Re-enable network and sign out/in to recover. | StockItemsCache.fetch throws, reportHandledError('supabase.stock_items.fetch') fires, and a 'download failed ...' snackbar is shown (StockItemsCache has scaffoldKey wired). Pickers are empty until a successful re-fetch. | Supabase: stock_items, Sentry (reportHandledError) |
| MAST-10 | Partial stock pagination failure mid-loop leaves cache empty (no partial commit) | both | masters_caches | error | Ability to fail the 2nd/3rd .range() page (e.g. kill network after first page returns). | 1. Sign in; allow the first 1000-row page to load, then drop the network so a subsequent page request fails. 2. Open the 'All items' stock picker. | Because items is only assigned after the full loop succeeds, a mid-loop failure leaves the cache at its prior value (empty on first load); a 'download failed' snackbar appears. No partial 1000-row set is silently presented as complete. | Supabase: stock_items (.range pagination), Sentry |
| MAST-19 | Party name is NOT editable when voucher is not in edit mode or not pending | both | masters_caches | edge | A voucher in view mode (not editing) and/or a non-pending (push_now/failed/pushed) voucher. | 1. Open a pending voucher WITHOUT tapping Edit and tap the party name. 2. Open a non-pending voucher, tap Edit if allowed, and try tapping the party name. | In view mode the party name is plain text (no dropdown caret, tap does nothing). Edit affordance only appears when _isEditing && !_loading, so the party picker cannot be opened outside an editable pending voucher. | UI state (_isEditing/_loading) |
| MAST-22 | Stock picker keyboard navigation (Down/Up/Enter) selects highlighted row | web | masters_caches | platform | On web/desktop with a physical keyboard. Stock picker open with a multi-row filtered list. | 1. Open the stock picker; type a query that returns several rows. 2. Press ArrowDown several times and watch the highlight + auto-scroll (ensureVisible). 3. Press ArrowUp once, then press Enter. | Highlight moves and the row scrolls into view (alignment 0.5); the search field keeps focus; Enter pops the currently highlighted item and adds it as a line. Arrow/Enter keys do not move the text cursor or submit the field. | UI keyboard handler (_onSearchKey, Scrollable.ensureVisible) |
| MAST-24 | Stock picker search matches by Part No. (partCode) as well as name | both | masters_caches | happy | Catalog has items with non-empty part_code values. | 1. Open the 'All items' stock picker. 2. Type a substring of a known Part No. (not present in the item name). | Items whose partCode contains the query are returned (filter checks name OR partCode, case-insensitive); the partCode is shown under the item name. | Supabase: stock_items (part_code) |
| MAST-26 | Party-history query error silently falls back to catalog rates (no snackbar, no Sentry) | both | masters_caches | error | Ability to make the voucher_items inner-join query throw (drop network just as the picker opens, or break the voucher_items->vouchers FK in a test project). | 1. Open a pending sale voucher with a party, Edit, Add Item to trigger _fetchPartyItems. 2. Force the query to fail. 3. Observe the list contents and whether any error surfaced. | DEFECT TO CONFIRM: the bare catch(_) swallows the error and the picker silently shows the full catalog cache instead of party history; no snackbar and no Sentry event. User may unknowingly add catalog rates instead of party-specific rates. | Supabase: voucher_items + vouchers, Supabase: stock_items (fallback) |
| MAST-27 | Party with > 4000 voucher_items may surface a stale 'latest' rate (no server-side order) | both | masters_caches | data-integrity | A high-volume party with > 4000 voucher_items rows where the genuinely most-recent rate for some item differs from older ones. | 1. Open a pending voucher for that party, Edit, Add Item (party-history view). 2. Pick an item and compare its shown rate against the true most-recent voucher rate for that party in the database. | DEFECT TO CONFIRM: because the query uses .limit(4000) with no server-side .order(), the returned slice is arbitrary; the client-side date-desc dedup can miss the truly latest voucher and present an older rate, risking a wrong price pushed to Tally. | Supabase: voucher_items (.limit 4000, no .order) |
| MAST-32 | partCode backfill on party-history items shows/searches catalog Part No. | both | masters_caches | happy | A party-history item that also exists in the catalog with a non-empty part_code. | 1. Open the party-history stock picker for that party. 2. Locate the item and confirm its Part No. is displayed. 3. Search by that Part No. | Although voucher_items has no part_code, partCodeFor() pulls it from the catalog cache so the Part No. is shown and is searchable. | Supabase: voucher_items, Supabase: stock_items (partCodeFor) |
| MAST-35 | Concurrency: two clients load masters simultaneously without interference | both | masters_caches | concurrency | Same account signed in on phone and web (or two web tabs). | 1. Sign in on both clients at roughly the same time. 2. Let both finish loading and open the stock/party pickers on each. | Each client loads its own independent in-memory caches from Supabase; no shared state or race causes one client's picker to be empty or to show the other's filter state. | Supabase: stock_items, Supabase: ledgers |
| MAST-38 | Web has no on-disk master cache; mobile keeps in-memory caches across foreground | web | masters_caches | platform | Web build (no path_provider). | 1. Sign in on web and load caches. 2. Refresh the page (F5) and observe network for a fresh stock_items/ledgers fetch. 3. Separately on mobile, background and foreground the app and check whether masters refetch. | Web has no on-disk caching, so a refresh re-emits authStateChanges and triggers a full refetch; mobile keeps the same in-memory caches across background/foreground (no refetch) until sign-out or an auth re-emit. Masters are never persisted to disk on either platform. | Supabase: stock_items, Supabase: ledgers, Firebase Auth (authStateChanges) |
| MAST-41 | Abandoned RPCs are not called; party history uses the voucher_items inner-join query | both | masters_caches | edge | Network capture available (web devtools / proxy). | 1. Open a pending voucher, Edit, Add Item to trigger party-history load. 2. Inspect outgoing Supabase calls. | The app issues a voucher_items select with a vouchers!inner embed (filtered by party_name + voucher_type ilike), and does NOT call get_sale_items_for_party / get_purchase_items_for_party RPCs (those are abandoned because the sync leaves voucher_items.party_name null). | Supabase: voucher_items + vouchers (inner join) |
| MAST-42 | Customer picker case-insensitive contains filter narrows results per keystroke | both | masters_caches | happy | CustomersCache loaded with several similarly-named customers. | 1. Open the customer picker on a pending sale voucher. 2. Type a lowercase substring that matches part of an uppercase customer name. | Filtering is case-insensitive name.contains; matching rows shrink as the query lengthens; clearing the field (suffix clear button) restores the full list. | Supabase: ledgers (Sundry Debtors) |
| MAST-46 | Sign out mid-fetch then immediately sign in does not leave caches stuck loading | both | masters_caches | concurrency | Slow network so a fetch is in flight when sign-out occurs. | 1. Sign in; while caches are loading (isLoading=true), sign out. 2. Immediately sign back in. 3. Open pickers and observe whether they load or appear permanently stuck. | clear() resets isLoading=false and a new fetch starts on the re-emitted sign-in; the in-flight prior fetch's finally also resets isLoading. Pickers ultimately populate and are not stuck behind a loading guard. (Watch for any race leaving stale items briefly.) | Firebase Auth (authStateChanges), Supabase: stock_items / ledgers |
| MAST-47 | Mobile: no master refetch on app foreground (masters only refresh on auth re-emit) | mobile | masters_caches | platform | Android, signed in, caches loaded. A desktop sync adds a master while app is backgrounded. | 1. Background the app for a while; add a new stock item via desktop sync. 2. Foreground the app (without sign-out) and open the stock 'All items' picker. | The new item is NOT present on foreground because masters have no foreground-resync (that mechanism is scan_jobs only); masters refresh only when authStateChanges re-emits a different uid (sign-out/in). Confirms staleness behavior on mobile. | Supabase: stock_items, Firebase Auth |
| XCUT-3 | Bare build with no dart-define falls back to testing defaults | both | crosscutting | edge | Build the app WITHOUT --dart-define-from-file (no env file). | 1. Run flutter build/run with no --dart-define args. 2. Launch and inspect resolved Config values (log Config.supabaseUrl, backendBaseUrl, sentryEnvironment). 3. Sign in and load data. | All Config.* resolve to the hard-wired testing defaults (yynuu Supabase, 828 backend, FLAVOR='testing', sentryDsn=''). App still runs; Sentry is a no-op because default DSN is empty. | supabase(yynuu), main-backend(828), firebase, sentry |
| XCUT-6 | Web build selects web FirebaseOptions per flavor (ignores google-services.json) | web | crosscutting | platform | Web build for each flavor. | 1. Build web with FLAVOR=testing and FLAVOR=deployment separately. 2. Sign in on each and observe Firebase project used (authDomain in network calls). | Web uses the _xxxWeb FirebaseOptions chosen by FLAVOR+kIsWeb; testing build hits testing Firebase, deployment hits deployment Firebase. No google-services.json involvement on web. | firebase |
| XCUT-7 | All three flavors share one Sentry DSN, differentiated only by environment tag | both | crosscutting | edge | Sentry project access; builds for testing and deployment. | 1. Run a testing build and trigger a handled error (e.g. force a backend 500). 2. Run a deployment build and trigger a similar error. 3. In Sentry, filter by the environment tag. | Both events land in the same Sentry project (DSN identical across env files); they are distinguishable only by environment=testing vs environment=deployment. No per-env project isolation. | sentry |
| XCUT-8 | Web blocks http (mixed-content) targets; all shipped URLs are https | web | crosscutting | security | Web build served over https. | 1. Confirm all flavor URLs (supabase/backend/parse) are https (they are). 2. Temporarily configure a build with an http BACKEND_BASE_URL and attempt a report fetch on web. 3. Restore https and retry. | With https URLs all requests succeed. With an http target, the browser blocks the request as mixed content (web only); native mobile would still attempt it. Confirms web-specific transport constraint. | main-backend, web-browser |
| XCUT-14 | Phone number shorter/longer than 10 digits is rejected before send | both | crosscutting | edge | On LoginScreen. | 1. Enter a 9-digit number and attempt continue. 2. Enter an 11-digit number and attempt continue. 3. Enter exactly 10 digits. | 9- and 11-digit inputs are blocked by the 10-digit guard (no verifyPhoneNumber call / SMS sent); only the 10-digit input proceeds. | firebase |
| XCUT-15 | Wrong OTP code shows error and does not sign in | both | crosscutting | error | Reached OtpScreen with a real verificationId. | 1. Enter an incorrect 6-digit OTP. 2. Submit. | Firebase returns an invalid-code error; an error message is shown; user remains on OtpScreen, not signed in. | firebase |
| XCUT-16 | Android SMS auto-retrieval auto-signs in (verificationCompleted) | mobile | crosscutting | platform | Android device that supports SMS auto-retrieval; valid number. | 1. Initiate OTP login on Android. 2. Wait for the SMS to arrive without manually typing the code. | verificationCompleted fires and auto-signs the user in without manual OTP entry (Android-only convenience). On web/iOS this path does not trigger and manual entry is required. | firebase |
| XCUT-19 | Token refresh (same uid) does not redundantly reload caches | both | crosscutting | edge | Signed in; leave app open long enough for an ID-token refresh. | 1. Stay signed in and idle until Firebase refreshes the ID token (authStateChanges may re-emit same uid). 2. Observe whether caches reload. | The uid guard detects the unchanged uid and skips a redundant cache reload; no flicker / duplicate fetch storms on token refresh. | firebase, supabase |
| XCUT-20 | Sentry scope tagged with SentryUser(uid) on sign-in | both | crosscutting | happy | Build with a non-empty SENTRY_DSN; signed out. | 1. Sign in. 2. Trigger a handled error (e.g. force a backend failure). 3. Inspect the Sentry event's user field. | The event carries SentryUser(id: uid) set during _wireSessionAndSentryUser, allowing per-user grouping. | sentry, firebase |
| XCUT-23 | Signed-out backend call falls back to x-api-key path | both | crosscutting | edge | Signed out but a code path that still calls ApiClient (e.g. activate is API-key only). | 1. With no Firebase user, invoke an ApiClient call that does not require a Bearer (activate). 2. Capture headers. | No Authorization header is attached (token null); backend requireApiKey/requireClientApiKey matches x-api-key against API_KEY/API_KEY_CLIENT. Call succeeds purely on the shipped publishable key. | main-backend |
| XCUT-26 | Non-JSON error body in get/post throws FormatException | both | crosscutting | error | Force a backend response with a non-JSON body (e.g. an HTML 500 page) on a get/post route. | 1. Trigger a get/post that returns HTTP 500 with a plain-text/HTML body. 2. Observe behavior after reportFailure. | jsonDecode throws FormatException (since status != 401 and not parseable); caller gets a confusing FormatException instead of a clean error. Confirms inconsistent error handling. | main-backend |
| XCUT-29 | getRaw strips Content-Type and throws on any non-200 | both | crosscutting | error | Reorder/report path uses getRaw. | 1. Trigger getRaw against an endpoint returning 200 then one returning 500. 2. Capture request headers and the thrown error. | Request omits Content-Type (kept x-api-key + Bearer). 200 returns the raw body; any non-200 calls reportFailure and throws (401 -> Unauthorized, else 'Request failed: <code>'). No silent decode. | main-backend |
| XCUT-31 | x-request-id is sent but never echoed/used by backend (correlation blind spot) | both | crosscutting | edge | Backend deployed as-is (no Sentry, no x-request-id middleware). | 1. Trigger any ApiClient call and note the x-request-id sent. 2. Inspect the backend response headers and backend logs/Sentry for that id. | DEFECT TO FLAG: backend has no Sentry dependency and no x-request-id middleware; it never reads or echoes the header. The advertised app<->backend request_id correlation does not exist server-side. Response carries no x-request-id back. | main-backend, sentry |
| XCUT-35 | Activate 400 when job_id missing | both | crosscutting | error | Ability to send activate with an empty/missing job_id (e.g. corrupted row id). | 1. Trigger activate without job_id in the body. | Backend returns 400 'job_id required'; UI reports failure via ApiClient.reportFailure and does not enter awaitingPushResult. | main-backend, POST /api/sync/push-queue/activate |
| XCUT-36 | Activate 404 when job not found | both | crosscutting | error | A job_id that does not exist in push_queue. | 1. Tap Push on a row that was deleted server-side just before (or send a bogus job_id). | Backend returns 404 'job not found'; activate POST is reported to Sentry; UI shows push failure, no status change. | main-backend, push_queue |
| XCUT-43 | Mismatched-length API key yields clean 401, not a crash | both | crosscutting | security | Build with an x-api-key whose byte length differs from the backend's expected key. | 1. Send activate (or reorder) with a deliberately shorter/longer key. 2. Observe response. | requireApiKey/requireClientApiKey check buffer length before timingSafeEqual, so a length mismatch returns a clean 401 Unauthorized instead of throwing (timingSafeEqual would otherwise throw on unequal lengths). | main-backend |
| XCUT-48 | Reorder 404 when company not found | both | crosscutting | error | Client Supabase has NO 'K V ENTERPRISES' row. | 1. Open a report. 2. Observe the result. | Backend returns 404 'company not found'; getRaw throws 'Request failed: 404' and reports to Sentry; the report screen shows a load error. | main-backend, supabase(client) |
| XCUT-49 | Reorder 400 on invalid reportKey | both | crosscutting | error | Ability to request an invalid/unknown reportKey. | 1. Trigger getRaw with a malformed reportKey (e.g. unknown category). 2. Observe response. | Backend returns 400 'invalid reportKey'; getRaw throws 'Request failed: 400'; error surfaced to the user, Sentry event recorded. | main-backend, GET /api/sync/reorder-levels/:reportKey |
| XCUT-50 | Reorder 401 on wrong client key | both | crosscutting | security | Build with mrpApiKey not equal to API_KEY_CLIENT (or missing). | 1. Open a report. 2. Observe response. | requireClientApiKey returns 401 Unauthorized; getRaw throws Exception('Unauthorized'). No report data leaks. | main-backend, GET /api/sync/reorder-levels/:reportKey |
| XCUT-51 | Reorder 500 on report build error | both | crosscutting | error | Force the backend report builder to fail (e.g. malformed client data). | 1. Open a report that triggers a server-side build error. 2. Observe response. | Backend returns 500; getRaw throws 'Request failed: 500' and reports the body to Sentry; report screen shows an error state, no partial CSV. | main-backend, supabase(client) |
| XCUT-52 | Web: report CSV is always refetched (no path_provider disk cache) | web | crosscutting | platform | Web build; signed in; a working report. | 1. Open a report (first fetch). 2. Navigate away and reopen the same report. 3. Observe network calls. | On web, report_screen has no on-disk CSV cache (path_provider unsupported) so it refetches the reorder endpoint each time. No file is written to disk. | main-backend, web-browser, local-disk |
| XCUT-53 | Native: report CSV cached to disk after first fetch | mobile | crosscutting | platform | Android build; signed in; a working report. | 1. Open a report (first fetch hits the network). 2. Reopen the same report. 3. Observe network vs disk read. | On Android the CSV is cached to disk on first fetch and read from file on subsequent opens (path_provider available); fewer network calls than web. | main-backend, local-disk |
| XCUT-54 | Desktop breakpoint (>=900) shows side-rail; <900 shows phone bottom-nav | web | crosscutting | platform | Web build on a resizable window. | 1. Open the app in a window >=900px wide. 2. Shrink the window below 900px. | At >=900 ScreenFrame renders _buildDesktop (AppSideNav rail width 96, full-width body, no centered card/cap). Below 900 it renders _buildPhone (centered 500px card + AppBottomNav). Layout switches live on resize. | web-browser |
| XCUT-57 | Web applies 1.4x text scaling to routes and modal sheets | web | crosscutting | platform | Web build. | 1. Open several screens and a modal bottom sheet (e.g. voucher detail). 2. Compare text size to a mobile build at default scale. | MaterialApp.builder wraps the child in MediaQuery(textScaler: TextScaler.linear(1.4)) only when kIsWeb, so web text (including modal sheets) is 1.4x larger. Mobile uses default scaling. | web-browser |
| XCUT-59 | Two-pane image+summary sheet on wide screen | web | crosscutting | platform | Web build in a window >=900px; a push_queue row with a stored image. | 1. Open the voucher detail sheet for a row that has an image in a wide window. 2. Then shrink below 900 and reopen. | isWideScreen (>=900) drives a two-pane layout (image + summary side by side). Below 900 it falls back to a single-pane/stacked sheet. Image still streams via getBytes. | main-backend, GET /api/sync/push-queue/:id/image/:page, web-browser |
| XCUT-61 | Live window resize across 900px swaps layout without restart | web | crosscutting | platform | Web build open in a resizable browser window. | 1. Start at ~1200px (desktop layout). 2. Slowly drag the window narrower past 900px and back. 3. Observe nav and content reflow. | LayoutBuilder/MediaQuery recompute on resize: side-rail<->bottom-nav and full-width<->card swap occur live at the 900 boundary with no crash, no lost navigation state. | web-browser |
| XCUT-62 | Web has no file share / on-disk image cache | web | crosscutting | platform | Web build; a row with an image. | 1. Open an invoice image on web. 2. Look for a 'share file' / 'save to disk' affordance and reopen to check caching. | No file-share option and no on-disk image cache on web (path_provider unsupported); image bytes are re-fetched each open. On Android these would be available. | web-browser, local-disk |
| XCUT-63 | Sentry is a no-op when DSN is empty (bare build) | both | crosscutting | edge | Build with no SENTRY_DSN (bare/default). | 1. Run the app and trigger an error path (force a backend failure). 2. Observe app behavior and confirm no events are sent. | With Config.sentryDsn='' the SentryFlutter.init, reportFailure, and reportHandledError are all behaviour-neutral no-ops; the app behaves exactly as before and no events are emitted. (Note: all three env files DO set a DSN, so only a bare build is a no-op.) | sentry |
| XCUT-64 | Failed push/report has app-side Sentry event but no correlated server event | both | crosscutting | data-integrity | Build with DSN; force an activate or report failure. | 1. Force an activate 409/500 or a report 500. 2. In Sentry, find the app event by request_id. 3. Search the backend for a correlated event with the same request_id. | DEFECT TO FLAG: Only the app-side event exists (tagged request_id/endpoint/status). The backend has no Sentry and ignores x-request-id, so there is no server-side counterpart. Push/report failures cannot be traced end-to-end. | sentry, main-backend |
| XCUT-68 | Two clients on different flavors operate on different Supabase projects (env isolation) | both | crosscutting | concurrency | Same user signed in on a testing build (yynuu) and a deployment build (ztugw) simultaneously. | 1. Sign in as the same account on both the testing and deployment builds. 2. Create/observe a push_queue row on each. 3. Check whether rows appear on the other client. | The two builds talk to different Supabase projects (yynuu vs ztugw), so realtime/queue changes do NOT cross between them. Confirms env-split data isolation — a user must use the correct flavor to see their real data. | supabase(yynuu), supabase(ztugw), push_queue, realtime |
| XCUT-71 | In-flight token refresh during a backend call does not duplicate or corrupt the request | both | crosscutting | concurrency | Signed in near token-expiry; ability to trigger a backend call while the token refreshes. | 1. Trigger an ApiClient.get exactly as the Firebase ID token is refreshing. 2. Observe the request's Authorization header and result. | _headers awaits getIdToken() which returns a fresh token; the single request carries a valid Bearer and completes once. No duplicate fire or stale-token 401 storm. | main-backend, firebase |

### P3 — Cosmetic / minor (84)

| ID | Title | Platform | Domain | Category | Preconditions | Steps | Expected | Integration points |
|----|-------|----------|--------|----------|---------------|-------|----------|--------------------|
| AUTH-11 | Edge: OTP backspace clears current box then steps back | both | auth_session | edge | On OtpScreen with several digits entered. | 1. Fill boxes 0-3, leaving focus at box 4 (empty). 2. Press backspace/delete (web) or '⌫' key (mobile) once — focus box empty case. 3. Press backspace again. | First backspace on an empty focused box steps focus back one and clears that box; backspace on a filled focused box clears it in place without stepping back. | firebase |
| AUTH-12 | Edge: tap a specific OTP box to re-target focus and overwrite | both | auth_session | edge | On OtpScreen with all 6 boxes filled. | 1. Tap box index 2 to move focus there. 2. Enter a new digit. | Focus moves to box 2 (active border/caret), the new digit overwrites box 2, then focus advances to box 3. | firebase |
| AUTH-40 | Platform: web applies 1.4x text scaling app-wide including auth screens | web | auth_session | platform | App in a web browser. | 1. Open LoginScreen, OtpScreen, SuccessScreen, SplashScreen on web. 2. Compare text size against the same screens on mobile. | kIsWeb MediaQuery textScaler 1.4x makes all auth-screen text ~40% larger; no clipping/overlap of labels, error banners, OTP boxes, or buttons. | firebase |
| AUTH-41 | Platform: web text is selectable/copyable via SelectionArea | web | auth_session | platform | Web browser, any auth screen. | 1. On LoginScreen/OtpScreen, drag-select the masked phone text or labels with the mouse. 2. Copy and paste elsewhere. | Displayed Text is selectable and copyable (SelectionArea wraps the gate), which is not Flutter web's default. Confirm no layout breakage from selection. | firebase |
| AUTH-43 | Platform: Android-only document scanner blocked while still allowing auth | web | auth_session | platform | Signed in on web/iOS. | 1. Attempt to invoke the document scanner from the web client. | ML Kit scanner shows 'only available on Android'; this does not affect auth/session — the user stays signed in and can use other flows. (Confirms platform gating is scoped to the scanner, not auth.) | firebase |
| AUTH-44 | Hot restart swallows Firebase 'duplicate-app' exception | both | auth_session | edge | Dev build; ability to hot-restart (or relaunch where native [DEFAULT] app stays registered). | 1. Trigger a hot restart of the running app. 2. Observe startup. | Firebase.initializeApp throws [core/duplicate-app]; main.dart swallows only that code and continues. App boots normally to Splash->gate; any other FirebaseException would rethrow. | firebase |
| AUTH-46 | Edge: masked phone display for short/edge inputs | both | auth_session | edge | Navigated to OtpScreen (requires Firebase to have accepted Send OTP). | 1. Reach OtpScreen with a normal 10-digit number and read the subtitle. 2. (If reachable) reach it with an unusual <2-digit raw — verify fallback. | Subtitle shows 'sent to +91 NNxxx xxxxx' with only the first 2 digits visible; if the cleaned phone has <2 digits it falls back to showing the raw phone. No full number is ever displayed. | firebase |
| AUTH-49 | Loading affordances on Send OTP (button disabled + progress bar) | both | auth_session | happy | LoginScreen with a valid number entered. | 1. Tap 'Send OTP →' and observe the UI until codeSent/verificationFailed. | During _loading the Send OTP button renders disabled (muted, no shadow, tap ignored) and a LinearProgressIndicator is shown; both clear when the callback returns. | firebase |
| AUTH-50 | Edge: keypad digit input ignored once all 6 boxes are filled (focus at 5) | both | auth_session | edge | On OtpScreen with all 6 digits entered (focus index at 5, last box filled). | 1. Enter another digit via keypad/keyboard. | Because _inputDigit advances focus only up to index 5, an extra digit overwrites box 5 in place (focus does not exceed 6); the code never exceeds 6 digits. Confirm overwrite behavior and that Verify uses the visible 6 digits. | firebase |
| SCAN-13 | Scanner page limit of 20 is enforced | mobile | scan_parse | edge | Android phone, scanner open. | 1. Attempt to scan more than 20 pages in one session 2. Observe the scanner behavior | The scanner caps the batch at 20 pages (pageLimit:20); the produced PDF contains at most 20 pages and the flow proceeds normally. | DocumentScanner |
| SCAN-15 | Processing snackbar text matches the chosen type | mobile | scan_parse | happy | Android phone. | 1. Scan once as Sale, note the snackbar 2. Scan once as Purchase, note the snackbar | Snackbar reads 'Your Sale voucher is being processed…' for sale and 'Your Purchase voucher is being processed…' for purchase, each visible ~2 seconds. | app_shell._enqueueScan |
| SCAN-23 | PDF file read failure before upload is caught | mobile | scan_parse | error | Android phone; the scanned PDF file is removed/unreadable before sendScanToParser reads it (race or deleted temp file). | 1. Trigger a scan where the temp PDF is no longer readable when readAsBytes runs 2. Observe | File(pdfPath).readAsBytes throws, caught, logged via debugPrint; no crash. (Note: the scan_jobs row may already exist, so the badge will sit until the 300s TTL since no parse landed.) | scan_uploader.sendScanToParser, scan_jobs table |
| SCAN-29 | Malformed/non-UUID job_id reaching the parser makes delete a no-op | mobile | scan_parse | edge | Craft a parse request whose &job_id is not a 36-char UUID (manual/test harness). | 1. POST a PDF to the parse URL with &job_id=not-a-uuid 2. Observe whether any scan_jobs DELETE happens | delete_scan_job fails the /[0-9a-fA-F-]{36}/ regex and is a no-op; if a corresponding scan_jobs row exists its badge relies on the 300s TTL to clear. No SQL injection via job_id. | parsing-service delete_scan_job, scan_jobs table |
| SCAN-34 | Oldest-start drives the loading timer ring | both | scan_parse | edge | At least one in-flight scan of a given type. | 1. Scan an invoice and open the Queue tab for that type 2. Watch the QueueLoadingTile ring 3. Scan a second of the same type a minute later | oldestStartFor returns the EARLIEST in-flight start; the looping step-timer ring is anchored to that oldest start and does not reset when a newer scan is added. | ScanJobsService.oldestStartFor, QueueLoadingTile |
| SCAN-40 | Capture tray opens with thumbnails after scanning | mobile | scan_parse | happy | Android phone with at least one captured shot. | 1. Tap the capture-batch thumbnail stack on the Camera screen 2. Observe the modal | A bottom-sheet grid of CapturedShot tiles opens showing the file name, type label ('Sale/Purchase receipt') and capture index, with a 'N shots' count matching the number of captures. | camera_screen._openCaptureTray |
| SCAN-41 | Capture tray no-op when empty | mobile | scan_parse | edge | Android phone with no captures (fresh shell). | 1. Tap the capture-batch thumbnail stack 2. Observe | Nothing happens (early return when captures is empty); no modal opens. | camera_screen._openCaptureTray |
| SCAN-42 | Save PDF from tray succeeds | mobile | scan_parse | happy | Android phone with a captured shot; storage writable. | 1. Open the capture tray, tap the download icon on a thumbnail 2. Choose a save location in the file picker and confirm | File is written as challan_<type>_<timestamp>.pdf and a 'PDF saved' snackbar appears. | camera_screen._savePdf, FilePicker |
| SCAN-43 | Save PDF failure shows error snackbar | mobile | scan_parse | error | Android phone; induce a save failure (e.g. cancel into an unwritable location or remove the source temp file). | 1. Open the tray, tap download on a thumbnail, trigger the failure 2. Observe | A 'Save failed: <error>' snackbar is shown; the app does not crash. | camera_screen._savePdf |
| SCAN-44 | Captures are in-memory and cleared when the shell is recreated | mobile | scan_parse | edge | Android phone with several captures in the tray. | 1. Note the capture count 2. Force the shell to be recreated (e.g. navigate in a way that rebuilds AccountantShell, or the scanner teardown recreates it) 3. Reopen the Camera screen and check the tray | The _captures list is empty (it lives only on app_shell in memory); the in-flight scan_jobs badge, however, persists because it is mirrored from Supabase, not from _captures. | app_shell._captures, scan_jobs table |
| SCAN-45 | Web has no capture tray and never produces captures | web | scan_parse | platform | Web session. | 1. Navigate to the Camera screen on web 2. Attempt to open the capture tray / look for thumbnails | No captures exist on web (the scan flow that populates _captures is Android-only and uses dart:io File); the tray is always empty and Save-PDF is unavailable. | camera_screen |
| SCAN-48 | Web 1.4x text scaling does not clip the processing snackbar / badge | web | scan_parse | platform | Web session. | 1. Trigger badge-related UI (have a phone start a scan so the web badge + loading tile render) 2. Inspect the QueueLoadingTile text and any snackbar at the web 1.4x scaling | Text renders at 1.4x scale without truncation or overlap; the Processing count and timer ring remain legible. | QueueLoadingTile |
| SCAN-49 | Web desktop breakpoint (>=900) shows side-rail and does not break the loading tile | web | scan_parse | platform | Web at a window width >= 900px. | 1. Have a phone start a scan so the badge climbs 2. Confirm the desktop layout: bottom-nav replaced by a side-rail; check the loading tile renders 3. Resize below 900px and confirm it switches back to bottom-nav | At >=900px the layout uses the side-rail (two-pane behavior available elsewhere) and the Processing loading tile/badge still render correctly; below 900px it reverts to the bottom-nav. | app_shell layout, QueueLoadingTile |
| SCAN-51 | created_at unparseable falls back to now() (badge still counts) | both | scan_parse | edge | Ability to inject a scan_jobs row with a null/invalid created_at (test harness). | 1. Insert/echo a scan_jobs row with a malformed created_at 2. Observe the badge | _toJob falls back to DateTime.now() so the row is treated as fresh and counted; it will be swept 300s after the fallback time. No crash from the bad timestamp. | ScanJobsService._toJob, scan_jobs table |
| SCAN-55 | job_id appends correctly because both base URLs already contain '?' | mobile | scan_parse | edge | Android phone; ability to inspect the outgoing parse URL. | 1. Scan a Sale invoice and capture the full request URL 2. Scan a Purchase invoice and capture the full request URL | Sale URL ends '...?type=sale&push=queue&job_id=<uuid>' and Purchase URL ends '...?purchase=all&source=runpod&job_id=<uuid>' — the bare '&job_id=' append is valid only because each base already has a query string. Flag that a future base URL without '?' would be malformed. | saleParseUrl, purchaseParseUrl |
| SCAN-56 | Oversized PDF returns parser 400 'Bad request size' and is logged only | mobile | scan_parse | error | Android phone; a scanned PDF larger than the parser's MAX_UPLOAD_BYTES (or craft an empty/zero-length body). | 1. Send an over-limit (or empty) PDF to the parse endpoint 2. Observe the app and the badge | Parser returns 400 ('Bad request size' when Content-Length<=0 or > MAX_UPLOAD_BYTES); the app logs the non-2xx via debugPrint only, shows no user error, and the scan_jobs row (if created) drains at the 300s TTL. | parsing-service, scan_jobs table |
| SCAN-59 | Non-Android batch chip is cosmetic on web | web | scan_parse | platform | Web session. | 1. Open the capture flow on web, choose Purchase (hits the gate) 2. Navigate to the Camera screen and observe the 'Purchase batch' chip | The _activeCaptureType chip/label may render but no scanning occurs; the tag has no functional effect on web beyond the cosmetic label. | camera_screen, app_shell platform gate |
| QUEUE-12 | Realtime UPDATE for an unknown (not-yet-loaded) id prepends it to the queue | both | queue_realtime | edge | Queue open; a row exists in Supabase that this client has not loaded (e.g. missed its INSERT). | 1. Issue a non-pushed UPDATE (e.g. edit_state change) to that unseen pending row. 2. Observe the queue. | The UPDATE handler upserts: since the id is unknown it prepends the row, so the queue self-heals and shows it even though the original INSERT was missed. | supabase:push_queue, realtime:push_queue_live |
| QUEUE-37 | Enter-on-Total field triggers push with the same guards as the button | both | queue_realtime | happy | A pending row; physical/soft keyboard available (primarily web/desktop). | 1. Open the row, focus the Total field at the end of the Enter-to-move chain, press Enter. | _submitFromTotal runs: respects _isSubmitting, applies the party-changed/items guard (snackbar if blocked), otherwise runs _confirmAndActivate exactly like the Push button. | POST /api/sync/push-queue/activate |
| QUEUE-44 | Discard of a seed/fresh-scan row with no __row_id is local-only | both | queue_realtime | edge | A seed/local sale entry not backed by a Supabase row (no __row_id). | 1. Open such a row, tap Delete, confirm. | onDiscard drops it locally and the sheet closes; because rowId is empty, NO Supabase delete runs and no 'Deleted.' DB op occurs. | supabase:push_queue |
| QUEUE-51 | Scan badge optimistic add is deduped against the realtime echo (no double count) | mobile | queue_realtime | edge | Android, scanning. | 1. Scan one document and watch the count immediately after the scan and again when the realtime INSERT echo arrives. | startScan adds the job locally; _add ignores the subsequent realtime INSERT for the same id (dedupe by id). The badge shows 1, not 2. | supabase:scan_jobs, realtime:scan_jobs_live |
| QUEUE-53 | Purchase rows with blank reference are never flagged as duplicates | both | queue_realtime | edge | Two purchase rows both with an empty/whitespace voucher_payload.reference. | 1. Open the Purchase tab and inspect both rows. | Blank/whitespace references are skipped, so neither row is tagged a duplicate even though both are reference-less (reference-less duplicates are undetectable by design). | supabase:push_queue |
| QUEUE-54 | Sale rows are never flagged as duplicates regardless of repeated references | both | queue_realtime | edge | Two sale rows sharing the same reference. | 1. Open the Sale tab and inspect the rows. | _queueDuplicateIds returns empty for the Sale tab; no duplicate tags appear on sale rows. | supabase:push_queue |
| QUEUE-56 | Row with no ledger_entries renders amount as ₹0 | both | queue_realtime | edge | A push_queue row whose voucher_payload lacks ledger_entries. | 1. Load the queue and read the row's amount. | amount = abs(first ledger_entry.amount) defaults to 0.0; the row shows ₹0 in the queue list. | supabase:push_queue |
| QUEUE-57 | Row with no party_name renders party 'Unknown' | both | queue_realtime | edge | A push_queue row whose voucher_payload has no party_name. | 1. Load the queue and read the row's party column. | rowToEntry falls back to 'Unknown' for party. | supabase:push_queue |
| QUEUE-60 | PHONE/NARROW: voucher sheet keeps the Image/Summary tab switcher and full width | mobile | queue_realtime | platform | Phone (narrow <900px); a voucher with a stored/scan image. | 1. Open the voucher detail sheet and toggle between Image and Summary tabs. | The sheet is full width; a _TabPill switches between Image and Summary (shown only because there is an image). The two-pane layout is NOT used. | GET /api/sync/push-queue/:id/image/:page |
| QUEUE-66 | Shell merge preserves per-id checked/status across realtime rebuilds | both | queue_realtime | edge | Queue with rows; a realtime event causes onEntriesChanged to fire. | 1. Observe a row's local UI state (e.g. status badge) before and after an unrelated realtime UPDATE rebuilds the list. | _onSupabaseEntriesChanged maps fresh rows by id and re-applies the existing checked/status from the prior list, so a rebuild does not reset those per-row UI fields. | supabase:push_queue, realtime:push_queue_live |
| QUEUE-67 | QueueBulkBar / multi-select is not wired: no bulk-confirm push exists | both | queue_realtime | edge | Queue with multiple rows. | 1. Look for any multi-select / select-all / bulk push affordance in the queue. | There is no live multi-select/bulk-confirm flow (QueueBulkBar and QueueEntry.checked exist but are not used by queue_screen.dart). 'checked' is only preserved across rebuilds; pushes are one row at a time. | supabase:push_queue |
| QUEUE-69 | Activate 400 when job_id missing/blank (backend contract) | both | queue_realtime | error | Ability to call POST /api/sync/push-queue/activate directly (the UI always supplies a rowId, so this is an API-contract check). | 1. Send POST /api/sync/push-queue/activate with x-api-key and an empty/missing job_id. | Backend returns 400 'job_id is required'. (UI cannot normally trigger this because _activate returns early when rowId is empty.) | POST /api/sync/push-queue/activate |
| QUEUE-73 | Edit/Add blocked while stock items cache is still loading | both | queue_realtime | edge | Just signed in; StockItemsCache still paginating (~13k items). | 1. Open a pending row and tap Edit (or Add) before the stock cache finishes loading. | Snackbar 'Loading of stock items in process, please wait'; edit mode does not start until the cache is ready. | supabase:stock_items |
| PUSH-51 | Purchase push shows no success/fail modal (sale-only) | both | voucher_edit_push | edge | Purchase row pushed successfully. | 1. Push a purchase voucher; wait for terminal result | Sheet pops and queue re-fetches but no sale-style success/fail dialog appears (modal is sale-only). | realtime:vd_status_{rowId} |
| PUSH-55 | Delete a seed/fresh-scan row with no __row_id (local only) | both | voucher_edit_push | edge | A row constructed without __row_id (fresh scan / seed). | 1. Open it, tap Delete, confirm | onDiscard drops it locally and the sheet pops; no Supabase delete is attempted (rowId empty -> early return). | supabase:push_queue |
| PUSH-70 | Web numeric column widths scaled x1.4 to match 1.4x text scaler | web | voucher_edit_push | platform | Open an editable voucher on web. | 1. Enter edit mode 2. Inspect Qty/Disc/Rate/ListPrice/Amount column widths and text overlap | Numeric columns are ~1.4x wider than mobile so the 1.4x web text scaling does not clip/overlap numbers. | supabase:push_queue |
| PUSH-71 | Invoice # autofocus only on wide layouts | web | voucher_edit_push | platform | Desktop/web wide layout vs phone. | 1. Enter edit mode on web (wide) 2. Then repeat on a phone | On wide, the Invoice # field autofocuses (keyboard-first Enter chain); on phone there is no autofocus (avoids popping the soft keyboard). | supabase:push_queue |
| PUSH-74 | Down-arrow on an item name opens the stock picker (hardware keyboard) | web | voucher_edit_push | platform | Web/desktop, row in edit mode, focus on an item name. | 1. Press Down arrow on a focused item name | The stock-item picker opens for re-pick; arrow keys navigate the list. | supabase:stock_items, supabase:voucher_items |
| PUSH-75 | Pinned items column header only on wide; inline+shared-scroll on phone | web | voucher_edit_push | platform | Open an editable voucher with many items on web vs phone. | 1. On web, scroll the items table vertically 2. On phone, scroll the items table horizontally | Web: SliverPersistentHeader pins the items header. Phone: header is inline inside the same horizontal SingleChildScrollView so header and rows share one horizontal offset (no misalignment). | supabase:push_queue |
| PUSH-76 | Summary capped/centered on wide (maxWidth 1080 web / 800) | web | voucher_edit_push | platform | Very wide browser window. | 1. Open a voucher with no image (Summary only) and observe width | Summary content is capped and centered (max ~1080 on web / 800 otherwise); it does not stretch full-bleed across an ultra-wide window. | supabase:push_queue |
| PUSH-77 | Desktop breakpoint resize toggles bottom-nav <-> side-rail and panes | web | voucher_edit_push | platform | Browser; a voucher sheet open with an image. | 1. Resize the window across the 900px breakpoint while the sheet is open | Crossing 900px switches between tabbed (single pane) and two-pane layouts (and bottom-nav<->side-rail at app level) without losing the open sheet's state. | main-backend:GET /api/sync/push-queue/:id/image/:page |
| PUSH-83 | Fresh-scan pendingPayload path is currently unwired (regression guard) | both | voucher_edit_push | edge | Code review / build inspection. | 1. Confirm both live constructions (queue_screen, history_screen) pass payload:, not pendingPayload: 2. Exercise the normal flows | No call site passes pendingPayload, so the _onPendingResolved / _DuplicatePurchaseDialog branch never triggers in the app; if a future change wires it, the duplicate-purchase dialog and processing state must be re-tested. | supabase:push_queue, parsing-service |
| PUSH-84 | Reorder/company-name 409 risk does not corrupt voucher push | both | voucher_edit_push | edge | Duplicate 'K V ENTERPRISES' company rows exist (known reorder 409 risk). | 1. Push a voucher while the company has duplicate rows 2. Confirm the push path omits company identity | The activate POST sends only {job_id} (no company_id/guid/name), so the optional company match is skipped and the duplicate-company 409 does NOT affect Push-to-Tally; only the reorder report is at risk. | main-backend:POST /api/sync/push-queue/activate |
| HIST-5 | Rows with empty id are skipped during initial load | both | history_images | edge | Craft a push_queue row with status='pushed' but an empty/null id (if achievable via direct DB insert). | 1. Insert a pushed row with empty id. 2. Open History. | The empty-id row is not added to the list (load loop skips ids that stringify to ''). No crash. Effectively unreachable from the UI. | Supabase: push_queue select (status='pushed') |
| HIST-16 | Card with empty rowId is non-tappable | both | history_images | edge | A HistoryEntry whose rowId is empty (rare; load loop normally skips empty ids). | 1. Attempt to tap such a card if present. | onTap is null — tapping does nothing, no sheet opens, no crash. | Supabase: push_queue |
| HIST-23 | Invalid id/page -> backend 400 -> error tile | both | history_images | error | Ability to force a malformed page index (e.g. negative) or empty id through the endpoint (manual/Postman) — UI normally sends valid 0..N-1. | 1. Issue GET /api/sync/push-queue/<id>/image/-1 (or empty id) with valid auth. 2. Observe the response. 3. In-app, if reachable, observe the error tile. | Backend returns 400 {error:'Invalid id or page'}. In-app FutureBuilder shows 'Couldn't load this page.' Tester confirms the UI never generates such a request itself. | Main backend: GET /api/sync/push-queue/:id/image/:page (400) |
| HIST-31 | GCS stream error mid-pipe -> that page's tile errors, others unaffected | both | history_images | error | Induce a mid-stream GCS read error (truncate object / kill connection) for one page of a multi-page row after headers sent. | 1. Open the multi-page Image view and load the affected page. | Backend logs and res.destroy()s the truncated response; getBytes throws -> that page shows 'Couldn't load this page.'; other pages (independent futures) still render correctly. | Main backend: GET image (stream error), GCS invoice bucket |
| HIST-40 | Duplicate realtime events for the same id do not duplicate the card | both | history_images | edge | History open; ability to emit two INSERT/UPDATE events for the same pushed row id. | 1. Fire two realtime events for the same row id. 2. Count the cards for that id. | Exactly one card (rows keyed by id in a Map; later event overwrites). Idempotent. | Realtime: history_push_queue |
| HIST-42 | Read-only history sheet opens vd_status_<rowId> channel that stays idle (terminal) | both | history_images | concurrency | History detail sheet opened for a pushed (terminal) row. | 1. Open a history card's detail sheet. 2. Leave it open briefly, then close it. 3. If possible, monitor the Supabase realtime connection for the channel. | The readOnly sheet subscribes to vd_status_<rowId> (shared sheet code) but since status is already 'pushed' (terminal) no status changes arrive — effectively idle. The channel is unsubscribed on sheet dispose (no leak). | Realtime: vd_status_<rowId>, Supabase: push_queue |
| HIST-43 | Two clients open the same row's image -> independent in-memory caches | both | history_images | concurrency | Same pushed scanned row reachable from two clients. | 1. Open the row's Image view on client A (pages cache). 2. Open the same row's Image view on client B. | Each client fetches its own copies via the backend (no shared cache); both render correctly. Closing one does not affect the other. | Main backend: GET image, GCS invoice bucket |
| HIST-46 | Web 1.4x text scaling renders History without overflow | web | history_images | platform | Web build (global 1.4x text scaling applied). | 1. Open History on web with several rows and long party names. 2. Inspect card text, month headers, filter tabs, and the detail summary. | Text is scaled 1.4x; party names/amounts/month headers wrap or ellipsize gracefully with no clipping/overflow exceptions; layout remains usable. | Supabase: push_queue |
| HIST-50 | Mobile bottom-nav + full-width sheet layout for History | mobile | history_images | platform | Android phone (width < 900). | 1. Open History on the phone and open a scanned row. 2. Observe navigation chrome and sheet width. | Bottom navigation bar is used (not side-rail); the detail sheet is full width with the Image/Summary toggle (single pane). No two-pane layout. | Supabase: push_queue, Main backend: GET image |
| HIST-56 | Large history list (thousands of pushed rows) loads and scrolls | both | history_images | edge | Project/tenant with a large number of pushed rows (e.g. 2,000+). | 1. Open History. 2. Scroll through multiple month groups. 3. Switch filter tabs. | List loads in one select (no pagination) and renders/scrolls without freezing or OOM; month grouping and sorting remain correct. Note load time and memory as observations (single un-paginated fetch is a scalability consideration). | Supabase: push_queue select (status='pushed') |
| HIST-57 | Month group header shows full month name and year; multi-year separation | both | history_images | happy | Pushed rows spanning the same month in two different years (e.g. June 2025 and June 2026). | 1. Open History and scroll through the groups. | Headers read full month + year (e.g. 'June 2025' and 'June 2026') as DISTINCT groups; cards are bucketed into the correct year group. | Supabase: push_queue (pushed_at/created_at) |
| RPT-2 | Chat tab shows static 'Coming Soon.....' with no backend call | both | reports | happy | Signed in. On the Report screen. | 1. Tap the 'Chat' tab (index 1). 2. Watch the AnimatedSwitcher transition. 3. Inspect network traffic. | A centered 'Coming Soon.....' text is shown. No API, Supabase, or realtime calls are made. Switching back to 'Insights' restores the list. | (none) |
| RPT-13 | Fully-blank CSV lines are dropped from the rendered rows | both | reports | edge | Backend/mock CSV contains some lines that are entirely empty fields between data rows. | 1. Open a report whose CSV has blank lines interspersed. 2. Count rendered rows vs CSV data lines. | parsed.skip(1).where((r)=>r.any(non-empty)) removes fully-blank rows; only rows with at least one non-empty cell are shown and counted in footerTrailing 'N rows'. | main-backend GET /api/sync/reorder-levels/:reportKey |
| RPT-14 | CSV EOL mismatch leaves a cosmetic trailing CR on the last column only | both | reports | edge | Backend joins lines with CRLF; app parses with eol '\n'. | 1. Open any report. 2. Inspect the last column header/value (scenario_name) for a trailing carriage return artifact (e.g. horizontal-scroll the sheet or share the CSV on mobile and open in an editor). | The last field per line (scenario_name and its header) carries a trailing '\r'. Because closing_stock_amount is NOT the last column, indexOf('closing_stock_amount') and the Σ are unaffected. Only cosmetic CR on scenario_name; no functional impact. | main-backend GET /api/sync/reorder-levels/:reportKey |
| RPT-18 | Σ of pre-rounded integers can drift from a true rupee total | both | reports | data-integrity | CSV closing_stock_amount values are Math.round + toLocaleString('en-IN') (paise dropped per row). | 1. Open a report with many rows whose true amounts have paise. 2. Compare the app Σ to a server-authoritative full-precision sum (compute from raw Supabase data). | Because per-row values are rounded server-side before the app sums them, the Σ can differ from a true full-precision total by a few rupees. Acceptable cosmetically but document the drift. | main-backend GET /api/sync/reorder-levels/:reportKey |
| RPT-25 | 400 invalid reportKey (not reachable from app UI) returns the keys list | both | reports | error | Ability to hit the endpoint with a bad reportKey directly (curl/proxy) — the app only ever sends valid R1-R7. | 1. Send GET /api/sync/reorder-levels/NOTAKEY?company_name=K V ENTERPRISES&format=csv with valid x-api-key. 2. Observe the response. | normalizeInventoryReportKey returns null -> HTTP 400 with body listing allowed keys (R1/act_now ... R7/full_portfolio_health). Confirms the app's 7 seed reportIds are all valid and this branch is not user-reachable. | main-backend GET /api/sync/reorder-levels/:reportKey |
| RPT-38 | WEB: 1.4x text scaling and desktop side-rail do not break the report sheet | web | reports | platform | Web build. Test at a wide (>=900px) and a narrow (<900px) viewport. | 1. Load the app on web at >=900px width: confirm side-rail nav. 2. Open a report and inspect layout/legibility (1.4x text scaling). 3. Shrink below 900px: confirm bottom-nav and re-open the report. | At >=900px the side-rail is shown; below 900px the bottom-nav. In both, the report bottom sheet renders at heightFactor 0.92 with 1.4x-scaled text that remains legible and non-overflowing. The Σ footer, search, and Close button all function. | (web layout) |
| RPT-39 | MOBILE: report sheet renders at 0.92 height and scrolls horizontally | mobile | reports | platform | Android phone, a report opened. | 1. Open a wide report (many columns) on a phone. 2. Scroll the spreadsheet horizontally and vertically. 3. Confirm the footer Σ and Share/Close controls. | Sheet opens at 92% screen height; columns (width 42 / 240 / 96...) are horizontally scrollable; rows scroll vertically. Footer shows Σ and 'Items' count; Share and Close are present and work. | local-disk cache |
| RPT-41 | reportId==null seed branch is dead code with current seed data | both | reports | edge | Source review / a build where a seed category temporarily has reportId=null. | 1. Confirm all 7 seedReportCategories have non-null reportId (R1-R7). 2. (Optional) temporarily set one reportId=null in a debug build and tap it. | With current seed data the else-branch (static columnLabels/category.rows/'Σ footerSum') is never reached — confirmed dead code. If forced via reportId=null, it renders the local static columns with no network call; verify it does not crash. | local-disk (seedReportCategories) |
| RPT-42 | Authorization Bearer is attached but ignored; report works while signed in | both | reports | edge | Signed in (Firebase JWT available). | 1. Capture the outgoing report GET headers. 2. Confirm Authorization Bearer <jwt> is present alongside x-api-key. 3. Confirm the request still succeeds purely on the x-api-key. | ApiClient attaches the Firebase Bearer JWT, but requireClientApiKey only checks x-api-key. The report returns 200 regardless of JWT validity. (Optional: an expired/absent JWT does not affect the report response.) | main-backend GET /api/sync/reorder-levels/:reportKey, Firebase Auth (JWT attached, unused) |
| RPT-45 | Failures are reported to Sentry with request_id/endpoint/status correlation | both | reports | error | Sentry DSN configured on app and backend. | 1. Trigger a report failure (e.g. 409 or network error). 2. In Sentry, find the corresponding event. | ApiClient.reportFailure captures an event tagged with request_id, endpoint '/api/sync/reorder-levels/<id>', status, and response body. The x-request-id matches between the app event and the backend event (cross-correlated). Confirms observability for report failures. | main-backend GET /api/sync/reorder-levels/:reportKey, Sentry |
| RPT-47 | Spinner clears even when the bottom sheet is dismissed immediately | both | reports | edge | A report loads successfully. | 1. Tap a report, let it open, then swipe-down/Close the sheet quickly. 2. Re-inspect the list row. | _loadingCategoryKey is set to null before showModalBottomSheet, so the row spinner is already cleared when the sheet opens; dismissing the sheet returns to a clean list with no stuck spinner. Re-tapping works again. | main-backend GET /api/sync/reorder-levels/:reportKey |
| RPT-48 | Navigating away mid-load does not crash (mounted guard) | both | reports | edge | Backend slow enough that the request is in flight. | 1. Tap a report; while loading, navigate away from the Report screen (switch tab/nav). 2. Let the request complete or fail. | The mounted checks guard setState and ScaffoldMessenger; if the widget is unmounted when the response/exception arrives, no setState-after-dispose exception is thrown and no sheet/snackbar is forced onto a disposed context. | main-backend GET /api/sync/reorder-levels/:reportKey |
| MAST-23 | Stock picker is touch-only on mobile (no hardware keyboard nav) | mobile | masters_caches | platform | On Android phone. Stock picker open. | 1. Type a query using the on-screen keyboard. 2. Scroll the list by touch and tap a row. | List filters and a tapped row is selected/added. No arrow-key highlight affordance is needed; selection works purely by touch. | UI (touch selection) |
| MAST-37 | Web 1.4x text scaling enlarges picker sheet rows and reduces visible count | web | masters_caches | platform | On web. Stock or party picker open with many rows. | 1. Open the stock picker on web and note row height / how many rows fit in the 0.72-height sheet. 2. Compare against the same picker on mobile (or a non-web build). | On web the app-wide TextScaler.linear(1.4) propagates into the modal sheet (it sits under the app MediaQuery), making rows taller and showing fewer rows per screen; layout remains usable (no clipping/overflow of names). | UI (web textScaler 1.4x) |
| MAST-39 | Desktop-web (>900px) two-pane layout: pickers function alongside the side-rail/two-pane sheet | web | masters_caches | platform | Web window wider than the 900px breakpoint. | 1. Widen the browser past 900px so the bottom-nav becomes a side-rail and the voucher opens as a two-pane image+summary sheet. 2. Open the stock and party pickers from within the two-pane voucher view. | Pickers open and select correctly within the desktop two-pane layout; selection updates the summary pane. No layout breakage from the wider breakpoint. | UI (responsive breakpoint 900), Supabase: stock_items / ledgers |
| MAST-40 | Search performance: typing in the ~13k-item stock picker stays responsive (no debounce) | web | masters_caches | edge | Full ~13k catalog loaded, 'All items' view, on a low-end web client. | 1. Open the 'All items' stock picker. 2. Type and delete characters quickly to trigger repeated full-scan filters. | Each keystroke runs a linear .contains scan over ~13k items inside setState (no debounce). Acceptable: no crash; observe whether perceptible jank occurs (logged as a perf note rather than a functional failure). | UI (_applyFilter linear scan) |
| MAST-44 | unitFor/partCodeFor return '' for blank or unknown names (defensive default) | both | masters_caches | edge | Caches loaded. | 1. Trigger an add-item path where the selected item's name is empty or not in the catalog (e.g. a party-history row with an empty stock_item_name, which is skipped, or a name absent from catalog). 2. Inspect the resolved unit/partCode. | unitFor/partCodeFor return '' for empty/unknown names (early-return on empty target); the add path then defaults unit to 'NOS' and partCode to ''. Rows with empty stock_item_name are skipped entirely in party-history dedup. | Supabase: stock_items (unitFor/partCodeFor) |
| XCUT-12 | SplashScreen shown while auth state is resolving | both | crosscutting | happy | App launching with slow auth restore. | 1. Launch the app / refresh web with throttled network. 2. Observe the transient state before login or shell. | While StreamBuilder is in ConnectionState.waiting, SplashScreen is displayed (no flash of LoginScreen for an already-signed-in user). | firebase |
| XCUT-21 | Sentry user cleared on sign-out | both | crosscutting | happy | Build with DSN; signed in with Sentry user set. | 1. Sign out. 2. Trigger a handled error while signed out (if reachable). 3. Inspect the Sentry event's user field. | After sign-out the Sentry user scope is cleared so subsequent events are not attributed to the previous uid. | sentry, firebase |
| XCUT-58 | Web text is selectable/copyable via SelectionArea | web | crosscutting | platform | Web build. | 1. On any screen, click-drag to select a Text label and copy it. | Text is selectable and copyable because home is wrapped in SelectionArea (web canvas text is otherwise non-selectable). On mobile this has no visible effect. | web-browser |
| XCUT-60 | ResponsiveCenter caps form width at 440 on wide screens only | both | crosscutting | platform | Any build; a form screen. | 1. View a form on a >=900px wide window/desktop. 2. View the same form on a narrow phone window. | On wide screens forms are capped at kFormMaxWidth 440 and centered; on phone widths they fill the 500px card. No edge-to-edge stretched form on desktop. | web-browser |
| XCUT-65 | SentryHttpClient records a breadcrumb per backend request | both | crosscutting | happy | Build with DSN; signed in. | 1. Make several ApiClient calls. 2. Then trigger a crash/error and inspect the Sentry event breadcrumbs. | Each request leaves an http breadcrumb (captureFailedRequests is OFF, breadcrumbs ON) so the captured event carries recent call history; sentry-trace is propagated when a span is active. | sentry, main-backend |
| XCUT-66 | Handled Supabase errors reported via reportHandledError | both | crosscutting | happy | Build with DSN; a direct Supabase op that fails (e.g. RLS denial or 400). | 1. Trigger a direct Supabase read/write that returns a handled error. 2. Inspect Sentry. | The handled Supabase error is captured via reportHandledError (not a crash) and appears in Sentry; the app continues without crashing. | sentry, supabase |
| XCUT-72 | Same x-request-id format is unique per call (no collisions under rapid calls) | both | crosscutting | edge | Ability to observe outgoing headers for many rapid calls. | 1. Fire several ApiClient calls in quick succession. 2. Collect the x-request-id values. | newRequestId() = microsecond-timestamp-hex + random-hex; values are unique across rapid calls. (Cosmetic: even if duplicated, backend ignores it, but uniqueness aids app-side Sentry grouping.) | sentry, main-backend |
